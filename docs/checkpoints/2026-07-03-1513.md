# Checkpoint — 2026-07-03 15:13

## Session summary

Continuation of the long-running `PHPCraftdream/crush` fork session
(across a resume + auto-compact). This slice covered three distinct
threads.

**Git alias fix**: user asked for `git hist` to show minute-precision
timestamps. Updated the global alias
(`~/.gitconfig` via `git config --global alias.hist`) from
`--date=short` to `--date=format:'%Y-%m-%d %H:%M'`. Verified with
`git hist 5`.

**Upstream merge audit (recon only, no porting)**: fetched
`origin/main` (charmbracelet/crush) and diffed 187 commits ahead of
fork `main`. Bucketed per fork CLAUDE.md's golden rule into SKIP
(~125 — multi-client shared-workspace server model, an entire bangmode
TUI feature, TUI cosmetics, CLA-bot spam, version tags, auto-update
chores), EVAL (~50 — openai-compat provider auto-discovery, oauth/
config defensive fixes), and PORT (~10 candidates: tool-call JSON
validation before storing, bounded glob search, oauth token-lifetime
hardening, event-subscription panic on cancel, non-interactive env
vars to prevent editor hangs, etc.). No commits were cherry-picked —
operator has not yet greenlit porting; this was pure reconnaissance.
Result was reported inline, not written to a file.

**YOLO-mode removal (the main deliverable this slice)**: operator
asked to research, then implement, full removal of the per-session
YOLO auto-approve UI feature while explicitly preserving the
non-interactive auto-approve machinery (`AutoApproveSession()`, used
by `crush run` and sub-agents) and the `yolo_enabled` DB column
(kept in schema for backward compat, just stopped being written).
Investigated via an Explore sub-agent first (40 files hit by a yolo
grep), then confirmed scope with the user via two AskUserQuestion
picks: (1) keep AutoApproveSession/non-interactive path untouched,
(2) keep the DB column, remove only the code paths that read/write
it (no DROP COLUMN migration). Implementation was delegated to two
parallel `om` (Opus 4.8 medium) sub-agents — one for Go backend, one
for the web frontend — per the user's explicit request ("реализуй
через @om агентов"), with an added constraint mid-flight that neither
agent may run modifying git commands (commit/push/etc.); both agents
were instructed accordingly and neither committed anything.

Backend agent removed: `handleSetYolo`, `CmdSetYolo`,
`SetYoloPayload`, `ConfigWire.Yolo`, the per-session DB-yolo
auto-grant check inside `permission.Request()`, `Session.SetYolo()`
and the `SetSessionYolo` sqlc query (hand-edited generated code after
a stock `sqlc generate` proved to rewrite unrelated files and broke
the build — reverted that and did a surgical hand-edit instead), and
the `--yolo`/`-y` CLI flag from `root.go`. Frontend agent removed:
`$yolo` atom + `setYolo()`, all WS sync/auto-grant logic in
`useWS.ts`, the Yolo button in `ChatToolbar.tsx` and
`PermissionDialog.tsx`, and deleted the three `yolo*.spec.ts` e2e
test files. Both agents left `Session.YoloEnabled` (Go struct field
and TS type field) in place, read-only, since the DB column and its
scan remain.

After both agents reported back, the orchestrating session personally
verified rather than trusting the envelopes: confirmed `git log`/
`git status` showed zero new commits and HEAD unmoved (the backend
agent's internal `git checkout` on its own over-generated sqlc output
was a local working-tree revert, not a history-modifying action — no
concern), ran `go build ./...` (clean), `pnpm exec tsc --noEmit` in
`web/` (5 pre-existing, unrelated errors — none new), and re-ran Go
tests in the touched packages personally (`internal/permission`,
`internal/session`, `internal/db`, `internal/app`, `internal/cmd` —
all `ok`). Grepped for residual yolo mentions and found one dead
comment left behind in `useWS.ts` referencing removed logic — fixed
that by hand as a small follow-up edit.

User then said "сделай коммит" — staged only the 19
yolo-removal files explicitly (excluding the standing intentional
`web/dist/.gitkeep` deletion and the still-untracked prior checkpoint
doc) and committed as `5c323b55`. Not pushed — no push request yet
this slice.

## Active goal
none

## TaskList
empty — no tasks were created this slice; all work was tracked
conversationally (Explore recon → AskUserQuestion confirm → two
parallel om agents → personal verification → commit).

## Decisions
- **YOLO removal scope**: strip UI/session toggle only; keep
  non-interactive `AutoApproveSession()` and the `yolo_enabled` DB
  column untouched — confirmed explicitly via AskUserQuestion before
  any code was touched.
- **Delegate implementation to two parallel `om` sub-agents** (backend
  Go, frontend web) rather than one sequential agent or doing it
  directly — files don't overlap, so parallel was safe and faster.
  User explicitly authorized sub-agent use for this task (required by
  the fork's global CLAUDE.md rule against auto-invoking sub-agents).
- **No git modifying commands inside sub-agents** — user's mid-flight
  instruction; both agents were already told not to commit/push, and
  post-hoc verification (`git log`/`git status`) confirmed compliance.
- **Hand-edit generated sqlc code instead of blind `sqlc generate`**
  — the backend agent found that regenerating rewrote unrelated
  generated files (dropped `DeletedTodos` from a params struct,
  touched `messages.sql.go`) and broke the build; reverted and did a
  surgical manual removal of just the `SetSessionYolo` pieces.
- **Upstream merge**: cherry-pick-only strategy reaffirmed, recon
  done, nothing ported yet — operator has not chosen which PORT
  candidates to act on.

## Open questions
- Upstream audit: operator has not yet said which of the ~10 PORT
  candidates (tool-call JSON validation, bounded glob, oauth
  hardening, etc.) to actually cherry-pick, or in what order. No file
  was written for the audit findings — they exist only in this
  session's transcript, so if this checkpoint is the resume point,
  the audit may need to be re-run or the operator may need to be
  asked to re-request the list.
- `internal/config/config.go`'s `SkipRequests bool` field is now dead
  (the backend agent found its only writer, the `--yolo` flag, was
  removed) — left in place as harmless deadcode per the "minimal
  edit" instruction. Not raised as a question to the user yet; worth
  flagging if a cleanup pass ever touches config.go.
- Same two long-standing un-observed threads from earlier checkpoints
  remain open: GLM `["high","max"]` effort-level restriction (real
  z.ai API limit vs fork conservatism, unconfirmed) and the original
  550k vs 235k post-compact prompt_tokens discrepancy (symptom fixed
  DB-side, root cause never traced at the wire level).

## Repo state
```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-06-28-2158.md
```
```
5c323b55 feat: remove per-session YOLO UI, keep non-interactive auto-approve
6ad27a35 docs: session checkpoint 2026-06-28-1758
e47711a0 fix(build): switch build.go to pnpm after the web/ migration
ceb4368f chore: Renovate config with 30-day minimumReleaseAge
bccb844b chore(web): migrate from npm to pnpm with 30-day minimumReleaseAge
```
