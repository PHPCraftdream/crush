# Checkpoint — 2026-07-05 20:39

## Session summary

Long session on the `PHPCraftdream/crush` fork focused on reviewing +
hardening two external contributor PRs and running the npm release
pipeline. The through-line: the fork publishes prebuilt binaries to npm
under the `@phpcraftdream` scope via `.github/workflows/publish-fork-npm.yml`,
triggered by `npm-vX.Y.Z` tags (a fork-specific prefix chosen because the
repo shares upstream's 180+ `vX.Y.Z` tag history and would otherwise
collide).

Work completed this session, in order:
- **PR #2** ("Harden scripted provider setup and package version
  reporting", contributor ayleen): reviewed via the /review skill,
  then applied fixes to the contributor branch via maintainer-edit
  (maintainerCanModify=true) — ZAI api-key resolution error vs empty
  distinction, debug-notice gating on cfg.Options.Debug, workflow
  version-verify robustness (whole-token match + no `|| true`), shared
  resolveModelRole dedup, Fork-patch markers, and a schema comma-escape
  fix. Merged (22e672a1) after enabling the repo's Dependency graph
  (the `security` job's dependency-review-action was failing purely
  because that GitHub setting was off on the fork — one-time toggle,
  unrelated to code).
- **PR #3** ("restrict permissions for non-interactive runs",
  contributor ayleen): reviewed via /review (ox agent) — surfaced 5
  findings. Applied fixes over 3 commits on the contributor branch:
  (1) `aa44b63a` don't arm the run allowlist in app.New() (it leaked
  restrict into interactive web/TUI sessions), replace substring
  chaining-guard with a real mvdan/sh parse (closed `\n` and `&`
  bypass), make glob cross-platform (glob→anchored-regexp) + apply the
  compound guard to glob (this was a cross-platform security hole:
  filepath.Match let `glob:git *` match `git diff && rm` on BOTH
  Linux and Windows for commands without `/`; also fixed the red
  windows-latest build), BashToolName cross-package guard test,
  strconv.Quote, skip/YOLO-precedence docs; (2) `4bd66ebb` escape
  commas in RunPermissions jsonschema tags so `crush schema` stops
  truncating descriptions; (3) `0e67b16c` extract the compound check
  into a single `shell.IsCompoundCommand` primitive used by BOTH the
  permission allowlist AND the interactive bash safe-read-only
  fast-path in tools/bash.go — the latter had the SAME `\n`/`&`
  substring-scan gap, meaning a newline/&-chained command behind a
  safe prefix ran WITHOUT a permission prompt. Self-reviewed the 3
  commits at /fx (no correctness bugs found; two cosmetic nits noted).
  Merged (753668b5).
- **Release 0.1.3**: bumped npm/crush/package.json 0.1.2→0.1.3,
  updated npm/crush/CHANGELOG.md, committed (0328e33a), pushed, CI
  green on main (build 3-OS/lint/security), tagged `npm-v0.1.3`,
  pushed tag → publish-fork-npm ran green (verify confirmed `v0.1.3`,
  all 6 packages published). Confirmed live on registry.

Currently in flight / unresolved: the operator asked to unpublish all
prior npm versions (0.1.0/0.1.1/0.1.2 across the meta + 5 platform
packages = 18) to avoid cluttering npm. The operator ran the
`npm unpublish --force` loop and npm's CLI printed the success
`- pkg@version` lines for all 18 — BUT the registry read path still
serves them 6+ minutes later (tarball crush-0.1.0.tgz → HTTP 200,
packument still lists 0.1.0–0.1.3, `npm view --prefer-online` resolves
0.1.0). The operator then reported "некоторые не удалятся - старые уже"
(some won't delete, they're old) — npm is refusing to actually remove
some/all old versions. I proposed `npm deprecate` as the clean
alternative (works on any version with no 72h limit, marks them with a
warning so nobody installs them, `latest` stays 0.1.3) and gave the
deprecate loop. Awaiting the operator running it; offered to re-verify
registry state afterward. NOTE: I cannot run npm unpublish/deprecate
myself — this machine has no npm auth (`npm whoami` → ENEEDAUTH);
publishing only happens via NPM_TOKEN in CI. All npm mutations are
operator-run.

No /goal or /babysit timers active. TaskList empty (session ran in
single-context mode, no TaskCreate used).

## Active goal
none

## TaskList
empty — TaskList returns no tasks; this session tracked work
conversationally per request/response, no TaskCreate/TaskUpdate used.

## Decisions
- **Fork PR fixes go on the contributor branch via maintainer-edit,
  merged normally** — both PR #2 and PR #3 were external (ayleen)
  with maintainerCanModify=true, so fixes were pushed straight to
  their branches rather than opening follow-up PRs.
- **Extract `shell.IsCompoundCommand` as one shared primitive** rather
  than leaving two substring-scan copies (permission + tools/safe.go).
  Placed in internal/shell (already owns mvdan/sh; no import cycle:
  shell does not import permission or tools). Both callers route
  through it; the mvdan-parse closes the `\n`/`&` gap in both.
- **npm tag scheme is `npm-vX.Y.Z`** (not bare `vX.Y.Z`) to avoid
  colliding with upstream's inherited tag history. The Go binary's
  embedded version stays clean `vX.Y.Z` (the `npm-` prefix is CI-only).
- **Use `npm deprecate` when unpublish is refused** for old versions —
  proposed to the operator as the way to hide 0.1.0–0.1.2 since npm
  won't fully unpublish them.
- **Dependency graph must stay ON in repo settings** — the fork's
  `security` workflow (dependency-review-action) hard-fails without it;
  toggled on this session to unblock PR CI.

## Open questions
- Will the old npm versions (0.1.0/0.1.1/0.1.2) actually disappear?
  npm CLI reported unpublish success but the registry still serves
  them; operator says some won't delete. Pending: operator runs the
  `npm deprecate` loop; then re-verify. If deprecate is the accepted
  end-state, "cleanup" means deprecated-not-deleted.
- The two cosmetic nits in PR #3 (config.go doc-comment lost column
  alignment after the AllowBash rewrite; glob's `value` field is
  dead-stored now that matching uses `re`) were left un-fixed as
  optional polish — operator did not ask to fix them.

## Repo state
```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-06-28-2158.md
?? docs/checkpoints/2026-07-03-1513.md
?? docs/checkpoints/2026-07-04-2234.md
```
```
0328e33a chore(npm): bump to 0.1.3, changelog for restricted-run permissions
753668b5 Merge pull request #3 from ayleen/feat/run-bash-allowlist
0e67b16c refactor(shell): extract shared IsCompoundCommand; close bash safe-path chaining bypass
4bd66ebb fix(schema): escape commas in run-permission descriptions; refresh allow_bash text
aa44b63a fix(review): address run-allowlist review — interactive leak, chaining bypass, cross-platform glob
```
