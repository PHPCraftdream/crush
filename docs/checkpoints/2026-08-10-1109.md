# Checkpoint — 2026-08-10 11:09

## Session summary

Продолжение долгого `/babygoal`-раунда, теперь в фазе "второй проход @oh-ревью после фиксов". Задачи #337-349 полностью закрыты и закоммичены (последний тематический коммит `781e6413`). Первый финальный `@oh`-ревью диапазона `ac0536c4..38c00b14` дал NO-GO с 6 находками (P0-1, P0-2, P1-1, P2-1, P2-2/P2-3, P2-4, P3-nits) — все исправлены и закоммичены в `781e6413` с собственными revert-check'ами оркестратора.

Запущен ВТОРОЙ (подтверждающий) проход `@oh` поверх `781e6413` — он вернул **NO-GO повторно**, но на этот раз указал НОВЫЕ дефекты, внесённые самим фиксом:
- **NEW-1 (MAJOR/BLOCKER, подтверждено)**: гонка данных на `app.AgentCoordinator` между записывающей горутиной (`InitCoderAgent`, вызывается синхронно внутри `App.New`) и горутиной пула, читающей то же поле без синхронизации через ленивый адаптер — пул стартовал РАНЬШЕ `InitCoderAgent`. Плюс отдельный путь потери работы: если `cfg.IsConfigured()==false` или `InitCoderAgent` падает с ошибкой, ранее запущенный пул получал "coordinator not ready yet" как обычную retryable-ошибку и мог удалить durable-запись через ~10 тиков. **ИСПРАВЛЕНО**: перенёс блок `app.RunQueuePump = ...; app.RunQueuePump.Start()` из середины `App.New` (после `go mcp.Initialize`) в конец функции — СТРОГО ПОСЛЕ успешного `app.InitCoderAgent(ctx)`. Это устраняет и гонку, и путь потери работы одновременно. Ленивое чтение `a.app.AgentCoordinator` в `coordinatorAdapterImpl.Run()` теперь defense-in-depth, а не первичный фикс — подтверждено экспериментально: revert только ленивого чтения (при верном порядке) → PASS теста-обёртки `TestAppNew_RunQueuePump_ExecutesRealEnqueuedCall`; revert порядка (при сохранённом ленивом чтении) → ТОЖЕ PASS этого же теста (тест не ловит гонку порядка, т.к. работа кладётся в очередь уже ПОСЛЕ `app.New()`). Написан ОТДЕЛЬНЫЙ тест `TestAppNew_RunQueuePump_OrderingRace` (`internal/app/p348_p0_1_ordering_race_test.go`) — предзаполняет durable-очередь ДО вызова `app.New()` (имитируя восстановление сессии после рестарта процесса), что и есть реальный сценарий гонки.
- **NEW-2 (BLOCKER, подтверждено и исправлено)**: новый тест `p348_p0_1_pump_coordinator_wiring_test.go` вызывал реальный `app.New()` без изоляции окружения — тот же класс отказа, что уже документирован в `internal/cmd/providers_test.go` ("this is the exact path that previously hung a stress run for 9+ minutes"). **ИСПРАВЛЕНО**: добавлен блок `t.Setenv` (XDG_DATA_HOME/CRUSH_GLOBAL_DATA/XDG_CONFIG_HOME/CRUSH_GLOBAL_CONFIG/CRUSH_PROVIDER_CACHE_ONLY) по образцу `runProvidersCmdInIsolatedApp`, применён и к новому `p348_p0_1_ordering_race_test.go`.
- **NEW-3 (MEDIUM, исправлено)**: после удаления инкремента `attempts` из `LeaseRunQueueEntryByID` (часть фикса P0-2), записи, чьё выполнение падает ДО нормального Ack/Nack (краш процесса, паника, зависание дольше `RunQueueLeaseTTL`=30с), никогда не накапливают `attempts` — `CleanupExpiredLeases` возвращает их в pending, не трогая счётчик → потенциальный бесконечный цикл для "ядовитой" записи. **ИСПРАВЛЕНО**: добавлен `attempts = attempts + 1` в `CleanupExpiredLeases` (SQL пересобран через `sqlc generate` — ОБЯЗАТЕЛЬНО убирать апострофы/двойные кавычки из SQL-комментариев, иначе naive quote-tracking парсера ANTLR ломает разбиение на statements, что уже дважды ловилось в этой сессии).
- **NEW-4/NEW-5 (NIT, устные, не проверено кодом на момент чекпоинта)**: устаревшая ссылка на "200ms" в doc-комментарии `lock.go` (нужно поменять на 500ms, синхронизировать с изменённым порогом в `release_gate_test.go`); неточная формулировка в комментарии к `TestRunQueue_PumpLifecycle_GracefulShutdown`'s `require.NotPanics` про "panic or hang" (Stop() безопасен по построению — гард `if !p.started`). ЕЩЁ НЕ ИСПРАВЛЕНЫ на момент этого чекпоинта.
- **NEW-6 (MINOR, не исправлено)**: `docs/release_gate_summary.md`/`docs/release_gate_report.md` — untracked-файлы, всё ещё содержат опровергнутое утверждение "confirmed PRE-EXISTING via git stash" про P1-1-регрессию (задача #337). Нужно поправить.

Также в этой части сессии: пользователь попросил переключиться на делегирование через агента `@sh` вместо `/crush` — учтено, но пока делегирования не было (вся работа этого раунда делалась напрямую, без саб-агентов), т.к. фиксы точечные и требуют глубокого контекста уже накопленного в разговоре.

**Сейчас в процессе**: перепроверяю revert-check для `TestAppNew_RunQueuePump_OrderingRace` под `-race -count=15` в фоне (первый прогон `-count=10` дал смешанный результат — 6 PASS, потом обрыв на "FAIL" без видимого race-report, возможно из-за `tail -80` обрезки; второй чистый прогон без `tail` дал 10/10 PASS — то есть гонка нестабильно воспроизводится, нужно больше итераций чтобы поймать буквальный `WARNING: DATA RACE` вывод race-детектора и подтвердить механизм, а не просто "иногда падает по непонятной причине"). Результат этого фонового прогона ещё не получен.

## Active goal

Армированное условие `/goal` Stop-hook (дословно, из исходного вызова `/babygoal`):

> реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу

## TaskList

### in_progress
- #350 Address @oh final review findings for #337-349 (без blockedBy) — второй проход правок в разгаре, финальная верификация + коммит ещё не сделаны

### recently completed
- #337-#349 — все закрыты в предыдущей части сессии (см. `git log`, коммиты от `ac0536c4` до `38c00b14`)

## Decisions

- **Перенос старта пула строго после `InitCoderAgent`** выбран как ПЕРВИЧНЫЙ фикс NEW-1 (не просто ленивое чтение поля) — устраняет гонку данных архитектурно, а не маскирует её.
- **Ленивое чтение `AgentCoordinator` оставлено как defense-in-depth**, а не убрано — не мешает, страхует от будущих изменений порядка в `App.New`.
- **Написан отдельный тест на саму гонку порядка** (`TestAppNew_RunQueuePump_OrderingRace`), а не переиспользован/расширен исходный `TestAppNew_RunQueuePump_ExecutesRealEnqueuedCall` — потому что экспериментально подтверждено, что исходный тест НЕ способен поймать эту гонку в принципе (работа кладётся в очередь после `app.New()` возвращает управление).
- **Изоляция окружения для новых тестов через `app.New()`** — скопирован проверенный паттерн из `internal/cmd/providers_test.go` (`runProvidersCmdInIsolatedApp`), а не изобретён заново, чтобы не повторить ту же 9-минутную зависшую CI-сессию, которую этот паттерн уже один раз предотвратил.
- **`sqlc`-комментарии — без апострофов и двойных кавычек**: дважды в этой сессии специальные символы в SQL-комментариях (апостроф в "turns' worth", двойные кавычки вокруг фраз) ломали ANTLR-парсер sqlc с непонятными ошибками в НЕСВЯЗАННЫХ statements — впредь избегать любой пунктуации кроме дефисов/точек в комментариях `.sql`-файлов.

## Open questions

Нет блокирующих для пользователя. Технически открыто: результат фонового `-race -count=15` прогона `TestAppNew_RunQueuePump_OrderingRace` ещё не получен — нужно решить, является ли наблюдаемая нестабильность (1 FAIL из 10 в первом прогоне, 0 из 10 во втором) настоящей гонкой (тогда NEW-1's ordering-fix точно нужен и уже применён) или шумом теста (тогда нужно донастроить сам тест). Порядковый фикс (перенос `pump.Start()`) уже применён в `internal/app/app.go` независимо от исхода этой проверки — он корректен по построению (устраняет саму возможность гонки), так что это не блокирует прогресс, только подтверждение качества revert-check'а.

Ещё не сделано: NEW-4, NEW-5, NEW-6 находки (мелкие доки/комментарии) и итоговая полная верификация (gofmt/build/vet/lint/-race по всем трём пакетам/`-count=2`) + финальный коммит + возможен третий подтверждающий проход `@oh`.

## Repo state

```
 M internal/app/app.go
 M internal/app/p348_p0_1_pump_coordinator_wiring_test.go
 M internal/db/querier.go
 M internal/db/run_queue.sql.go
 M internal/db/sql/run_queue.sql
?? dev/
?? internal/app/p348_p0_1_ordering_race_test.go
```

```
781e6413 fix(agent,app,session,db): close all NO-GO findings from the final @oh review of #337-349
38c00b14 docs: changelog, checkpoints, and #349 deferral plan for the #337-349 round
3a8d3253 test(agent,app,session): executable release gate suite for #337-347 (#348)
21c20f90 docs(agent): correct doc comments that contradicted the code after #339/#340 (#347)
74034db3 test(agent,app): rewrite/remove vacuous and mislabeled regression tests (#346)
```
