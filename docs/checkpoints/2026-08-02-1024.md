# Checkpoint — 2026-08-02 10:24

## Session summary

Final checkpoint closing out the `/babygoal` review-fix cycle documented in `2026-08-02-0955.md`. After that checkpoint was written and committed, a Stop hook (condition: `@sh` + commits between tasks, then `/checkpoint` + changelog + commit-all-`.md` + `@oh` review at the end) fired and correctly caught that the round-2 fixes (#204-210) had gotten commits + changelog but no checkpoint, no markdown commit for the checkpoint file itself, and no fresh `@oh` review of their own — that gap is what this final stretch closed.

Sequence: wrote and committed `2026-08-02-0955.md` (plus the earlier, previously-uncommitted `2026-08-02-0326.md`), then launched a second `@oh` review scoped to the full `e9544a8f..HEAD` range with explicit instruction to focus fresh eyes on the 10 previously-unreviewed round-2 commits. That review confirmed all 6 round-2 fixes (#204/205/206/207/209/210) genuinely close what they claim, found zero hangs/deadlocks/data-races/correctness regressions, and separately found and fixed the pre-existing test flake (`checkpoint_stall_probe_test.go`) is real but unrelated to this range (confirmed via git blame/merge-base). It did surface two new, real problems in the round-2 fixes:

1. `internal/agent/agentic_fetch_tool.go`'s nested delegation agent never set `IsSubAgent`, so it was invisible to task #205's parent/child asymmetric-grace fix and quietly kept the old symmetric-cancel-out bug on that one path. Fixed directly by the orchestrator (one-line, `IsSubAgent: true`) rather than delegating — too small to warrant a sub-agent round-trip.
2. Task #205's own doc comment and its regression test overstated the guarantee: `toolCleanupGraceDefault` (90s) only protects a child that wedges *early* (within ~90s of being delegated to), not one that works productively for a while and wedges deep into its turn — the parent still wins that race, same as before #205. Not a correctness bug (the parent still terminates; cost transfer is already cancel-immune per #197) — just an inaccurate claim. Corrected the doc comment with honest, worked-through math, renamed the existing test to `_EarlyWedge` to describe what it actually proves, and added a companion test (`_LateWedgeParentStillWinsKnownLimitation`) that documents the uncovered case as an explicit passing assertion instead of a silent gap.

Both fixes independently verified by the orchestrator (build/vet/targeted tests/full uncached `-race` run of `internal/agent`) before committing, same discipline as every other commit this session. `CHANGELOG.md` updated with both. This is genuinely the end of this review-fix cycle — task #208 (the remaining lower-priority backlog, now with 3 more items added by the second review: a `data_directory`-hardcoding bug affecting both `sessions kill` and `sessions reset --force`, an undocumented truncation-on-idle behavior change in the #210 stdin fix, and a reintroduced mutable-package-global pattern in `root_test.go`) is left exactly where it was: tracked, prioritized, not fixed, not requested.

## Active goal

The Stop hook condition from this checkpoint's session summary is satisfied as of this checkpoint + the changelog/markdown commits alongside it + the two `@oh` review rounds already run. No further action pending against it.

## TaskList

### in_progress
(none)

### pending
- #192 Heartbeat reports "alive" for a fully deadlocked process (unrelated pre-existing task)
- #208 Lower-priority hygiene backlog (8 items now, see task description for full list and suggested priority — top two: untested queued-after-summarize path, and the pre-existing checkpoint_stall_probe_test.go race)

### recently completed
- (docs-only, no task number) toolCleanupGrace doc-honesty correction
- (docs-only, no task number) agentic_fetch_tool.go IsSubAgent fix
- #210, #209, #203, #207, #206, #205, #204 — see `2026-08-02-0955.md` for the full round-2 list
- #195-202 — see `2026-08-02-0326.md` for the full round-1 list

## Decisions

- The agentic_fetch IsSubAgent fix and the doc-honesty correction were both small enough (one struct field; one doc comment + test rename + one new test) that the orchestrator implemented them directly rather than delegating to `@sh` — the sub-agent round-trip overhead wasn't justified for either.
- The "late wedge" limitation in toolCleanupGrace is accepted as-is, not structurally fixed (would need a child→parent progress-signal mechanism that resets the parent's watchdog) — the practical impact is diagnostic quality on an already-rare path, not correctness, and this was made an explicit tracked/tested limitation rather than either silently left wrong or over-engineered away.

## Open questions

None. Task #208's backlog is fully prioritized and not blocking anything.

## Repo state

```
 D web/dist/.gitkeep
```
```
06b008bb docs: changelog entry for the second @oh review's two fixes
ef60b916 docs(agent): correct toolCleanupGrace's overstated invariant
cc53728c fix(agent): classify the agentic_fetch nested agent as a sub-agent
178dfdd2 docs: checkpoints for the review-fix and follow-up-review cycle
f442ddcf docs: changelog entry for #203/#209/#210 (agents-single docs, stdin idle timeout)
```
