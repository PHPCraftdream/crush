# Checkpoint — 2026-08-13 14:12 [second-review-round-closed-except-p0-1]

## Session summary

Continuing from checkpoint `2026-08-13-0641.md`. That prior checkpoint left the
round genuinely blocked on a single item: task #421 (P0-1 regression found by
`@oh`'s independent review — an interrupted busy-owner call could be lost for
non-persistent `crush run`), awaiting a user architectural decision that had
been asked for twice with no answer.

**This session opened with the user invoking `@oh` a second time**, asking it
to review a NEW document: `docs/reviews/2026-08-13-release-readiness-static-audit.md`
(a second, independent static-audit review, distinct from the earlier
`2026-08-13-release-readiness-concurrency-and-code-review.md`). Its verdict was
also **NO-GO**, with four release blockers (P0-2 through P0-4, all new/different
findings than #421) plus four P1s. The orchestrator independently verified
every finding by reading the actual code before acting on it (same zero-trust
discipline as the whole prior session) — all four P0s and both config-related
P1s were confirmed real, including two (P1-1, P1-2) that were **direct gaps in
the orchestrator's own prior fix (#423)**: `workerSubAgentActive()` and
`RemoveProviderAPIKey` were missed when threading config snapshots through
`buildAgentModels`/`SetProviderRuntimeConfig` last round. Filed as tasks
#424–#430 (later renumbered #436–#439 for three of the P1s, see below), in the
review's own recommended fix order.

**The user then re-invoked `/babygoal`** with the same standing instruction
from the prior round (implement via `/crush` + worktrees, commit between
tasks, checkpoint + changelog + commit-md + `@oh` review at the end). Domain
was already understood (just re-derived by reading the new review), so
investigation was skipped. Strategy chosen: **batches with worktree
isolation**, but refined mid-round at the user's explicit request ("если
что-то можно параллельно решать — используй worktrees") from a fully serial
chain into **three parallel tracks grouped by the file each touches**
(`internal/session/run_queue_pump.go`, `internal/agent/coordinator.go`,
`internal/config/store.go`), since the TaskUpdate tool has no way to *remove*
a `blockedBy` relation once set — the fix was to delete and recreate tasks
#427–#430 as #436–#439 with the corrected, narrower dependency graph.

**Work executed, in order, all zero-trust reviewed and personally verified**
(build + tests + at least one personal revert-check per fix, not just trusting
delegate/sub-agent reports):

- **#431 (CI fix, prerequisite)**: `crush run` uncovered the actual CI failure
  from the PRIOR round's already-merged work — `TestP0_3_DBAckSucceedsAfterStop`
  failing on ubuntu-latest with "no such table: session_run_queue". Root cause:
  four `internal/session` test helpers used `sql.Open("sqlite", ":memory:")`,
  and Go's `database/sql` can silently recycle a bad connection into a BRAND
  NEW empty in-memory database after `driver.ErrBadConn` (common on context
  cancellation during pump shutdown). Fixed three of four helpers to use a
  file-based DB in `t.TempDir()`; the fourth (`TestP1_4_BoundedWorkerPoolRespectsLimit`,
  which has a tight 100ms lease TTL) regressed badly under file-based I/O
  (10/15 failures vs. 1/15 baseline) — orchestrator caught this via personal
  A/B testing the delegate's own report didn't surface, re-delegated with the
  precise diagnosis, delegate tried a shared-cache in-memory DSN first (didn't
  work, different failure), then correctly reverted just that one file to
  `:memory:` with a documented rationale. Also found and cleaned up three
  stray scratch `.go`/`.db` files the delegate left at repo root that broke
  `go build ./...` for the whole tree.
- **#424 (P0-2)**: post-compaction continuation of a durable call was silently
  lost (`mb.submit(call, nil)`'s return value discarded; P0-1's own guard
  skips enqueueing `FromDurableQueue` calls into `mb.submitted`), and the
  durable row got Acked as if the whole request succeeded. `/crush` hit a
  **hard weekly/monthly quota limit** mid-fix (reset time given as 17:01);
  user explicitly authorized falling back to a local `@sh` sub-agent for this
  one task while a one-shot cron (`a3173102`, since fired and auto-deleted)
  checked back at 11:11 for the quota. Fixed by returning the continuation
  directly as `(next, hasNext=true)` from `runTurn`, bypassing `mb.submit`
  entirely for this internal handoff — mirrors the existing pattern used a
  few lines below for queued messages.
- **#425/#436/#437 (P0-3, P1-1, P1-2 — first parallel batch, three `@sh`
  agents)**: P0-3 fixed the lease watchdog computing its deadline from
  post-call `time.Now()` instead of the DB-committed `lease_expires_at`
  (found and fixed a SECOND bug along the way: `LeaseRunQueueEntry` was
  double-flooring the expiry to whole seconds, which zeroed out any
  sub-1-second test TTL — fixed with a new `ceilUnixSeconds` helper). P1-1
  threaded one config snapshot through `workerSubAgentActive`/
  `resolveSessionModels`/401-rebuild (the #423 gaps). P1-2 applied the exact
  same independent-snapshot-publish pattern from #423's `SetProviderRuntimeConfig`
  fix to the sibling `RemoveProviderAPIKey` method. All three personally
  revert-checked, all three merged.
- **#426 (P0-4, `/crush`, quota had recovered by then)**: orphan outbox rows
  could get permanently stranded in `processing` after a crash between claim
  and enqueue/delete (no owner/lease/claim-deadline column existed). Fixed by
  replacing the three-step claim/enqueue/delete sequence with ONE atomic SQL
  transaction (`DrainOrphanOutboxEntry`) — the vulnerable intermediate state
  no longer exists at all, rather than being patched. Orchestrator caught
  that this silently turned the old `MaxAttempts`-exhaustion → `failed`
  terminal-state logic into dead code (nothing increments `attempts` in the
  new atomic path anymore) — filed as follow-up **task #440** (not blocking;
  strictly safer failure mode than the bug just fixed, just a smaller
  observability regression).
- **#438 (P1-3, `/crush`)**: bounded each interrupt tick's own operation with
  `context.WithTimeout` (10s) so a tick blocked on a hypothetically
  context-unaware dependency can't hang `Coordinator.Run` forever — explicitly
  does NOT put a timeout on the ticker's own join (`<-tickerDone` stays
  unconditional), since that specific anti-pattern (named directly by the
  review) would trade a hang for a use-after-close. Orchestrator's own
  `-race -count=30` combined run caught a genuine data race the delegate's
  `-count=20` isolated run had missed (a new test polling a mock's plain
  slice field while a background goroutine concurrently wrote to it) — fixed
  directly by adding a mutex + thread-safe snapshot accessor to the shared
  `mockSessionAgent` test double, then reconfirmed clean.
- **#439 (P1-4, done directly, no worktree)**: orphan outbox drain only ran
  on the periodic ticker (15s in production), so a short-lived `crush run`
  process could exit before ever attempting recovery. Fixed with a one-shot
  initial drain on `Start()`, mirroring the existing initial `p.tick()` call.
  This deliberately broke `TestOrphanOutbox_RevertCheck`'s premise (its whole
  point was "drainage can be fully disabled" — no longer true by design) —
  deleted rather than patched into a disguised race, since a race-free
  rewrite would need a synchronization hook that doesn't exist.
- **Two more mock/lint gaps found by the FULL `.githooks/pre-push` run** (not
  caught by any individual task's own scoped verification): `internal/app`'s
  `mockSessionService` was missing the new `DrainOrphanOutboxEntry` method
  (same recurring pattern as last round — P0-4's own test scope never
  touches `./internal/app/...`), and a `noctx` lint finding (bare
  `sqlDB.QueryRow` instead of `QueryRowContext`) in the new P0-3 test. Both
  fixed directly, full pre-push re-run came back clean.

**Final state: full `.githooks/pre-push` (build + golangci-lint v2.10 + the
ENTIRE `go test -race` suite) passed cleanly on the very last commit.** All
worktrees created this session removed, all branches deleted, no stray files.

## Active goal

None (no `/goal` Stop hook armed). A session-scoped Stop hook condition IS
active from the `/babygoal` re-invocation this session (implement via
`/crush`/worktrees, commit between tasks, then checkpoint/changelog/commit-md/
`@oh` review) — this checkpoint task (#432) is the first of that closing
sequence; #433 (changelog), #434 (commit md), #435 (`@oh` review) remain.

## TaskList

### in_progress
- #432 Write session checkpoint after this round closes (this file — will be
  marked completed immediately after this write)

### pending
- #433 Update CHANGELOG for this round's fixes (blocked by #432)
- #434 Commit all markdown docs produced by this round (blocked by #433)
- #435 Run `@oh` review of this round's work (#431, #424-#430) (blocked by #434)
- #440 P0-4 follow-up: orphan outbox atomic drain lost attempts/exhaustion
  tracking (not blocking anything, small, can be done any time)
- **#421** [ВАЖНО, ОТЛОЖЕНО] Fix P0-1 regression — STILL the one standing
  item requiring a user decision, carried over unchanged from the prior
  checkpoint. Independently re-confirmed by the SECOND `@oh` review this
  session too (same root cause, same two remediation options named). Not
  touched this session per the user's own prior instruction not to guess at
  this architectural call.

### recently completed (this session)
- #431 CI fix (SQLite `:memory:` DSN flake)
- #424 P0-2 (post-compaction continuation loss)
- #425 P0-3 (lease watchdog absolute expiry)
- #436 P1-1 (config snapshot threading, was #427 before task-recreation)
- #437 P1-2 (RemoveProviderAPIKey snapshot mutation, was #428)
- #426 P0-4 (orphan outbox atomic drain)
- #438 P1-3 (interrupt ticker operation timeout, was #429)
- #439 P1-4 (orphan outbox initial drain on Start, was #430)
- Two unplanned follow-up commits: `internal/app` mock gap, `noctx` lint gap

## Decisions

- Restructured the P1 tail of the task chain from fully-serial into three
  file-grouped parallel tracks mid-round, at the user's explicit request to
  parallelize wherever genuinely independent — required deleting and
  recreating tasks #427–#430 as #436–#439 since TaskUpdate has no
  blockedBy-removal primitive.
- When `/crush` hit a hard quota limit (not a transient/retryable failure),
  stopped and explicitly asked the user rather than auto-falling-back per the
  `/crush` skill's own documented default — the user's global CLAUDE.md
  ("never invoke sub-agents without explicit request") is a stronger,
  standing override than a skill's built-in fallback behavior. User answered
  with a specific instruction (armed an 11:11 wake-up, authorized `@sh` for
  the one blocked task in the meantime) rather than a blanket approval.
- Ran the FULL `.githooks/pre-push` (not just each task's own scoped tests)
  as the final gate before checkpointing — this is what caught both of the
  cross-package mock/lint gaps that no individual task's own verification
  scope would have found. Treating "each task's tests pass" as sufficient
  proof of a clean tree has now caused this exact class of gap in two
  consecutive rounds; running the full gate at the end is the actual fix,
  not a per-task discipline improvement.
- Deleted (not patched) two tests whose entire premise was invalidated by a
  fix's own correct new behavior (`TestOrphanOutbox_ClaimProtection` →
  rewritten as `TestOrphanOutbox_ConcurrentDrainProtection` in #426;
  `TestOrphanOutbox_RevertCheck` deleted outright in #439) rather than
  forcing them to keep passing via increasingly contorted assertions.
- Filed #440 as a non-blocking follow-up instead of either silently accepting
  the attempts-tracking regression or blocking #426's merge on fixing it —
  the atomic-transaction fix is strictly safer than the bug it replaces, so
  blocking would have been the wrong tradeoff, but the gap still needed to be
  on record rather than silently dropped.

## Open questions

- **#421's remediation approach — still open, now confirmed by a SECOND
  independent review this session.** Two named options, unchanged since the
  prior checkpoint: (1) keep `mb.replacement` for durable-queue-originated
  interrupts too, with the replacement's own execution acking/deleting the
  durable row via `LogicalCallID`/`InjectID`; or (2) have
  `RunNonInteractive`/`Shutdown` drain pending run-queue entries for the
  active session before the process exits. Not touched this session — the
  user's own `/babygoal` instruction this round was scoped to the SECOND
  review's findings (#424–#430/#436–#439), not #421, and #421 continues to
  require a human architectural call the orchestrator has consistently
  declined to make unilaterally.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
?? docs/reviews/2026-08-13-release-readiness-static-audit.md
```

```
b10f3377 fix(session): use QueryRowContext in P0-3 watchdog test (task #425 follow-up)
d4b9bcb5 fix(app): add DrainOrphanOutboxEntry to mockSessionService (task #426 follow-up)
d8a7a768 merge: P1-3 interrupt ticker operation timeout fix (task #438)
fdaa0ed3 fix(agent): bound each interrupt tick's own operation deadline (task #438)
65affa8c fix(session): run orphan outbox drain immediately on pump Start() (task #439)
```

No worktrees remain outstanding — every one created this session
(`ci-sqlite-fix`, `p0-2-compaction`, `p0-3-lease-watchdog`,
`p1-1-config-threading`, `p1-2-remove-provider-key`, `p0-4-orphan-outbox`,
`p1-3-ticker-deadline`) has been merged and removed (`git worktree remove` +
`git branch -d`), including one that needed `crush sessions kill`/manual
`taskkill` to release a Windows file-handle lock on the lock file before the
directory could be deleted.

The untracked `docs/reviews/2026-08-13-release-readiness-static-audit.md`
(the second `@oh` review that drove this whole round) is NOT yet committed —
that is exactly what task #434 ("commit all markdown docs produced by this
round") is for, still pending.
