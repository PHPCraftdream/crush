# Checkpoint — 2026-07-06 08:39

## Session summary

Session resumed from `2026-07-06-0817.md` (npm 0.1.3 live, PRs #2/#3
merged — see that file and `2026-07-05-2039.md` for the release arc),
then pivoted to a NEW feature: **per-provider peak hours** during which
crush refuses to do LLM work. This session was research + task
planning only — ZERO code written, working tree untouched.

Research findings (all verified by reading code):
- Closest existing mechanism: `ProviderConfig.Disable`
  (`internal/config/config.go:129`), static on/off, filtered in
  `internal/app/provider.go:56`, `internal/cmd/models.go:52`,
  `internal/config/load.go`. No scheduling concept exists anywhere.
- Enforcement point chosen: `internal/agent/coordinator.go` — three
  spots right after `Providers.Get(...)` that already return
  `errModelProviderNotConfigured`: line ~505 (Run entry), ~1906, ~2041
  (sub-agent/aux paths). Check must be per-turn, not config-load,
  because sessions are long-lived. Peak-hours error must be
  operator-actionable (non-transient) so the auto-retry loop at
  coordinator.go:~596 does not chew it.
- CLI provider management ALREADY EXISTS: `crush providers`
  list/show/set/add/enable/disable/unset/fetch-models/grep in
  `internal/cmd/providers.go` — extend, don't build.
- API: no REST; everything is WebSocket (`internal/server/handlers.go`).
  Already present: handleAddCustomProvider/handleUpdateCustomProvider/
  handleRemoveCustomProvider (lines ~145-149), handleSetProviderKey,
  handleGetConfig.
- Web UI ALREADY HAS `web/src/components/ProvidersModal.tsx` (471
  lines): add/edit provider (id, name, type, baseUrl, apiKey, models).
  Task #4 is an extension of this modal, not a new page. Unverified:
  whether it has a delete button (remove_custom_provider WS command
  exists server-side).

Operator decisions during planning (came as interrupting messages):
1. web UI needs a provider-settings editor → task #4;
2. peak hours model SIMPLIFIED: either off, or a single start+end
   window by the computer's LOCAL time — no weekday masks, no timezone
   field. Overnight wrap (start > end) still supported. All task
   descriptions updated to this model.

5 tasks created in TaskList (see below) with dependencies:
#1 → (#2, #3 parallel) → #4; #5 last. Implementation is to be
delegated to /crush agents per the operator's standing methodology —
operator has NOT yet said "go"; last words were effectively "start #1
on my command". Model was switched to Fable via /model.

## Active goal
none — no /goal or /babysit active.

## TaskList
### pending
- #1 Ядро peak-hours: конфиг, парсер, enforcement в coordinator
- #2 CLI: peak_hours в командах crush providers + просмотр статуса  (blockedBy: #1)
- #3 WS API: peak_hours в provider-командах + полнота provider-менеджмента  (blockedBy: #1)
- #4 Web UI: редактор настроек провайдеров (список, изменение, удаление, peak_hours)  (blockedBy: #3)
- #5 Хелпы: cobra-тексты, схема, slash-команда /crush, README  (blockedBy: #1, #2, #3)

Task descriptions are detailed (file paths, line numbers, config shape
`"peak_hours": {"start": "09:00", "end": "18:00"}`, new file
`internal/config/peak_hours.go`, schema comma-escaping reminder per
precedent 4bd66ebb) — read them via TaskGet before implementing.

## Decisions
- Peak hours = off OR one {start, end} window in computer-local time;
  no weekday masks, no peak_tz. Operator explicitly simplified.
- Enforce per-turn in coordinator (3 points), NOT by filtering
  provider/model lists — provider stays visible, just refuses during
  peak. Refusal only, no fallback-provider in v1 (recommended, operator
  did not object).
- Extend existing surfaces (crush providers CLI, WS custom-provider
  handlers, ProvidersModal.tsx) rather than building new ones.
- Implementation via /crush agents on operator's explicit go-ahead
  (per operator's global methodology; not started yet).

## Open questions
- Operator has not yet given the "go" to start task #1.
- Does `providers set` parse composite values cleanly for
  peak_hours=09:00-18:00? (to be resolved in #2)
- Does ProvidersModal have a delete-provider button? Does WS
  remove_custom_provider handle non-custom providers? (to be resolved
  in #3/#4)
- Carried from prior checkpoint: npm deprecate on 0.1.0–0.1.2
  unconfirmed; auto-init-into-~/.claude feature still parked; two
  cosmetic nits in PR #3 un-fixed.

## Repo state
```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-06-28-2158.md
?? docs/checkpoints/2026-07-03-1513.md
?? docs/checkpoints/2026-07-04-2234.md
?? docs/checkpoints/2026-07-05-2039.md
?? docs/checkpoints/2026-07-06-0817.md
```
```
0328e33a chore(npm): bump to 0.1.3, changelog for restricted-run permissions
753668b5 Merge pull request #3 from ayleen/feat/run-bash-allowlist
0e67b16c refactor(shell): extract shared IsCompoundCommand; close bash safe-path chaining bypass
4bd66ebb fix(schema): escape commas in run-permission descriptions; refresh allow_bash text
aa44b63a fix(review): address run-allowlist review — interactive leak, chaining bypass, cross-platform glob
```
