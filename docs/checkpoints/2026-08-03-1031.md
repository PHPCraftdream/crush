# Checkpoint — 2026-08-03 10:31

## Session summary

Closes out the `/babygoal` run that started at checkpoint `2026-08-03-0915.md` (tasks #228-231, the four MEDIUM/LOW findings deferred from the second `@oh` review). All four were implemented via `@sh` (after `/crush`'s first attempt hit a peak-hours provider refusal, correctly surfaced to the user per the `/crush` skill's standing rule rather than bypassed unilaterally — user chose `@sh` for the rest of this run), each independently verified before committing:

- `49717b61` (#228) — `internal/session.InspectSessionLock` PID-liveness fallback, closing the web UI's `OwnedExternal` flicker gap.
- `91f094dd` (#229) — `sessions_inject.go`'s `isSessionLockAlive` now delegates to the just-fixed `InspectSessionLock` instead of a third independent mtime-only implementation.
- `6aac8415` (#230) — documented (not structurally fixed, after working through a real tradeoff) `lockHolderProvablyDead`'s residual TOCTOU window; explicitly declined to gate `sessions locks`' auto-delete behind a flag.
- `5aaba24f` (#231) — `sessionsLocksCmdRun`'s cwd-hardcoded data directory and raw PID read (missing the Windows sidecar fallback), both fixed.

`CHANGELOG.md` and a checkpoint (`2026-08-03-0915.md`) were committed (`e0546fd4`), then a third `@oh` review was launched, scoped to the unreviewed delta (`63a7269a..HEAD`, 8 commits) plus a whole-range build/vet/test sanity pass. Verdict: all 4 functional fixes from this round (plus the 2 carried over from round 2's own findings) confirmed correct by independent re-derivation; full `-race` suite clean across `internal/agent`, `internal/session`, `internal/cmd`, `internal/config`, `internal/server` (one pre-existing, unrelated timing flake in `internal/config` noted and confirmed non-reproducing in isolation); no layering violations, no removed-upstream-pattern reintroduction. The review found 7 more findings (NEW-A through NEW-G), all explicitly characterized by the reviewer as LOW severity with "none blocks the round."

Two were trivial enough to fix immediately without a sub-agent round-trip (both independently re-verified by direct reading before fixing): NEW-B, a self-contradicting sentence in `sessions locks`' own `--help` text (claimed "does NOT delete locks automatically" three lines above a paragraph saying the opposite); NEW-G, a `CHANGELOG.md` line overclaiming that a data-dir/PID-read fix was a "liveness fallback" change when it wasn't. Fixed in `0345614c`.

The remaining 5 (NEW-A: a blocking `onFire`/`DumpGoroutines` disk write could theoretically prevent the stream watchdog from ever cancelling; NEW-C: the same cwd-hardcoding bug just fixed for `sessions locks` survives in 5 sibling commands — `list`/`reap`/`watch`/`why`/`queue`; NEW-D: a silent `os.Remove` failure after a successful death-probe leaves a fresh-looking-but-actually-dead lock file with no error surfaced; NEW-E: the PID-liveness fallback from #228 has no time bound, so a reused PID could in principle pin a session read-only in the web UI forever; NEW-F: a duplicated `"Stream stalled"` string literal between the watchdog's finish-message logic and the coordinator's retry-matching logic could silently break transparent stall-retry on an innocent future rewording) were presented to the user, who — given this was now a fourth review pass and all remaining items were confirmed low-severity — chose to file them as tasks and stop, rather than continue a fourth autonomous fix round. Filed as tasks #232-236 with full architectural context (exact file:line references, concrete failure scenarios, and — for the two that are genuine design tradeoffs, #235 and #232's second half — explicit instructions to think through the tradeoff rather than mechanically implement, matching the precedent task #230 set).

## Active goal

None. This `/babygoal` invocation's contract (implement via `/crush`→`@sh` with commits between tasks, then `/checkpoint` + changelog + commit all `.md` + `@oh` review) is satisfied: checkpoint, changelog, markdown commits, and a genuinely adversarial `@oh` review all happened; the review's findings were triaged (2 fixed immediately, 5 filed as tasks per explicit user choice) rather than either ignored or chased indefinitely.

## TaskList

### in_progress
(none)

### pending
- #232 Store watchdogCause before DumpGoroutines; document onFire must not block (LOW)
- #233 Fix cwd-hardcoded data dir in 5 more sessions commands (LOW)
- #234 Fix silent os.Remove failure after lockHolderProvablyDead (LOW)
- #235 Bound the PID-liveness fallback against unbounded PID-reuse false positives (LOW, design judgment call)
- #236 De-duplicate the "Stream stalled" finish title (LOW)

### recently completed
- #228-231 — this run, see commits `49717b61`/`91f094dd`/`6aac8415`/`5aaba24f`
- #213-227 — see `2026-08-02-{1406,1630,2059}.md` and `2026-08-03-0915.md` for the full prior history

## Decisions

- When `crush run` (the `/crush` delegation path) hit a peak-hours provider refusal, did not retry, bypass, or silently switch provider — surfaced the refusal and three concrete options to the user via `AskUserQuestion`, per the `/crush` skill's explicit, non-negotiable rule about `--allow-peak-hours`.
- After the third `@oh` review, fixed the two most trivial findings (NEW-B, NEW-G — both simple, low-risk, independently re-verified doc/wording corrections) directly rather than via another sub-agent round-trip, consistent with this session's established proportionality judgment for single-file, low-complexity fixes.
- For the remaining 5 findings, explicitly asked the user whether to continue into a fourth autonomous review-fix round rather than assuming "more fixing is always better" — this is now the SECOND time in this run a natural pause point was surfaced rather than mechanically continued, and the user's consistent choice both times (file tasks, stop) was honored directly.
- Filed task #232 and #235 with explicit "think through the tradeoff, don't mechanically implement" framing, since both are genuine design decisions (an unbounded-blocking-write mitigation strategy, and a PID-reuse-false-positive bound) rather than mechanical bug fixes — matching the precedent set by task #230's own worked-through tradeoff.

## Open questions

None blocking. Tasks #232-236 are fully scoped and independently actionable. Two (#232, #235) explicitly call out that "document as an accepted risk" (matching task #230's precedent) is an acceptable outcome if no clean structural fix is available — not every finding needs to end in a code change.

## Repo state

```
 D web/dist/.gitkeep
```
```
0345614c docs: fix self-contradicting sessions-locks help text + changelog overclaim
e0546fd4 docs: changelog + checkpoint for the third heartbeat-consumer hardening round
5aaba24f fix(cmd): fix sessions locks' cwd-hardcoded data dir and raw PID read
6aac8415 docs(cmd): document lockHolderProvablyDead's residual TOCTOU and contention
91f094dd fix(cmd): make sessions inject's liveness check honor the PID fallback
```
