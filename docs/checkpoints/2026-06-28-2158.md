# Checkpoint — 2026-06-28 21:58

## Session summary

Resume-after-compact continuation of the long 2026-06-28 session on the
`PHPCraftdream/crush` fork. The earlier portion shipped, committed and
pushed: atomic `rerun_message`, intermediate-text-as-full-message UI,
`/compact` deletes old non-pinned messages, todos tombstone, GLM effort
clamp + backend preserve-unset, single-action accordion unwrap, audio
keep-alive RMS recovery, pnpm migration with 30-day `minimumReleaseAge`
and a matching `renovate.json`, `build.go` switched to pnpm with
`CI=true`. Latest pushed commit on `main` and `fork/main`: `6ad27a35
docs: session checkpoint 2026-06-28-1758`. Local tree is clean except
for the intentional `D web/dist/.gitkeep` — fork CLAUDE.md explicitly
forbids committing that deletion, so it stays in the working tree
indefinitely.

After resume the only substantive work this turn was an upstream-merge
audit. Operator's intent was reconnaissance, not porting: "просто
посмотрим их коммиты и составим список того, что они там сделали".
Fetched `origin/main` (charmbracelet/crush) and bucketed all 187
commits ahead of fork main into SKIP / EVAL / PORT per the fork CLAUDE
golden rule. Headline finding: ~125 of 187 are pure SKIP — the bulk is
upstream's new **multi-client shared-workspace server model** (which
fork CLAUDE.md explicitly forbids re-importing), an entire **bangmode
TUI feature** (19 commits — fork has no TUI), TUI cosmetics, CLA bot
spam, version tags, auto-update chores and dep bumps. ~50 are EVAL
candidates (provider auto-discovery for openai-compat: litellm /
ollama / lmstudio / omlx + a fireworks provider, plus
hooks/oauth/config defensive fixes). ~10 are crisp PORT candidates,
the strongest being `21a457d5` (validate tool-call JSON before storing
— defends against stuck conversations), `67f50014` (bounded glob on
large directories), `d87a632a`/`c83418d1` (oauth token-lifetime
hardening), `ed870ecb` (event subscription panic on cancel),
`a1e4a7c9` (test data race), `ffaeec19` (avoid startup crash),
`c2be8cbf` (non-interactive env vars to prevent editor hangs in our
cliprovider). Several upstream commits are already in the fork as
equivalents (six 401-retry / model-listing / hooks-name-field / etc.
commits identified by side-by-side log read). The audit ended with an
offer to surface 5–10 PORT candidates with file-level conflict
analysis — operator has not yet accepted that offer.

The 30-day pnpm/Renovate cooldown is now the supply-chain default
across fork web work. No `/babysit` or `/goal` is active. The earlier
`/checkpoint` from this session is `2026-06-28-1758.md`, committed as
`6ad27a35`. This checkpoint captures the state after the upstream
audit but BEFORE any cherry-picks — those are still pending operator
go-ahead.

## Active goal
none

## TaskList
empty — TaskList returns "No tasks found"; this resumed half of the
session has been audit/discussion only, no task creation needed.

## Decisions
- **Upstream merge strategy: cherry-pick only, no `git merge`.** Per
  fork CLAUDE.md golden rule. The 187-commit delta is dominated by
  multi-client server work and bangmode TUI — bulk-merging would
  drag those in and trigger massive conflicts in subsystems the fork
  rewrote.
- **Defer porting until operator selects from the EVAL/PORT list.**
  Offered to do file-level conflict analysis on 5–10 strongest PORT
  candidates; awaiting confirmation rather than picking unilaterally.
- **Multi-client server model stays out.** ~30 upstream commits build
  toward upstream's new "share one workspace per directory across
  clients" architecture; the fork's lock-file + heartbeat model is
  battle-tested and stays. Every server/db/lock/permissions-idempotent
  commit on the upstream side is SKIP-by-default.
- **`web/dist/.gitkeep` deletion stays uncommitted indefinitely.**
  Operator confirmed this is intentional; fork CLAUDE.md echoes it.
  When the user says "commit all" we explicitly carve this out.

## Open questions
- Same two un-OBSERVED-at-wire questions inherited from the
  2026-06-28-1758 checkpoint: (1) whether GLM-5.x `["high","max"]` is
  a real z.ai API limit or fork-side conservatism; (2) why
  post-compact `prompt_tokens` ran to 550 k vs the expected ≈235 k.
  DB-side compact fix removed the symptom, root cause still unproved.
- Should the audit's PORT candidates land as **one big cherry-pick
  batch** (faster, more risk of cross-coupling) or **one commit at a
  time with rebuild + test between** (slower, exactly what fork
  CLAUDE.md prescribes)? Default per CLAUDE.md is one-at-a-time.

## Repo state
```
 D web/dist/.gitkeep
```
```
6ad27a35 docs: session checkpoint 2026-06-28-1758
e47711a0 fix(build): switch build.go to pnpm after the web/ migration
ceb4368f chore: Renovate config with 30-day minimumReleaseAge
bccb844b chore(web): migrate from npm to pnpm with 30-day minimumReleaseAge
18c5ae70 fix(web): restore keep-alive RMS — narrowband lowpass needed gain back up
```
