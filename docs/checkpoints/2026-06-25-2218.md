# Checkpoint — 2026-06-25 22:18

## Session summary

Long session on the `PHPCraftdream/crush` fork (Go backend + React web UI,
`D:\dev\go\crush`). The thread: a series of "why did this session die/freeze"
investigations on OTHER workspaces (shamir-db, sefer-alloc) that each surfaced a
real fork bug, which we then fixed, verified, committed and pushed.

Landed and pushed to `fork/main` (CI green) this session:
- **490a6b12** transient-provider auto-retry (generalised the turn-retry beyond
  watchdog stalls).
- **2ee953dd** web: stream sub-agent work live into its block (composite
  `messageID$$toolCallID` session key).
- **6390224a** pause stall-watchdog during tool exec + non-zero `crush run` exit
  on incomplete runs.
- **adecefe6** reliable session titles (use truncated title on
  FinishReasonLength, budget 40→96, self-heal) + `crush run` default removes
  sub-agent tools.
- **c74b60d3** show originating command in `sessions last/watch` tool-result rows.
- **bb9a3f8e** NEVER-FREEZE (Phase 1a+1b+2): watchdog tool-cap
  (`Options.StreamToolTimeoutSeconds`, 15m default), bounded `job_output --wait`
  (`jobOutputMaxWait` 90s), `WaitForChange` early-return + elapsed/exit in
  job_output. Plus `docs/design/event-loop-turn.md`.
- **28447843** `BackgroundShell.OnDone` (Phase 3a).
- **0d4da8c0** push background-job completion into the owning session via
  InjectMessage (Phase 3b) + `Options.NotifyOnBackgroundJobDone` kill-switch.
- **d2ef73c5** test fix: stop asserting the racy PTY stderr-tail in cliprovider
  `TestStreamExitError` — it went flaky/red on Linux CI under `-race` (extra
  concurrency from the new tests exposed a pre-existing PTY-drain race;
  cliprovider code itself untouched/independent). CI now green (build/lint/security).

Phases 1–3 were delivered via `/crush` sub-agents (one per stage, commit between,
zero-trust verify each). Phase 4 was just PLANNED this turn (via /oxx): tasks
#81–#85 created, NOT started.

Two open threads remain (see Open questions). No babysit cron is active (the
Phase-3 one self-deleted / was cancelled; /oxx did not arm one).

## Active goal
none (no /goal Stop hook in force this session).

## TaskList
### in_progress
(none)
### pending
- #81 Phase 4 design: event-loop autonomous idle-resume (doc only)  — ACTIONABLE NOW (only ungated one)
- #82 Phase 4 impl 1: config + autonomy guardrail primitives  (blockedBy: #81)
- #83 Phase 4 impl 2: wake-supervisor (idle auto-resume)  (blockedBy: #82)
- #84 Phase 4 impl 3: web UX marker for auto-resumed turns  (blockedBy: #83)
- #85 Phase 4 impl 4: tests + full pre-push verify  (blockedBy: #83, #84)
### recently completed
- #78 Phase 3a: BackgroundShell.OnDone (committed 28447843)
- #79 Phase 3b: inject job completion into session (committed 0d4da8c0)
- #80 Phase 3 final verify (full pre-push green)
- #72 watchdog tool-cap, #73 job_output bounded, #74 responsive polling (→ bb9a3f8e)
- #76 design push-completion + event-loop doc, #77 final verify (Phases 1-2)

## Decisions
- **Phase 4 shape: "wake-on-event supervisor" (Shape A) over "true suspend/
  resume" (Shape B).** A reuses messageQueue+Run+the Phase 3 hook to resume an
  IDLE session; B (freeze/restore a mid-flight turn) is far riskier and not
  needed for the goal — deferred.
- **Autonomy is opt-in: `AutoResumeOnJobDone` default OFF**, with a
  max-consecutive-auto-resume bound (reset by any human msg) to prevent runaway,
  and NEVER for `crush run`. (To be confirmed by the user at the design gate.)
- **Phase 3 boundary**: busy session → completion merges into the running turn;
  idle session → message persisted + web-visible but NO auto-turn (auto-resume
  is exactly Phase 4). Kept Phase 3 additive, no turn-loop surgery.
- **CI red was NOT our regression**: pre-existing flaky cliprovider PTY-drain
  race, exposed by added `-race` concurrency. Fixed the over-strict test
  (assert the guaranteed non-zero-exit error always; assert stderr TEXT only in
  the deterministic pipe path) rather than blind-patching Linux PTY internals
  from a Windows box. The "real" PTY-drain hardening is an optional follow-up.
- **Phase 4 design doc written by orchestrator (me), not /crush** — context-heavy.

## Open questions
- **Deploy gap (important):** installed `crush` is `c74b60d31122` (= c74b60d3),
  3 commits behind HEAD `d2ef73c5`. None of the never-freeze / Phase-3 / CI fixes
  are ACTIVE in the running binary until rebuilt. `deploy.go` exists (prod-build +
  kill running + replace npm binary). User must say go (it kills live crush
  sessions). NOT done.
- **Phase 4 next step:** user to choose — "пиши дизайн" (execute #81, the
  detailed implementable design doc) → review → approve gate → implement #82–85
  via /crush; OR deploy first. Awaiting the user's pick.
- Optional: real cliprovider PTY tail-drain fix (read-until-EIO before Close on
  Unix) — Linux-specific, can't verify from Windows; only the test was relaxed.

## Repo state
```
 D web/dist/.gitkeep          (intentional — do NOT commit, per fork CLAUDE.md)
?? docs/checkpoints/2026-06-24-0100.md   (prior checkpoint, untracked)
```
(this checkpoint adds docs/checkpoints/2026-06-25-2218.md, also untracked)
```
d2ef73c5 test(cliprovider): don't assert the racy PTY stderr-tail in TestStreamExitError
0d4da8c0 feat(agent,config): notify the session when a background job finishes
28447843 feat(shell): BackgroundShell.OnDone completion callback
bb9a3f8e fix(agent,shell): never let a blocking tool freeze the turn
c74b60d3 fix(cmd): show originating command in sessions tool-result rows
```
```
