# Checkpoint — 2026-08-11 15:30 [release-readiness-review-babygoal-round]

## Session summary

Continuing directly from the prior two checkpoints in this same session
(`2026-08-11-0805.md`, `2026-08-11-1005.md`), the user asked me to read a
new, much deeper review report — `docs/reviews/2026-08-11-release-readiness-concurrency-and-code-review.md`
(NO-GO verdict: 4 release blockers P0-1..P0-4, 5 high-priority P1-1..P1-5, 6
medium/code-smell P2-1..P2-6, evaluated at commit 78f2090c) — and create
tasks from it. I created 15 tasks (#367-381) with full per-finding detail
(file:line refs, exact failure chains, proposed fixes, from the report).

The user then ran `/babygoal` again with the same instruction pattern as the
prior round: implement via `/crush`, commit between tasks, then at the end
checkpoint + changelog + docs commit + `@oh` review. Domain was already
understood (I'd just read the report). Strategy: sequential sub-agents via
`/crush`, same reasoning as before (shared files, explicit "commit between
tasks" requirement). Armed babysit (cron `eab417a2`, every 15 min, still
running). Built a strict serial `blockedBy` chain reflecting the report's own
recommended fix order: #367→#383→#384→#368→#376→#369→#370→#371→#372→#374→#373→#375→#377→#378→#379→#380→#381→#382.

**Mid-session, the user separately reported a concrete UI bug** (switching
model in web UI changed it globally instead of per-session) via `/oh`. I
investigated live (not from the review report) and found the exact root
cause: `coordinator.applyModelOverrides` correctly resolved a per-session
model snapshot for pinning onto the call (correct, from tasks #265/#341) but
ALSO wrote that resolution into the single shared `coordinator.currentAgent`
— last-writer-wins across the whole process. The code itself documented this
as a deliberately deferred decision. I created two new tasks (#383 core
fix, #384 follow-up API split + server call-site fixes) and spliced them
into the chain right after P0-1 (before P0-2), since P0-1 was already
in-flight and I didn't want the user's live bug to wait behind 13 unrelated
tasks.

**Completed and committed so far, each with independent zero-trust
verification (not just trusting the delegated /crush diff or its summary)
and revert-checks on every fix:**
- **#367 (P0-1)** — commit `6bd8c927`. Mailbox no longer double-queues a
  durable-originated call when the mailbox is already owned (new
  `FromDurableQueue` flag on `SessionAgentCall`, set only by
  `RebuildSessionAgentCall`, the durable pump's sole entry point).
  **Caught the delegated sub-agent's own regression test as vacuous** — it
  used a mocked Coordinator that never touched the real `mailbox.submit`,
  so it passed even with the guard reverted. Wrote the real test myself
  directly against `mailbox.submit` (`TestMailbox_Submit_DurableCallSkipsQueueWhenAlreadyOwned`),
  revert-checked it for real.
- **#383 (UI-BUG-1, core)** — commit `20da5fe9`. New pure
  `resolveSessionModels(ctx, sessionID)` resolver, `applyModelOverrides` no
  longer writes to shared state, all execution paths (Run/InjectMessage/
  buildSummarizeSnapshot/resolveSessionSystemPrompt) route through it.
  **Found and fixed THREE bugs in the delegated diff before committing**:
  (1) the model cache swapped large/small config args to "pick" a slot,
  silently mixing up which model was large vs small on every cache miss —
  invisible to the sub-agent's own tests because its fixtures used
  identical large/small configs; (2) `c.modelCache` nil-panics for any
  `*coordinator` built as a struct literal (several existing test fixtures
  do exactly that) since `resolveSessionModels` is now on the hot path for
  every `Run()`, not just DB-overridden sessions as before; (3) the
  delegated session's own final report text ended up entirely inside a
  `[thinking]` block — empty `final_text` — had to recover the actual diff
  via `git diff` + `crush sessions show --with-messages`.
- **#384 (UI-BUG-2)** — commit `3782c8fe`. New
  `BuildSystemPromptForSession(ctx, sessionID)` alongside the existing
  global-default `BuildSystemPrompt(ctx)`; migrated `handleCreateSession`/
  `handleInitializeProject` to it. Found a SEPARATE bug while auditing:
  `handleInitializeProject` only ever wrote the session's LARGE model from
  config, silently leaving small empty — fixed alongside. **Caught the
  delegated diff's new regression test claiming to test
  `BuildSystemPromptForSession` while never actually calling it** (second
  occurrence of this exact pattern this round) — rewrote it honestly, and
  discovered along the way that the method's own nil-prompt guard runs
  BEFORE model resolution, meaning the original test's intended assertion
  technique (checking model-cache side effects) was structurally
  impossible with the lightweight test coordinator's nil `prompt.Prompt`.
- **#368 (P0-2)** — commit `56238306`. `startDetachedRun` and
  `restartOrphanedWithRetry` now return errors instead of silently
  swallowing enqueue failures; the latter is now genuinely synchronous
  (`sync.WaitGroup`) instead of returning before its spawned goroutines even
  start their DB writes. The delegated sub-agent's own report honestly
  admitted it attempted but failed to add regression tests ("hit complexity
  in test infrastructure") — I wrote them myself
  (`internal/agent/p0_2_orphaned_retry_test.go`, two tests, both
  revert-checked for real this time).

**Currently in flight (NOT yet verified or committed):** `/crush` session
`release-review-p2-1` (background bash task id changes each babysit tick
check — last confirmed alive via `crush sessions why`, heartbeat within
normal range) is mid-way through task #376 (P2-1: idempotency key is
regenerated fresh every call instead of once-per-logical-call and reused
across retries). Working tree currently shows `internal/agent/agent.go`,
`internal/agent/coordinator.go` modified and a new
`internal/agent/p2_1_idempotency_key_test.go` — **this is the delegated
sub-agent's in-progress, unread, unverified work**, not yet checked by me.
Given this round's pattern (2 of 4 completed sub-agent diffs so far
contained real bugs the delegated session's own testing missed, and 2 of 4
had misleading/vacuous self-written tests), the next step when this
session's `<task-notification>` arrives is the same rigorous zero-trust
read-the-diff-myself process, not a quick skim.

## Active goal

A session-scoped Stop hook is armed with condition: "реализуй задачи с
помощью /crush, между тасками делай коммиты. После завершения всей работы
сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента
@oh - пусть проверит всю работу" — auto-clears once satisfied.

## TaskList

### in_progress
- #376 P2-1: durable-queue "idempotency key" is actually a fresh unique ID every call, not a stable retry key

### pending (in blockedBy chain order)
- #369 P0-3: RunQueuePump.Stop() doesn't wait for executeEntry (blockedBy: #376)
- #370 P0-4: shutdown join doesn't cover manual summary / other detached DB writers (blockedBy: #369)
- #371 P1-1: runWg.Add can race CancelAll.Wait (blockedBy: #370)
- #372 P1-2: lease loss doesn't stop non-idempotent execution (blockedBy: #371)
- #374 P1-4: pump fan-out after backlog/restart is unbounded (blockedBy: #372)
- #373 P1-3: FIFO durable queue ordering undefined within the same second (blockedBy: #374)
- #375 P1-5: async lock cleanup can clobber a new live owner's metadata (blockedBy: #373)
- #377 P2-2: agentguard `env -S` bypass + stale CheckWindowSafety wrapper list (blockedBy: #375)
- #378 P2-3: Codex MCP token exposed via argv/query string (blockedBy: #377)
- #379 P2-4: SSRF guard denylist misses shared/reserved address space (blockedBy: #378)
- #380 P2-5: mailbox finalizer allows reordering across ownership eras (blockedBy: #379)
- #381 P2-6: recovery subsystem architecture cleanup, deliberately scheduled LAST (blockedBy: #380)
- #382 Wrap-up: checkpoint, changelog, commit docs, @oh review of whole round (blockedBy: #381)

### recently completed
- #367 P0-1, #368 P0-2, #383 UI-BUG-1, #384 UI-BUG-2 (all committed, see commits above)

## Decisions

- Spliced the user's live-reported UI bug (#383/#384) into the P0-2/P0-3/...
  chain immediately after P0-1 rather than appending it at the end, since
  P0-1 was already running and the user's own concrete bug report deserved
  priority over the remaining review-report findings.
- For every delegated `/crush` diff this round, performed full independent
  zero-trust verification before committing — this discipline caught real
  bugs in 2 of 4 completed diffs (UI-BUG-1's large/small model swap + nil
  modelCache; nothing missed in P0-1/P0-2 beyond test vacuousness) and
  vacuous/misleading self-written tests in 2 of 4 (P0-1's mailbox test,
  UI-BUG-2's BuildSystemPromptForSession test) — never assumed a "tests
  pass" claim or a clean-looking summary was sufficient.
- P0-2: consciously left the in-memory mailbox fallback's runnerless-ness
  unfixed (documented as a known remaining gap in the commit message)
  rather than attempting a broader redesign in one delegated session — the
  fix now at least surfaces the failure as a returned/logged error instead
  of pretending success.
- Twice now, a delegated `/crush` session's final assistant message ended
  up entirely inside a `[thinking]` block with empty `final_text` — both
  times recovered via `git diff`/`git status` + `crush sessions show
  --with-messages` rather than treating the empty envelope as "nothing
  happened."

## Open questions

- None new. Same three from the prior checkpoint remain implicitly
  answered by continuing: yes to implementing further findings, no
  decision yet needed on push+CI monitoring for this round (not requested).

## Repo state

```
 M internal/agent/agent.go
 M internal/agent/coordinator.go
?? dev/
?? docs/reviews/2026-08-11-release-readiness-concurrency-and-code-review.md
?? internal/agent/p2_1_idempotency_key_test.go
```

Note: the two modified files + new test file above are the **currently
in-flight, unverified** output of the still-running `release-review-p2-1`
`/crush` session — not yet read, checked, or committed by me. Everything
before that (through commit `56238306`) is committed and verified.

```
56238306 fix(agent): surface enqueue failures instead of silently losing accepted calls (P0-2)
3782c8fe fix(agent,server): make system-prompt building session-scoped (UI-BUG-2)
20da5fe9 fix(agent): make model resolution a pure function of (session, config) (UI-BUG-1)
6bd8c927 fix(agent): stop mailbox from double-queuing durable-originated calls (P0-1)
0835ae5a docs: changelog entry for the closing @oh review's 5 fixes
```
