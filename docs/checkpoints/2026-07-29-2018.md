# Checkpoint — 2026-07-29 20:18

## Session summary

Эта сессия продолжила от чекпоинта `2026-07-29-1342.md` (планирование + завершение горячей замены бинарника, реальный деплой+пуш+CI, факт-чек внешнего ревью → задачи #127-137). Основное содержимое этой сессии — **реализация всех 11 задач по ревью (#127-137)** через `/babygoal` + `/babysit` (крон `d700ee11`, каждые 15 мин) + последовательные `/crush`-воркеры (реальные `crush run --role smart`, не внутренний `Agent`-тул), по одной задаче за раз, с коммитом после каждой.

**Все 11 задач закрыты и закоммичены** (по возрастанию приоритета P1→P2→P3):
- #127 `687eda16` — deploy.go: откат rename-aside при неудаче второго rename (моя регрессия из предыдущей сессии).
- #128 `524fbe58` — Session.Save → точечные апдейты (Rename/SetUsage/SetSummaryAndUsage/SetTodos), Save удалён из интерфейса.
- #129 `56f66cfd` — пагинация transcript: `ORDER BY created_at DESC, rowid DESC` вместо голого created_at; воспроизведено на старом коде.
- #130 `16a83a98` — WebSocket replay buffer: настоящий ring buffer, byte budget 16 МиБ + per-event 1 МиБ лимит.
- #131 `d0aae0ff` — лимит фоновых задач: отдельный atomic-счётчик активных вместо `shells.Len()`.
- #132 `43f8328f` — permission Grant/Deny: `csync.Map.Take` (атомарный get+delete) вместо блокирующей отправки в drained-канал.
- #133 `5d08f5fd` — HTTP debug-логгер: ленивое стриминговое логирование (teeBody + sync.Once на Close), 16КБ preview, RetryTransport ретраит только идемпотентные методы.
- #134 `6e9d2e36` — WSL bash launcher теперь пропускается в пользу Git Bash при резолвинге shebang-интерпретатора.
- #135 `ace69e8f` — grep fallback: проверка ctx каждые 64КБ внутри `readBoundedLine`, не только между строками.
- #136 `d369bec0` — session fork теперь транзакционен (новый `Sessions.ForkSession`), ошибки больше не проглатываются.
- #137 `b53cbf3d` — ConfigStore: межпроцессный file lock (переиспользован `session.AcquireFileLockContext`, тот же механизм что у MCP-конфига).

Каждая задача прошла **облегчённый батч-протокол** (не полный zero-trust ревью на каждый шаг — build + scoped tests лично оркестратором, затем коммит; полное ревью — задача #141 в конце). При двух транзиентных сбоях (`Process restarted`, task-129 и др.) сессии `/crush` перезапускались в тот же `--session` id, как и предписано — не эскалировались как fallback.

**Закрывающая цепочка выполнена частично:**
- #138 `112779e5` — создан новый `CHANGELOG.md` (репозиторий раньше имел только `CHANGELOG.fork.md` — это документ для мёрж-гайдов, НЕ релизный чейнджлог, сказано в его собственном заголовке). Unreleased-секция описывает все 11 фиксов пользовательским языком.
- #139 `112779e5` (тот же коммит) — CHANGELOG.md + чекпоинт `2026-07-29-1342.md` закоммичены.
- #140 (эта задача) — в процессе, чекпоинт пишется прямо сейчас.
- #141 (запуск @oh для финального ревью) — ещё НЕ начата, следующий шаг.

**Побочная арка — правки инструкций `/crush` и `/crush-fallback` (НЕ являются частью review-задач, отдельный запрос пользователя в процессе сессии):**
Пользователь попросил переписать поведение "smart-агент делает ревью воркеров один раз в конце, не после каждого" — сначала это было ошибочно применено к внешнему `/crush`-скиллу (моей собственной инструкции по использованию `crush run`), затем правильно переадресовано на РЕАЛЬНУЮ цель — правило 7 "Orchestrator mode" в `internal/agent/templates/coder.md.tpl` (системный промпт САМОГО crush, показывается его модели только когда настроен `--worker`). Это исправлено и подтверждено тестами `internal/agent/prompt`. Заодно все 7 правил в `coder.md.tpl` сжаты без потери смысла (по отдельной явной просьбе "слишком много текста").

Отдельно — `/crush-fallback` подвергся ДВУМ раундам переосмысления семантики, спровоцированным путаницей в ДРУГИХ параллельных сессиях пользователя (пользователь приносил вставки из сессий с task-номерами #448/#449/#894, не относящихся к этой сессии):
1. Первый раунд: ужесточил правило "не входить в fallback без реально наблюдаемой сигнатуры peak-hours/hard-limit" — это было СЛИШКОМ строго (требовало от агента пруфов даже при явном вызове оператором команды).
2. Пользователь поправил: явный вызов `/crush-fallback <agent>` САМ ПО СЕБЕ есть авторизация, Step 1 — только классификация, не допрос.
3. Финальный, третий раунд (самый важный): пользователь объяснил РЕАЛЬНУЮ семантику — `/crush-fallback <agent>` **регистрирует (armed)** агента на будущее, НЕ переключается немедленно. Переключение происходит автоматически и молча только при одном из ДВУХ триггеров (peak-hours ИЛИ hard-limit) — до этого `/crush` продолжает использоваться как обычно. Файл полностью переписан (`Write`, не `Edit`) под эту модель в 4 копиях: `~/.claude/commands/crush.md`, `~/.claude/commands/crush-fallback.md`, `internal/cmd/claude_slash_command.md`, `internal/cmd/claude_crush_fallback_command.md`. Побайтово проверено, что установленные копии на машине идентичны `sentinel + repo-source-template` (формула из `claude_init.go`).

В ЭТОЙ сессии `/crush-fallback @sh` был явно вызван оператором несколько раз — согласно финальной модели, это создало sentinel-задачу **#142** в статусе `armed` (agent=sh), НИКАКОГО реального переключения не произошло (ни peak-hours, ни hard-limit сигнатура не наблюдались) — `/crush` использовался для всех 11 задач #127-137 как обычно.

**Побочная находка при #136**: CLI `crush sessions fork` (internal/cmd/sessions_fork.go) имеет ту же нетранзакционную форму, что была у серверного handleForkSession — заведена как задача **#143** (P3-подобный, независимая от closing-цепочки, не переверифицирована лично оркестратором).

Все изменения `coder.md.tpl`/`claude_slash_command.md`/`claude_crush_fallback_command.md` (побочная арка про инструкции) **ОСТАЮТСЯ НЕЗАКОММИЧЕННЫМИ** — не было явного запроса на коммит именно этих файлов (в отличие от review-батча #127-137, где коммит между задачами был явно предписан `/babygoal`-целью).

Никаких `/loop` не активно. `/babysit` (`d700ee11`) всё ещё активен, будет самоудалён когда TaskList опустеет (учитывая #142/#143 остаются pending навсегда/до отдельного решения — #142 это вечный sentinel по дизайну skill'а, #143 реальная задача которая рано или поздно должна быть либо взята в работу либо удалена).

## Active goal

Нет `/goal`. Работа управляется TaskList + `/babysit`, установлено через `/babygoal` в начале сессии (после `/resume`).

## TaskList

### in_progress
- #140 Выполнить /checkpoint после завершения всей работы по задачам #127-141 (эта задача, выполняется прямо сейчас)

### pending
- #141 Запустить агента @oh для ревью всей проделанной работы (#127-137)  (blockedBy: #140 — снимется сразу после записи этого чекпоинта)
- #142 crush-fallback state (persistent — do not complete) — armed, agent=sh, ни один триггер не сработал
- #143 CLI `crush sessions fork` тоже нетранзакционен (найдено при #136) — не блокирует остальное, независимая находка

### recently completed
- #139 Закоммитить все обновлённые markdown-файлы (changelog, чекпоинты, планы)
- #138 Обновить CHANGELOG.md по итогам задач #127-137
- #137 ConfigStore не защищён от двух параллельных процессов crush
- #136 fork сессии нетранзакционен, может оставить частично созданный fork
- #135 grep fallback проверяет отмену контекста только между строками
- #134 различать WSL bash.exe от Git Bash/MSYS при резолвинге shebang-интерпретатора
- #133 HTTP debug-логгер буферизует стриминг целиком даже когда включён только уровень выше debug
- #132 повторный/конкурентный ответ на permission request может заблокировать горутину
- #131 лимит фоновых задач считает завершённые задачи активными
- #130 WebSocket replay buffer — байтовый лимит и настоящий ring buffer

## Decisions

- Батч-протокол сменён с "полное zero-trust ревью после каждой задачи" на "лёгкая проверка (build+scoped tests) после каждой + один раз полное ревью в конце" — по явной просьбе пользователя, отражено и в `/crush`-скилле (уже откачено обратно, см. ниже), и в `coder.md.tpl` rule 7.
- Изменения инструкций `/crush`/`coder.md.tpl` в итоге НЕ применены к внешнему `/crush`-скиллу (откачен к исходному виду) — правильная цель была `coder.md.tpl` rule 7 (внутренний системный промпт crush для Orchestrator mode), не моя собственная делегирующая инструкция.
- `/crush-fallback` переопределён как "arm now, switch later on trigger" вместо "switch now on invocation" — по прямому указанию пользователя после двух неверных попыток с моей стороны (сначала слишком строго требовал пруфы, потом слишком мягко переключался немедленно на голый вызов команды).
- CHANGELOG.md создан с нуля как новый файл (не переиспользован CHANGELOG.fork.md, который для другой цели — мёрж-трекинг).
- Найденная при #136 находка про CLI `sessions fork` заведена в отдельную задачу #143, а не исправлена попутно — вне исходного скоупа #127-137.

## Open questions

- Коммитить ли изменения `coder.md.tpl`/`claude_slash_command.md`/`claude_crush_fallback_command.md` (побочная арка про инструкции)? Явного запроса не было — оставлены незакоммиченными, ждут решения пользователя.
- #142 (`crush-fallback state`) — вечный sentinel, останется `pending` навсегда по дизайну (не помечать `completed`) — это ожидаемо, не блокер.
- #143 — требует отдельного решения: брать ли в работу сейчас или оставить в бэклоге.

## Repo state
```
 M internal/agent/templates/coder.md.tpl
 M internal/cmd/claude_crush_fallback_command.md
 M internal/cmd/claude_slash_command.md
 D web/dist/.gitkeep
```
```
112779e5 docs: add release changelog, checkpoint the concurrency remediation arc
b53cbf3d fix(config): add a cross-process file lock around config read-modify-write
d369bec0 fix(session): make session fork transactional, stop swallowing errors
ace69e8f fix(grep): check context cancellation inside readBoundedLine, not just between lines
6e9d2e36 fix(shell): skip the WSL bash launcher when resolving a shebang interpreter
5d08f5fd fix(log): stream HTTP debug logging instead of buffering whole bodies
43f8328f fix(permission): stop Grant/Deny from blocking on a repeat response
d0aae0ff fix(shell): count only active background jobs against MaxBackgroundJobs
16a83a98 fix(server): bound the WebSocket replay buffer by bytes, make it O(1)
56f66cfd fix(db): make transcript pagination deterministic across pages
524fbe58 fix(session): replace whole-row Save with column-scoped updates
687eda16 fix(deploy): restore old binary if the second rename-aside step fails
```

Примечание: `web/dist/.gitkeep` — намеренно нетронутое удаление (см. CLAUDE.md), не коммитить без явного запроса. Все 11 review-фиксов (#127-137) полностью закоммичены; незакоммичены только 3 файла побочной арки про `/crush`/`coder.md.tpl` инструкции.
