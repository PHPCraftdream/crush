# Checkpoint — 2026-08-05 14:46

## Session summary

Continuation of the release-stabilization work on the `crush` fork
(`D:\dev\go\crush`). This checkpoint covers roughly a full working day
in which **every P0 and every P1 from the external release review was
closed**, plus the mailbox migration was finished, plus a feature was
planned. Sixteen commits landed.

The standing `/babygoal` contract drove it: implement tasks via `/crush`
(or `@sh` while the fallback was active), commit between tasks, and at
the end do `/checkpoint`, update the changelog, commit the md, and run an
`@oh` review. `/babysit` cron `f6043a8d` fires every 15 min and has been
picking the next task each time.

**Closed today, in order:** #265 (P0-1 model/prompt contamination between
parallel sessions), #269 (P0-5 lock cleanup unlinking a live OS lock),
#284 (P1-2 interrupt during the DB preamble), #285 (P1-1 inject
duplicate/loss), #281 (stage 2.5 partial deletion), #268 (P0-4
summarization outside session ownership), #272 (P1-3 queue false
success), #273 (P1-4 — **closed as stale**, not fixed), #296 (P1-C
mbReleasing), #299 (regression tests, the gating item), #289 (a test that
caught its own bug 13/20), #307 (inter-turn dispatcher kill), #308 (stage
2.6, the missing migration step), #310 (compaction silent to heartbeat
and watchdog).

**Currently in flight:** #312 — cross-process compaction is not
serialized (`beginCompact` only guards in-process). A `crush run`
(session `p0-4-crossproc`, PID 29152) is working on it; the tree has
`internal/agent/agent.go` modified and a new `compact_lock_test.go`. Not
yet reviewed by me.

**The verification discipline that mattered.** Every delegated diff was
re-read, every suite re-run under `-race`, and every claimed
revert-check reproduced independently. That caught real defects the
agents' own green runs did not: in #269 a test that pinned a DEFECT with
`require.True` plus a removed young-lock guard that re-opened the very
bug being fixed; in #285 tests that mirrored production logic in a local
helper instead of calling it (the #243 defect, recurring); in #296 a
finalize path that handed work back to a turn loop AFTER releasing the
OS lock (two owners across processes) — and, when that was fixed, a
second draft that silently destroyed a queued user message; in #307 the
same message-destruction shape again. The lesson worth carrying: a
green `-race` suite proved nothing in several of these — no test asserted
the invariant being broken, so nothing could fail. Reading the diff is
what caught them.

**One incident worth remembering.** A `crush run` was killed by my own
60-minute timeout *while it was mid-revert-check*, leaving its deliberate
`append(mb.submitted, call, call)` sabotage in the tree. The result built
cleanly and failed 12 tests, which reads exactly like a broken migration.
It was not — a timeout during verification leaves a *deliberately broken*
tree, and `go build` says nothing about that. Also: my own first
revert-check of that same fix targeted the wrong `append` (two functions
append to `mb.submitted`), passed green, and briefly looked like the
agent had fabricated its evidence. It had not.

**Peak-hours fallback fired and was exercised end to end.** Provider
`zai` refused 08:00–12:00; `@sh` took over for five tasks; cron
`c6363790` restored `/crush` at 12:00. The refusal itself exposed that
the `/crush-fallback` instructions required three marker strings while
the sub-agent-stream refusal path emits only one — an agent following
the rule literally would not have switched. Fixed in `a167d36e`, and the
installed copy at `C:\Users\Computer\.claude\commands\crush-fallback.md`
was updated by hand (the deployed binary is stale and `claude-init` would
have re-installed the old text).

**Also done at user request:** `crush models use --large glm4_7` — the
smart slot moved from GLM-5.2 to GLM-4.7. Backup at
`crush.json.bak-before-glm47`. Noted that this is a 1M→200k context
downgrade and that `large` now equals `worker`.

**A feature was planned but not started:** peak hours per weekday
(#314–#318), deliberately blocked behind the release gate. The operator
settled both design questions: numbering is 1=Sunday…7=Saturday (NOT
ISO-8601), displayed as English abbreviations everywhere; and the day
boundary is 00:00 with no anchor logic for overnight windows.

## Active goal

Standing `/babygoal` contract (Stop-hook is user-side; not set by me):

```
/goal реализуй задачи с помощью /crush [or @sh], между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу
```

`/babysit` cron `f6043a8d`, every 15 min (`7,22,37,52 * * * *`), still armed.

## TaskList

### in_progress
- #312 [P0-4 остаточное] Cross-process compaction not serialized — `crush run` in flight

### pending — gate path
- #309 Release gate: 8 mandatory regression scenarios (blockedBy: #296✓, #299✓, #307✓, #308✓, #310✓, #312)
- #313 [LOW] Check whether `internal/shell` is protected from grandchild-wedge like `cliprovider` (blockedBy: #309)

### pending — wrap-up chain
- #303 `/checkpoint` (blockedBy: #309) → #304 CHANGELOG (blockedBy: #303) → #305 commit md (blockedBy: #304) → #306 `@oh` review (blockedBy: #305)

### pending — behind the gate
- #262, #263, #264 (LOW, blockedBy #309)
- #274, #275, #276 (P2, blockedBy #309)
- #311 [OPEN QUESTION] remove synchronous compaction from the response path (blockedBy #309)
- #319 [LOW] `message.CreatedAt` second granularity + random-UUID tie-break in `isAfter` (blockedBy #309)
- #320 [LOW] `TestStreamWatchdog_*` fail under load (blockedBy #309)
- #321 [MEDIUM] `sessions locks` reports `offline` for a live session (blockedBy #309)
- #314–#318 peak-hours-by-weekday feature (blockedBy #309 via #314)

### sentinel
- #261 crush-fallback state — `armed`, agent `sh`. Never real work.

### recently completed
- #310, #308, #307, #289, #299, #296, #273, #272, #268, #281

## Decisions

- **Kept synchronous silent compaction** rather than reverting to a
  background goroutine (#311 filed instead). @fm's review showed the real
  danger was the commit ORDER (delete before writing `SummaryMessageID`,
  on a cancellable ctx), not the synchrony; going back to background
  would re-open the concurrency class the mailbox had just closed.
- **Closed #273 as stale rather than fixing it.** Its evidence had
  dissolved: the live repro was actually P0-D. The one real thing the
  investigation surfaced — `config.go` promising a 900s default that has
  been 2700s since `e9544a8f` — was fixed separately (`e565c030`).
- **Refused the first #296 draft** (hand-back to `mbOwned` after
  `release()`) and the first #307 draft (discarding the pre-empted call).
  Both were "green under -race"; both were data-integrity holes.
- **Blocked all LOW/P2 and the new feature behind the gate** so
  `/babysit` cannot start them before the release closes.
- **Did not deploy.** The running binary is built at `55eb2fc5` and is
  now 16 commits behind. Deploy needs an explicit instruction.

## Open questions

- **#311** — the operator said "кажется, синхронность нужно устранять"
  BEFORE @fm's analysis arrived, and has not re-confirmed since. The task
  records both positions. Not a correctness blocker.
- **#314–#318** — should the peak-hours-by-weekday feature ship in THIS
  release? Currently blocked behind #309. One word unblocks it.
- **Stashes**: four exist. `stash@{0}` (#272 partial) is now superseded
  by better work and is safe to drop; `stash@{2}` (#296 draft) likewise.
  Not dropped — awaiting the operator's word.
- **#321's root cause** is genuinely unknown. Two observations recorded;
  the #310 agent could not explain it and said so.

## Repo state

```
 M internal/agent/agent.go
 D web/dist/.gitkeep
?? dev/
?? docs/checkpoints/2026-08-04-2150.md
?? internal/agent/compact_lock_test.go
```

(`agent.go` + `compact_lock_test.go` are #312's in-flight work, unreviewed.
`web/dist/.gitkeep` deletion is long-standing and must NOT be committed.)

```
5f707abb fix(agent): make compaction report its progress to both the heartbeat and the stream watchdog (P0-4 follow-up)
95a7322c refactor(agent): migrate QueueMessage off the legacy queue, completing the mailbox migration (stage 2.6)
5b2f3ff0 fix(agent): an interrupt between turns no longer kills the dispatcher, and no longer eats a queued message (P1-2 follow-up)
5cc4af02 test(agent): make the reclaim-window cancel test deterministic instead of winning a race 13 times out of 20
834dd274 test(agent,cmd): regression coverage for the four blockers that were fixed without tests
```

Stashes (do NOT blind-pop; each predates several of today's commits):
```
stash@{0}: #272 partial WIP from crush run p1-3-queue — halted by peak-hours mid-delegation, unverified
stash@{1}: P0-5 (#269) partial WIP from @sh agent killed mid-edit — unverified, superseded
stash@{2}: P1-C mbReleasing WIP from killed crush agent (#296) — unverified, superseded
stash@{3}: WIP on scroll-fix: 6c876c66 fix Int64
```
