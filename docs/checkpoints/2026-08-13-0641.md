# Checkpoint — 2026-08-13 06:41 [round-closed-except-p0-1-regression-awaiting-decision]

## Session summary

This session (continuing from checkpoint `2026-08-13-0345.md`) executed the user's `/babygoal`
instruction end to end: implement all 9 findings from `docs/reviews/2026-08-12-post-fix-release-readiness-follow-up.md`
via `/crush` + git worktrees with a commit between each task, then `/checkpoint`, update the
CHANGELOG, commit all markdown, and run an independent `@oh` review of the whole round. All of that
completed. **The pivotal event of this session was the `@oh` review**, which found a P0-severity
functional regression the orchestrator's own zero-trust process had missed — the session has spent
most of its remaining time independently verifying that finding (confirmed, not just trusted),
fixing four smaller follow-ups it also surfaced, and is now genuinely blocked waiting on the user
for how to fix the one big one.

**Execution phase (P0-1 through P2-3, tasks #404-#412)**: ran through the full round using the
established worktree-per-finding pattern from the prior session, zero-trust reviewing every diff
personally before merging. Found and fixed several real bugs along the way that no delegate
reported: a nil-pointer panic in `runWithUnauthorizedRetry` (called `rebuildCall()` unconditionally;
two call sites pass `nil` by design), a genuine deadlock from Go's LIFO defer-execution order in the
interrupt-ticker join (`defer stopTicker(); ...; defer func(){<-tickerDone}()` — the join ran BEFORE
the cancel), a pre-existing broken mock in `internal/app` (session.Service gained 6 orphan-outbox
methods in P0-3 that `mockSessionService` never implemented — broke `go vet`/`go test`, not
`go build`, so it went unnoticed until an unrelated P2-2 diff forced a build of that test package),
and a pre-existing `gofmt` gap from the P1-1 merge. All fixed as their own standalone commits.

**Release gate (task #413)**: done directly by the orchestrator (not delegated — judgment/synthesis
task needing full-round context). Ran the actual `.githooks/pre-push` (build + golangci-lint v2.10 +
full `go test -short -failfast ./...`) — found and fixed 2 real lint findings (gofumpt, unchecked
`rows.Err()`), then came back clean on every package. Wrote
`docs/reviews/2026-08-13-release-gate.md` mapping each of the review's 9 minimum-release-gate
properties to what closes it, disclosing weak spots honestly (properties 6 and 7 rest on code review
rather than fully empirical tests). **Original verdict: GO.**

**Independent `@oh` review (task #419)**: launched with a long, self-contained brief (full context
of what was fixed, the 5 bugs the orchestrator itself had already found, the honestly-disclosed weak
test spots) and explicit instructions to build/vet/test itself rather than just read code. It came
back disagreeing with GO. **Its most serious finding, F1**: P0-1's fix (durable row as sole owner of
a busy-session interrupt, `mb.replacement` skipped for `FromDurableQueue` calls) correctly closes
double-execution, but removes the ONLY in-process continuation path for `crush run` (non-interactive,
`persistentMode=false`) when an interrupt lands on a busy session — `coordinator.Run` has no retry
loop, `sessionAgent.Run`'s turn loop exits on `hasNext=false`, `abandonOwnershipWithHandoff`'s
fallback only picks up calls already in the mailbox (which this call never entered by P0-1's own
design), and `crush run`'s own `RunQueuePump` doesn't drain pending entries before `Shutdown()`
stops it — so an interrupted busy session in `crush run` can genuinely lose the continuation until
some unrelated future `crush` invocation happens to touch the same session/data dir.

**The orchestrator did NOT just trust this — independently reproduced it by reading the full call
chain** (`coordinator.go:452` `Run`, `agent.go:1398` `sessionAgent.Run`'s loop at `1601-1603`,
`abandonOwnershipWithHandoff` at `1079`, `app.go:295` confirming `RunQueuePump` DOES start
unconditionally for `crush run` too, `run_queue_pump.go:398` `Stop()` confirming it only waits for
already-in-flight workers not new pending entries). Confirmed real. Also independently reproduced
`@oh`'s second finding, F5 (`TestP1_1_FastRenewalNoFalsePositive`, added THIS round, misattributed in
the release-gate doc as pre-existing flaky — actual constants `ttl=500ms/margin=100ms` contradicted
the test's own doc comment claiming `TTL=2s/margin=500ms`, leaving only 150ms of real headroom).
Amended `docs/reviews/2026-08-13-release-gate.md` with a clear AMENDMENT section at the top revising
the verdict to **NO-GO**, left the original body intact below it for the record, and filed 4
follow-up tasks (#421 critical, #422/#423/#424 lower severity) plus 3 more findings `@oh` raised
(F2 sharper framing of the already-tracked outbox-drain gap, F3 residual P1-3 atomicity gap, F4 two
vacuous P0-2 tests) as #423/#424 alongside.

**Then closed everything except the big one**: #420 (orphan outbox drain — was already filed before
the `@oh` review, unrelated to F1, done via `/crush` delegate + the orchestrator personally finding
AND fixing a real bug in the delegate's own retry logic: entries that failed a transient
non-exhausted enqueue attempt were left stuck in `'processing'` forever since nothing ever scans that
status — added a new `ReleaseOrphanOutboxEntryForRetry` query+method, revert-checked); #422 (widened
the flaky test's margins to match its own doc comment, done directly, 20/20 clean + clean on the
exact combined `-run` pattern that reproduced the original flake); #423 (fixed `buildAgentModels`'s
remaining torn-read gap and `SetProviderRuntimeConfig`'s shallow-clone bug — the latter meant an
OLDER snapshot's `Providers` map was still mutated by a later write despite `clone()`'s shallow copy,
fixed by building a genuinely independent `*Config` with its own `csync.Map`, done directly with a
new regression test and a real revert-check); #424 (deleted the two vacuous P0-2 tests, done
directly, real coverage of the same property already exists via `TestCheckpointGenerationOverlap`).

**What remains, genuinely blocked**: task #421, the P0-1 regression itself. This needs an
architectural decision the orchestrator explicitly declined to make unilaterally and has now asked
the user about TWICE (once in a full turn-ending message, once implicitly via a `babysit`-tick
"blocked" report) with no reply yet. Two remediation options were named: (1) keep `mb.replacement`
for durable-queue-originated interrupts too, and have ITS OWN execution ack/delete the durable row
via the call's `LogicalCallID`/`InjectID` (re-introduces some coordination complexity between the two
owners, but keeps the in-process fast path); or (2) have `RunNonInteractive`/`Shutdown` actively
drain pending run-queue entries for the active session before the process exits (keeps P0-1's
"durable row is sole owner" model clean, but adds a drain-before-exit step to the CLI shutdown path
that doesn't exist today). Both are real design tradeoffs, not obvious - hence deferring to the user
rather than picking one autonomously for a P0-severity change to the fork's flagship interrupt model.

`/babysit` job `a0285312` (every 15m) is still armed and has been correctly reporting "blocked #421"
on its last two ticks since #421 is the only remaining task and needs human input the tool has no way
to model as a `blockedBy` dependency.

## Active goal

None (no `/goal` Stop hook armed).

## TaskList

### in_progress
(none)

### pending
- #421 Fix P0-1 regression: interrupted busy-owner call can be lost for `crush run` (non-persistent)
  — CRITICAL, confirmed real by the orchestrator's own independent code-chain read, not just taken on
  `@oh`'s word. Awaiting user decision between two remediation approaches (see session summary).
  Not blocked by any other task; blocked only by the need for a human architectural call.

### recently completed
- #404-#412 (all 9 findings from the follow-up review, P0-1 through P2-3)
- #413 Release gate — original verdict GO, amended to NO-GO after independent review
- #414 P2-4 research (prior session)
- #416-#419 (checkpoint, changelog, commit-md, @oh review — the `/babygoal` closing sequence)
- #420 Orphan outbox drain (+ a real stuck-in-processing bug found and fixed along the way)
- #422 Fixed the flaky `TestP1_1_FastRenewalNoFalsePositive` margins
- #423 Fixed the residual P1-3 atomicity gap (`buildAgentModels` + `SetProviderRuntimeConfig`)
- #424 Removed two vacuous P0-2 tests

### deleted (prior session, not this one)
- #415 P2-4 implementation — superseded by the deferred plan doc

## Decisions

- Did the release gate (#413) and the amendment after `@oh`'s findings directly rather than
  delegating either — both are judgment/synthesis tasks needing the full round's accumulated context,
  not code-writing tasks suited to a fresh-context delegate.
- Independently re-verified every significant `@oh` finding by reading the actual code/running the
  actual reproduction before accepting or acting on it — F1 and F5 both confirmed real this way, not
  taken on the review's word, consistent with the zero-trust discipline applied to delegates all
  session.
- Fixed #420/#422/#423/#424 directly (three of them without even spinning up a worktree — #422/#423/
  #424 were small enough to do inline) rather than re-delegating everything through `/crush`, given
  each fix was already fully understood after the investigation needed to confirm the `@oh` finding
  in the first place.
- Explicitly declined to pick an approach for #421 unilaterally — asked the user twice (once in a
  full message, once via a babysit-tick "blocked" report) rather than guessing at a P0-severity
  architectural decision for the fork's flagship non-interactive workflow.
- Amended the release-gate document in place (added a dated AMENDMENT section at the top, left the
  original GO analysis below intact as a record) rather than rewriting it, so the audit trail of what
  was believed when is preserved.

## Open questions

- **#421's remediation approach** — the single open question blocking all remaining work. Two named
  options: (1) keep `mb.replacement` for durable-queue-originated interrupts too, with the
  replacement's own execution acking/deleting the durable row via `LogicalCallID`/`InjectID`; or
  (2) have `RunNonInteractive`/`Shutdown` drain pending run-queue entries for the active session
  before the process exits. Not yet answered by the user as of this checkpoint — asked twice, no
  reply received in either case.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
```

```
bf10289b test(agent): remove two vacuous P0-2 tests found by @oh review (task #424)
b9b48f3e merge: P1-3 residual atomicity gap fix (fix/p1-3-atomicity-gap, task #423)
17c3d543 fix(agent,config): close P1-3's residual atomicity gap (task #423)
0c5eb78c fix(session): widen TestP1_1_FastRenewalNoFalsePositive margins to match its own doc comment (task #422)
a026c47c merge: orphan_call_outbox drain into run queue (fix/p0-3-outbox-drain, task #420)
```

No worktrees remain outstanding — every one created this session (`crush-wt-p01-owner` through
`crush-wt-outbox-drain` and `crush-wt-p13-gap`) has been merged and removed
(`git worktree remove` + `git branch -d`).
