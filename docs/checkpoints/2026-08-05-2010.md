# Checkpoint — 2026-08-05 20:10

## Session summary

Continuation of the release-stabilization round on the `crush` fork (`D:\dev\go\crush`). This checkpoint covers the tail end of a long day: closing the release gate (#309, verdict **GO**), a full `@oh` closing review that found and fixed two real defects in my own #312 work (HIGH-1: OS lock released *after* the mailbox reported idle, reopening the exact race `mbReleasing` exists to prevent; MEDIUM-1: manual `/compact`'s own lock was never wired into the heartbeat activity-notify chain), a user-requested `git push` to `fork/main` that surfaced 16 pre-existing `golangci-lint` findings never caught because the pre-push hook hadn't been run clean all round, and — after the push succeeded — a CI check that found `build (macos-latest)` red on a known flaky test (`TestSessionAgent_HandleWatchdogFire_StoresCauseAndDispatchesDumpAsync`, task #320).

The user then explicitly authorized eliminating all flaky watchdog tests, not just the one that failed. I started reproducing under artificial CPU load (12 parallel `yes` processes) to find genuinely tight timing margins, but the user manually killed those processes — the load was making the whole machine unusable — and told me explicitly: no more CPU bombs. I switched to a pure code-review approach instead: read every timing-dependent assertion in `internal/agent/stream_watchdog_test.go` and judged each on its own margin (ratio of sleep/wait duration to the constant it's racing against), rather than blindly widening everything.

Found and fixed four genuinely tight ones, each revert-checked against the real production code (not just re-run after widening): (1) the confirmed CI failure — `require.Eventually` budget for a goroutine-dump write widened from 5s to 30s, justified by the test's own 300-level-deep `os.MkdirAll` plus `-race` plus full-package parallel contention; (2) `TestStreamWatchdog_SequentialBatchProgressResetsCapClock` — scaled `toolMaxDuration`/`stepGap` 10x (60ms/30ms → 600ms/300ms, same 2:1 ratio, ten times the absolute jitter headroom) — first attempt at this fix broke the test because I'd missed a *second* hardcoded 500ms wait later in the same test body that also needed to scale with the new cap; caught by actually running the test, not just trusting the edit; (3)+(4) `TestStreamWatchdog_HardCapRespected` and its `WithoutExtendsOnProgress` twin — widened `hardCap` 200ms→400ms and the upper-bound assertion window from a 150ms-slack 350ms ceiling to `hardCap+800ms`. All four were revert-checked by temporarily breaking the actual production invariant they protect (disabling the `hardCap` enforcement branches, disabling the tool-progress clock-reset) and confirming the widened test still fails on the right assertion — proving the widening didn't quietly remove correctness coverage, which is exactly what task #320's own text warns against.

**Currently in flight:** a full, unmodified `go test ./internal/agent/ -race -count=1` is running in the background (no artificial load this time) as the final gate before committing the #320 fixes — not yet returned when this checkpoint was written. An earlier `-count=5` full-package run that WAS partially contaminated by the CPU-bomb experiment hit the default 10-minute package timeout with no specific failing test named; that result was explicitly discarded as inconclusive fallout from the abandoned load-testing approach, not treated as evidence of a real flake.

Also still open, paused mid-work: **#262** (attachmentsDataDir test coverage gap from round 8) was `in_progress` when the user asked for the push, then again when CI-checking and #320 work took priority. Not resumed yet.

`/babysit` cron is armed (job id was `025b1422` as of the last checkpoint; not re-verified this session — confirm with `CronList` on resume). The peak-hours-by-weekday feature remains deliberately off the TaskList, living only in `docs/plans/peak-hours-weekdays.md`.

## Active goal

Standing `/babygoal` Stop-hook condition (verbatim, still in force):

```
реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит
всю работу
```

This contract was already fulfilled once this session (checkpoint → CHANGELOG → commit md → @oh review, commits through `e70ad793`); the current work (push, CI fixes, #320) is a new round of activity under the same standing hook, not yet re-closed with its own checkpoint/changelog/review cycle.

## TaskList

### in_progress
- #262 [LOW] Round 8: attachmentsDataDir has zero test coverage, reverting #248 stays green — paused, not resumed
- #320 [LOW] TestStreamWatchdog_* flaky under load — four fixes applied and revert-checked, final full-package verification run in flight

### pending
- #261 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), switch back with /crush-fallback clear (sentinel)
- #263 [LOW] sessions pick has the same --data-dir-forwarding bug #247 just fixed in queue run
- #264 [LOW] sessions_why's two "genuinely dead PID" branches print byte-identical text
- #274 [P2-1] Same-named attachments uploaded in the same second silently overwrite each other
- #275 [P2-2] Concurrent watchdog dumps from one process collide on the same filename
- #276 [P2-3] Hard-cap watchdog log reports idle duration instead of wall-clock elapsed
- #311 [OPEN QUESTION] Убрать синхронную компакцию с пути ответа, не открывая заново гонку
- #313 [LOW] Проверить, защищён ли internal/shell от grandchild-wedge так же, как cliprovider
- #319 [LOW] message.CreatedAt имеет гранулярность в секунду, isAfter разрешает ничью по случайному UUID
- #321 [MEDIUM] sessions locks показывает offline для живой сессии — still unestablished root cause

### recently completed
- #309 Релизный гейт — **GO**
- #322, #323 — release-gate remainder (scenario 3a, #302 test)
- #303–#306 — full checkpoint/changelog/commit-md/@oh-review wrap-up cycle

## Decisions

- **Pushed to `fork` (PHPCraftdream/crush), not `origin` (upstream charmbracelet)** — per CLAUDE.md's remote convention. `main` tracks `fork/main`.
- **Fixed all 16 pre-existing golangci-lint findings before pushing**, rather than `--no-verify` bypassing — the user explicitly chose "fix everything, including ST1008" when asked. Included two production function-signature reorders (`runTurn`, `drainOrReleaseFinal` — error moved to last return value) touching ~25 call sites total, verified by full rebuild + `go vet ./...` + a clean `golangci-lint run` (0 issues) before committing (`ca79b0bb`).
- **Confirmed 8 of the 16 gofumpt findings were pure local-Windows CRLF artifacts** (committed blobs already LF; `git diff` was empty after normalizing the working-tree copies) — normalized in place rather than "fixing" content that was never actually wrong.
- **Rejected treating the `build(macos-latest)` CI failure as a regression.** Traced it to `TestSessionAgent_HandleWatchdogFire_StoresCauseAndDispatchesDumpAsync`'s 5s `require.Eventually` budget, last touched 2026-08-03 (two days before this round started) — pre-existing, already tracked as #320. Reproduced locally in isolation (3/3 green) to confirm it's a full-package-under-load-only flake, not a correctness bug.
- **User vetoed CPU-saturation load-testing mid-task** — 12 parallel `yes` processes made the machine unusable. Switched to code-review-based margin analysis instead of empirical reproduction. This is now a standing constraint for the rest of the session (and probably beyond): no more artificial load generation for flake-hunting.
- **Widened timing margins only where a principled "too tight" case could be made from the code itself** (ratio of sleep to the constant it races, cross-referenced against the one confirmed CI failure's magnitude) — not blanket-widened all 23 tests in the file. Every widened assertion was revert-checked against the real underlying invariant to prove it didn't just get weaker.

## Open questions

- **#311** — synchronous compaction on the response path: still unresolved from earlier in the session, no new input this round.
- **#321** — root cause of false `offline` in `sessions locks` for live sessions: still unestablished, observed multiple times this session, most recently during a `crush run` for #312/round-2.
- **Whether #262 should be resumed now or deferred** — it was mid-flight when interrupted twice (once for the push request, once for the CI check). Not explicitly asked, but flagging since it's the oldest still-open in_progress task.
- **Whether the #320 fixes, once the final full-package run confirms clean, should be committed as their own commit or folded into a fresh checkpoint/changelog/commit-md/@oh-review cycle** matching the standing `/babygoal` contract's shape — the contract says "между тасками делай коммиты" (commit between tasks), which argues for committing #320 on its own once verified, but the contract's closing steps (checkpoint/changelog/@oh review) haven't been re-triggered for this round's work yet.

## Repo state

```
 M internal/agent/stream_watchdog_test.go
 D web/dist/.gitkeep
?? dev/
?? internal/agent/mailbox.go.tmp.2524.ec6bfba9866c
```

(`internal/agent/stream_watchdog_test.go`'s modification is the four #320 fixes described above — NOT YET COMMITTED, pending the final full-package test run in flight. `internal/agent/stream_watchdog.go` itself is byte-identical to HEAD — every revert-check restored it, confirmed via `git diff --stat` returning empty for that file specifically. `web/dist/.gitkeep` deletion is long-standing and must NOT be committed. `internal/agent/mailbox.go.tmp.2524.ec6bfba9866c` is untracked leftover cruft from an earlier agent session in this same conversation — ownership unclear, never touched, safe to ignore or delete at the operator's discretion.)

```
ca79b0bb chore(lint): resolve golangci-lint v2.10 debt accumulated across the round, unblocking pre-push
e70ad793 fix(agent): serialize manual compaction's OS-lock release before mailbox idle, and wire its heartbeat (independent review follow-up)
03e4639b docs: checkpoints, peak-hours-by-weekday plan, and CHANGELOG for this round
f3489c4d test(agent): close the release-gate hole on #302 — abandoned title fallback must not clobber a later title
d4f19852 fix(agent): serialize manual compaction across processes without locking the process out of its own drain (P0-4 residual)
```

`fork/main` is currently at `ca79b0bb` (pushed and confirmed). CI on that push: `lint` ✓, `security` ✓, `build(ubuntu-latest)` ✓, `build(macos-latest)` ✗ (the #320 flake, being fixed right now), `build(windows-latest)` cancelled (fail-fast sibling of the macOS failure). Nothing past `ca79b0bb` has been pushed yet.
