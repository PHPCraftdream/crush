# Checkpoint — 2026-08-04 21:50

## Session summary

Continuation of `2026-08-04-2115.md`. That checkpoint closed with a review
launched against the closing-review blocker fixes (`2cfc71f9`). This one
covers what that review found — and it found a defect that was mine and
not subtle.

**The headline: `2cfc71f9` shipped leftovers from the killed P1-C agent.**
When that agent was stopped for writing `internal/agent/mailbox.go`
concurrently with my own blocker fixes, its work was stashed — but its
edits to `agent.go` had already been staged wholesale and rode along into
the commit. Three concrete consequences, all confirmed here before fixing:

1. `agent.go` was not gofmt-clean (a stray double blank line after the new
   shutdown gate). CI runs golangci-lint with gofumpt, so HEAD would have
   failed lint on a clean checkout. Invisible locally only because this
   tree has CRLF endings, which makes `gofmt -l` flag the whole file and
   bury the single real line — verified by re-running gofmt on
   LF-normalised copies, which is now the way to check formatting here.
2. Three doc blocks plus `IsBusy` described an `mbReleasing` state that is
   never assigned anywhere (`grep "= mbReleasing"` → nothing). The
   constant exists from stage 1; `drainOrReleaseFinal` still releases the
   lock with `mb.mu` held, exactly as its OWN doc 400 lines away says. Two
   contradictory descriptions of one function, the wrong one first.
   `IsBusy`'s change (`state == mbOwned` → `state != mbIdle`) was
   behaviourally inert because only two states are reachable — which is
   precisely why tests passed and it slipped through.
3. Same class as the false-persistence comment that same commit fixed: a
   comment promising a guarantee the code does not provide. It would have
   misled whoever picks up #296.

Four further findings from the same review, all fixed in `8f2c8412`:
- The false "it is already persisted" claim had TWO copies; `2cfc71f9`
  fixed `hardStop`'s and missed the identical sentence in
  `drainAfterCancel`'s stopped branch 420 lines below.
- The title fallback's new guard was **fail-open**: any error from the
  session re-read fell through to the unconditional rename, reinstating
  the clobber-and-loop it exists to prevent — and that read is likeliest
  to fail exactly when it matters (5s budget expiring, DB closing during
  shutdown, both meaning the goroutine was abandoned). Now fails closed.
- `drainAfterCancel`'s stopped branch returned with the spent generation
  handle still set while state stays `mbOwned` — the stale-cancel shape
  rounds 9-13 found five times. Cleared.
- `shutdown_gate_test.go`'s first subtest asserted `require.True` on
  `submit` granting ownership to a hard-stopped mailbox, pinning a DEFECT
  as a contract: anyone later gating `submit` on `stopped` (plausibly
  part of #296) would get a red test reading like their own regression.
  Now logged, not asserted, with the fixture starting from a genuinely
  owned mailbox so the scenario matches its comment.

`mailbox_invariant_test.go` was extended with the shutdown branches it had
stopped covering. One candidate row was deliberately dropped with a
written reason: `interruptAndReplace`'s refusal path mutates nothing, so
the table's blanket "current.cancel must be nil" post-condition does not
apply to it, and forcing it would mean a pointless write.

Also started this session but NOT yet worked: #265 (P0-1) was marked
in_progress by a babysit tick and the investigation began — `applyModelOverrides`
(`coordinator.go:498-543`) confirmed to mutate the SHARED `c.currentAgent`
via `SetModels`/`SetSystemPromptPrefix`/`SetSystemPrompt`, which `runTurn`
then re-reads per turn. No code changed for it yet.

## Active goal

Standing `/babygoal` contract. Commits ✅, CHANGELOG ✅ (`d98c5caf`),
checkpoint ✅ (this file), review ✅ (its findings are what `8f2c8412`
fixed). `/babysit` cron `f6043a8d` still armed, every 15 min.

## TaskList

### in_progress
- #265 [P0-1] Parallel sessions overwrite each other's model/provider/prompt
  state — investigation only, no code written. Top of the queue.

### pending (intended order)
- #265 → #268 [P0-4] → #269 [P0-5] → #284 [P1-2] / #285 [P1-1] → #281
  (stage 2.5, blockedBy #284/#285) → #296 [P1-C] → #299 (regression tests)
  → #277 (release gate, blockedBy nearly everything)
- Deliberately blocked behind #265/#268/#269 so `/babysit` cannot pick
  them first: #262, #263, #264, #272, #273, #274, #275, #276, #289
- #261 — crush-fallback sentinel (armed: sh), never real work

### recently completed
- #301 (P0-C completeness), #302 (title clobber regression), #300
  (timer tickers), #298 (P0-D sub-agent hang), #297 (watch), #295 (P1-B),
  #294 (P0-C), #293 (P0-B), #292 (P0-A), #286 (P0-6)

## Decisions

- Reverted the accidental `mbReleasing` docs/`IsBusy` change rather than
  keeping them "since P1-C will land eventually" — a doc that describes
  unwritten code is worse than no doc, and #296 may well be re-derived
  from scratch rather than merged from the stash.
- Kept `interruptAndReplace` OUT of the invariant table with an explicit
  written rationale, instead of adding a `current.cancel = nil` write
  just to satisfy the table. The table is about operations that hand back
  work or end an era; a pure early return is neither.
- Made the title fallback fail CLOSED. Chose "session may briefly keep an
  empty title" over "a late goroutine may stamp a default over a good
  one", since the latter loops via `needsTitle`.

## Open questions

- Whether the next review round finds a sixth consecutive defect. Rounds
  9-13 each found one in the prior round's fix; round 14 found three
  (two regressions); round 15 found this commit-hygiene defect. The
  findings ARE shrinking in blast radius — from data-loss races to
  comment/format/test-shape issues — but "clean twice in a row" has still
  never been observed.
- #296: port the stash by hand or re-derive? The stash predates
  `2cfc71f9`/`8f2c8412` and a blind `pop` would drop the `mb.stopped`
  gate and the corrected docs. Re-deriving may be cheaper than merging.
- #299's regression tests are still the gating item for #277. The
  production fixes exist for most of the 8 mandatory scenarios; a gate
  declared GO on fixes without tests is not a gate.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
```
```
8f2c8412 fix(agent): remove P1-C leftovers accidentally shipped in 2cfc71f9, and harden two paths it missed
d98c5caf docs: checkpoint + changelog for the closing-review blocker fixes
2cfc71f9 fix(agent): close the three blockers the closing review found in this round's own fixes
b7c94748 docs: checkpoint for the release-review blocker batch + the sub-agent hang root cause
c2eb1f13 docs(changelog): document this round's blocker fixes
```

Stashes (do not blind-pop `stash@{0}` — see #296's description):
```
stash@{0}: On main: P1-C mbReleasing WIP from killed crush agent (#296) - unverified
stash@{1}: WIP on scroll-fix: 6c876c66 fix Int64
```
