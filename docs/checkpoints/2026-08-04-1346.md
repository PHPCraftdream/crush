# Checkpoint — 2026-08-04 13:46

## Session summary

This round started as a `/babygoal` pass over the P0/P1/P2 findings from
`docs/reviews/2026-08-04-multi-agent-stability-follow-up.md` (an independent
review the user pasted in, verdict: release NO-GO, 5×P0 + 4×P1 + 3×P2). I
filed all twelve as tasks #265-#276 plus a release-gate checklist (#277),
spot-checking three of the P0s against live code first — P0-1 and P0-2 were
confirmed exactly as described, P0-5 turned out already partly mitigated.

The mailbox migration then ran per the design doc written in #278
(`docs/plans/2026-08-04-session-owner-mailbox-design.md`), which decomposes
P0-2/P0-3/P1-1/P1-2 into five staged tasks rather than four local `if`s —
the reviewer's explicit warning. Landed: #280 (stage 1, mailbox type,
unwired, `630c498d`), #282 (stage 2.1, submit/drainOrRelease, closes P0-3,
`3646b946`), #283 (stage 2.2, interrupt-and-replace, closes P0-2,
`23004ec3`).

Mid-round the user reported a real production wedge in a sibling repo
(`split-gui` in D:\dev\rust\wezterm: 38 minutes, zero file edits). Diagnosing
it produced two findings that were NOT in the written review:

1. **The heartbeat lies** (#286, filed, not yet fixed). `stream_watchdog.go`
   :332-340 calls `recordActivity()` unconditionally on every tick while ANY
   tool call is open, so the lock mtime cannot go stale for up to 45 minutes
   regardless of progress. I had been reporting "pulse fresh → alive" using
   exactly this metric; the user caught that `sessions list`'s stale UPDATED
   column was the only honest signal. User's decision, recorded in the task:
   remove the timer-based pulse permanently, activity-only.
2. **The real blocker** (#287, FIXED, `6644933c`). `recoverInterruptedTurns`
   runs on every process start, walks EVERY session in the data directory,
   and stamps `FinishReasonError "Process restarted"` on any assistant
   message unfinished and older than 30s — with no cross-process liveness
   check at all. Since `message.Update` rewrites the whole Parts blob from
   the sweep's snapshot, it also clobbers what the live owner streamed. Any
   turn over 30s in process A dies when process B merely starts. This is the
   fork's core use case (N concurrent sessions, one data dir), so it fired
   constantly.

The user reprioritised sharply ("crush не работает, а мы low фиксим"), had
me kill the in-flight `/crush` agent working #283, finish its work by hand,
fix #287 directly, and deploy. All done: both fixes committed with
revert-verified regression tests, `go run deploy.go` shipped `23004ec3` to
the installed binary (verified via `crush --version`).

Not done this round and still open: the remaining mailbox stages (#284
P1-2, #285 P1-1, #281 stage 2.5 cleanup), the other P0s (#265 model-state
contamination, #268 summarization ownership, #269 lock cleanup), P1s (#272
queue lease, #273 tool timeout), P2s (#274/#275/#276), the round-8 LOW
backlog (#262-#264), and the release gate (#277). A round-9 `@oh` review of
everything landed since `1eb4aec3` is being launched as this checkpoint is
written.

## Active goal

`/babygoal` contract: "реализуй задачи с помощью @sh, между тасками делай
коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог,
закомить все мд и запусти ревью агента @oh". Commits ✅, CHANGELOG ✅,
checkpoint ✅ (this file), @oh review being launched now. `/babysit` cron
`f6043a8d` still armed.

## TaskList

### completed this round
- #278 mailbox design doc — `8827e1bd`
- #280 mailbox stage 1 (type, unwired) — `630c498d`
- #282 P0-3 lost wakeup (stage 2.1) — `3646b946`
- #283 P0-2 interrupt-and-replace (stage 2.2) — `23004ec3`
- #287 P0-7 recovery sweep clobbers live sessions — `6644933c`

### pending — blockers first
- #265 [P0-1] parallel sessions overwrite each other's model/provider state
- #268 [P0-4] summarization outside session ownership, can delete live history
- #269 [P0-5] lock cleanup can unlink a live OS lock → two owners
- #286 [P0-6] kill the timer-based pulse (user decision recorded)
- #284 [P1-2] interrupt during DB preamble (mailbox stage 2.3)
- #285 [P1-1] inject duplication at turn boundaries (mailbox stage 2.4)
- #281 mailbox stage 2.5 — delete the old structures
- #272 [P1-3] queue lease / false success
- #273 [P1-4] tool timeout vs configured value
- #274/#275/#276 [P2] attachment + dump name collisions, hard-cap elapsed
- #262/#263/#264 round-8 LOW backlog
- #277 release gate — 8 regression scenarios, blocked on the above
- #261 crush-fallback sentinel (armed: sh) — not real work

## Decisions

- Split #271 into design (#278) + staged implementation rather than one
  dispatch: the same core touched by 8 prior review rounds, and the
  reviewer explicitly warned against four local patches.
- Killed the `/crush` agent mid-#283 on user instruction and finished its
  diff by hand. Its code was sound; I added the missing integration test it
  had only referenced, plus two gaps it left: `Cancel`'s
  `dispatcherCancel` fallback (otherwise a Cancel during OS-lock
  acquisition became a no-op) and `TestHandleInterruptTick`'s assertions.
- Fixed #287 directly instead of delegating, given the urgency — it is the
  defect that actually breaks day-to-day use.
- Pushed back on "make the pulse write updated_at": wiring a lying pulse
  into `updated_at` would have destroyed the one honest signal. User agreed;
  order is now honest-pulse first (#286), surfacing second.
- Deployed from `main` at `23004ec3` without a version bump — `deploy.go`
  replaces the PATH binary in place and leaves live sessions untouched.

## Open questions

- Remaining P0s (#265, #268, #269, #286) are unfixed, so the review's NO-GO
  verdict still stands. The deploy shipped two genuine blocker fixes but is
  not a release.
- `web/dist/.gitkeep` was restored by the killed agent and then shows as
  deleted again in the working tree; left uncommitted either way per
  CLAUDE.md.
- Untracked `dev/` appeared during the round — unexamined, not committed.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
?? docs/reviews/2026-08-04-multi-agent-stability-follow-up.md
```
```
23004ec3 fix(agent): interrupt-and-replace no longer deletes the message it just queued (P0-2)
6644933c fix(app): don't let startup recovery stamp sessions owned by a live process
3646b946 fix(agent): close the lost-wakeup window between final drain and reservation release (P0-3)
630c498d feat(agent): introduce per-session mailbox type (stage 1/5, unwired)
8827e1bd docs: design per-session owner/mailbox for P0-2/P0-3/P1-1/P1-2 (#278)
```
