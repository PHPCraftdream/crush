# Checkpoint — 2026-08-12 15:30 [post-fix-release-readiness-round]

## Session summary

This session worked through a full `/babygoal`-driven round closing the 10 findings from
`docs/reviews/2026-08-12-post-fix-release-readiness-review.md` (NO-GO verdict: 3 release
blockers, 4 high-priority, 1 security, 1 medium-priority, plus a separate P0-3 the review flagged
as a known-and-honestly-documented gap from the prior round's own P0-2 fix). Per the user's
explicit mid-run redirect ("используй worktrees для того, что можно решать параллельно"), the
genuinely-independent findings were parallelized across five `git worktree`s (one branch each:
`fix/p0-4-checkpoint`, `fix/p1-model` [P1-3+P1-4], `fix/p1-pump` [P1-1+P1-2],
`fix/p2-1-lock-metadata`, `fix/sec-1-prompt-log`), each delegated to a `/crush` sub-agent, while
the genuinely-coupled P0-1→P0-2→P0-3 chain stayed sequential in the main tree.

Every delegated diff was zero-trust verified before acceptance: full diff read, every new test
checked for vacuousness (does it actually fail without the fix?), and every concurrency/
correctness-critical fix personally revert-checked (temporarily undone, confirmed the exact
predicted failure symptom, restored, confirmed green). This caught real defects in most of the
delegated diffs: P0-2's atomic-consume query re-selected "the oldest pending row" instead of
matching the specific peeked row (fixed to match by ID, closing a lose-the-wrong-row race);
P0-3's regression test was vacuous (closed the DB entirely, so it could only prove the enqueue
error fires — true whether or not the fix exists — rewritten to fail only `EnqueueRunQueueEntry`
and prove the call actually executes via a real fake-SSE model); a cross-branch coordination gap
where P0-2's rewrite of `handleInterruptTick` introduced a brand-new `pinned=nil` call site that
P1-4 (branched before P0-2 existed) never saw — fixed proactively in main before P1-4 merged, so
the eventual merge was clean; a gofmt miss on `p0_2_fault_injection_test.go`; and an overclaiming
doc comment on `startBoundedDetachedRun` walked back to state its honest best-effort limits.

All five worktree branches are now merged into `main` (commits `96522855`, `c694eda2`, `9ab87906`,
`275824d9`, `0d352acc`), each verified with a full build + `go vet` + `gofmt -l` + the relevant
package's `go test -race -count=1` before and after merging, plus two additional full
`internal/agent -race` runs after the final (`fix/p1-model`) merge to confirm two isolated
timing-flake failures (`TestP0_338_FinalizerReachableDespiteHungCleanup`,
`TestStreamWatchdog_BumpKeepsItAlive`) were pre-existing environmental flakiness under heavy
parallel `-race` load, not merge regressions — both pass reliably in isolation and a clean full
run followed. A whole-repo `go test ./... -race -count=1` was launched in the background
(id `bu9ayf1xn`) as the final gate before this checkpoint; **its result had not yet arrived when
this checkpoint was written — check that background task's outcome first thing on resume.**

`CHANGELOG.md` has been updated with the full round's changelog entry (10-bullet "Ten findings
from a 2026-08-12 post-fix release-readiness review" block, following the exact style of the
prior round's entry). Not yet committed. Still pending at cutoff: confirm the background full
test suite is green, commit `CHANGELOG.md` + this checkpoint + the (still-untracked)
`docs/reviews/2026-08-12-post-fix-release-readiness-review.md` doc, remove the now-fully-merged
worktrees (`crush-wt-p04-checkpoint`, `crush-wt-p1-model`, `crush-wt-p1-pump`, `crush-wt-p2-1-lock`,
`crush-wt-sec1-log`) and their branches, and launch the closing `@oh`-equivalent review of the
whole round's work via the `Agent` tool, zero-trust-verify its findings, then report to the user.
No `git push` has been performed or requested this session — none should happen without an
explicit future ask, per standing `CLAUDE.md` constraints.

## Active goal

None (no `/goal` Stop hook armed this session — `/babygoal`'s TaskList is the governing structure,
tracked via the still-active `/babysit` cron, interval unknown/not re-confirmed this window but
was ticking every ~15 minutes through this session).

## TaskList

### in_progress
- #403 Wrap-up: merge worktree branches, checkpoint, changelog, commit docs, @oh review всего раунда

### recently completed
- #385 P0-1: LogicalCallID теряется при durable-сериализации — повторное исполнение вызова
- #386 P0-2: cross-process interrupt удаляется до гарантированного handoff — событие теряется
- #387 P0-3: orphan recovery оставляет accepted call без runner (runnerless fallback)
- #396 P0-4: checkpoint goroutine переживает Run() и может затереть terminal message старым snapshot
- #397 P1-1: RunQueuePump.Stop может зависнуть навсегда — неограниченный p.wg.Wait()
- #398 P1-2: ошибка lease renewal допускает параллельное выполнение старого и нового owner
- #399 P1-3: model cache без инвалидации — регрессия, введённая UI-BUG-1 этого раунда
- #400 P1-4: session-scoped paths всё ещё падают обратно на shared model (pinned == nil)
- #401 SEC-1: CLI provider пишет полный prompt и raw argv в INFO-логи
- #402 P2-1: решить судьбу lock metadata generation guard — закрыть или явно принять как diagnostic risk

## Decisions

- Chose to parallelize via `git worktree` per the user's explicit mid-run redirect, splitting
  work into 3 sequential (main-tree) + 5 parallel (worktree-isolated) tasks rather than keeping
  the fully-sequential chain used earlier in the session.
- Chose to fix the `handleInterruptTick` nil-pinned gap proactively in main (rather than deferring
  to the P1-4 merge) once discovered, since it was already a live bug in already-committed code
  and I had full context on the exact fix pattern from having just reviewed P1-4.
- Chose to rewrite (not just patch) P0-3's regression test, since the delegated version's own
  comment admitted it would pass even with the bug fully reverted — matching this session's
  established zero-tolerance stance on vacuous tests over accepting "mostly right."
- Chose to document `startBoundedDetachedRun`'s residual risk explicitly (best-effort, not a
  durability guarantee) rather than leave the overclaiming original doc comment, consistent with
  this session's pattern of treating doc-comment honesty as part of the fix, not polish.
- Merge order was chosen safest-first (SEC-1, P2-1, p1-pump, p0-4-checkpoint, p1-model) — smallest
  blast radius / least entangled with main's own recent P0-1/P0-2/P0-3 changes first, saving the
  most likely-to-conflict branch (p1-model, touching `coordinator.go`) for last with full context
  fresh.

## Open questions

- None requiring user input yet. The one outstanding technical unknown — whether the background
  whole-repo `go test ./... -race -count=1` (task id `bu9ayf1xn`) is clean — is a verification
  step to resolve on resume, not a decision for the user.

## Repo state

```
 M CHANGELOG.md
 D web/dist/.gitkeep
?? dev/
?? docs/reviews/2026-08-12-post-fix-release-readiness-review.md
```

```
0d352acc Merge branch 'fix/p1-model' into main (P1-3, P1-4)
275824d9 Merge branch 'fix/p0-4-checkpoint' into main (P0-4)
9ab87906 Merge branch 'fix/p1-pump' into main (P1-1, P1-2)
c694eda2 Merge branch 'fix/p2-1-lock-metadata' into main (P2-1)
96522855 Merge branch 'fix/sec-1-prompt-log' into main (SEC-1)
```

Worktrees still present (all fully merged, safe to remove once the background full-suite
confirms green): `D:/dev/go/crush-wt-p04-checkpoint`, `D:/dev/go/crush-wt-p1-model`,
`D:/dev/go/crush-wt-p1-pump`, `D:/dev/go/crush-wt-p2-1-lock`, `D:/dev/go/crush-wt-sec1-log`.
