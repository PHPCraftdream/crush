# Checkpoint — 2026-08-04 09:18

## Session summary

Continuation of the same multi-round session arc (see `2026-08-04-0716.md`
for the full history through the seventh "closing" `@oh` review). The user
re-invoked `/babygoal` with the standing contract text ("implement via
`/crush`, commit between tasks; then checkpoint + changelog + commit .md +
launch `@oh` review") to work through the five remaining backlog tasks
(#247, #248, #257, #258, #259) that were intentionally parked at the end of
the prior round. `/babysit` re-armed (`8613b3ee`, every 15m).

The first `/crush run` (task #247) failed mid-session with a `zai`
peak-hours refusal ("provider zai is in peak hours (08:00–12:00), refusing
until 12:00") — not a transient error. The user had pre-armed
`/crush-fallback @sh` for exactly this scenario immediately prior; per its
contract, fallback was switched to ACTIVE (trigger: peak-hours, provider:
zai, until 2026-08-04T12:00:00+02:00) with a durable one-shot auto-revert
cron (`47e0880a`) scheduled for that time. All five remaining tasks were
then completed via local `Agent(subagent_type: "sh")` sub-agents instead of
`crush run`, each independently zero-trust verified (diff read, `go
build`/`go vet`, targeted + relevant package `-race` test reruns) before
being accepted:

- `89495720` — **#247**: `queue run`'s spawned `crush run --session ...`
  children now inherit the parent's resolved `--data-dir` (previously each
  child re-resolved its own from `--cwd`, diverging from the parent when an
  explicit `--data-dir` was passed). New test uses a `queueTaskExecOverride`
  seam to intercept child argv without spawning a real process.
- `4d69e5cd` — **#248**: of the cwd-hardcode catalog, only the web UI's
  `saveAttachmentToDisk` (internal/server/handlers.go) was a genuine bug
  (the rest — prompts/mcp/skills/commands — are legitimately project-scoped
  assets, left untouched per the reviewer's own assessment). Attachments now
  save under the resolved data directory instead of always `<cwd>/.crush`.
- `b7aa0ec1` — **#257+#258** (combined, same file): `sessions why`'s
  age-bound branch now calls `IsProcessAlive` to distinguish a genuinely
  dead PID from a real OS-reuse case before choosing reason-text wording
  (previously always guessed "PID reuse"); the unreadable-PID (`pid<=0`)
  case no longer cites a fictional "PID 0" and instead names the actual
  evidence (stale heartbeat).
- `37bf3d9e` — **#259**: corrected the CHANGELOG bullet (added in the prior
  round in response to a Stop-hook nudge) that misattributed the
  PID-fallback-boundary test-timing-margin widening (tasks #246/#251) to
  the unrelated `crush models use --large/--small` feature (task #249).
  Done directly (Edit + commit), not delegated — a one-line doc fix.

All five were independently re-verified by the orchestrator (not just
trusted from sub-agent claims): every commit's diff was read directly,
`go build ./...` / `go vet` were rerun, and the relevant test scopes were
rerun with `-race` (targeted `sessions_why` suite: 12/12 pass including the
two real-spawn tests, not skipped; `internal/server` full package: pass;
`internal/cmd` full package had already been run once by the #247 sub-agent
— 362s ok — and was not redundantly rerun in full a second time, only
targeted subsets were).

CHANGELOG.md was kept up to date incrementally — each of the four
code-fixing commits added its own `### Fixed` bullet as part of that same
commit, and #259's commit corrected the one pre-existing misattribution.
No separate end-of-round CHANGELOG pass was needed.

TaskList is now fully caught up: 20 of 21 real tasks completed (#241-259),
plus the two persistent scaffolding tasks (#260 this wrap-up, #261 the
crush-fallback state sentinel, currently ACTIVE).

## Active goal

The `/babygoal`-issued standing contract for this round ("implement via
/crush, commit between tasks; then checkpoint + changelog + commit .md +
launch @oh review") is being satisfied by this very task (#260). No
separate `/goal` Stop hook is armed this round (per `/babygoal`'s own
design — it uses `/babysit` + TaskList, not `/goal`).

## TaskList

### in_progress
- #260 this wrap-up task (checkpoint/changelog/commit-md/review), about to
  proceed to the review launch step

### pending
- #261 `crush-fallback state (persistent — do not complete)`: sh — ACTIVE
  (peak-hours, until 2026-08-04T12:00:00+02:00, auto-revert cron
  `47e0880a` armed) — sentinel, not real work, intentionally never
  completed/deleted

### recently completed
- #259 CHANGELOG misattribution fix — `37bf3d9e`
- #257+#258 sessions_why reason-text cases — `b7aa0ec1`
- #248 attachments dir honors configured data directory — `4d69e5cd`
- #247 queue run forwards --data-dir to spawned children — `89495720`
- (#241-246, #249-256: see `2026-08-04-0716.md` and earlier checkpoints)

## Decisions

- Honored `/crush-fallback @sh`'s "switch immediately and silently, don't
  ask" instruction the moment the first `/crush run` hit a peak-hours
  refusal — did not fall back to waiting out the window or asking the user,
  per the skill's explicit contract armed just one turn earlier.
- Combined #257 and #258 into a single `Agent` dispatch (same file, same
  function, adjacent branches) rather than two separate sequential
  dispatches, to avoid a two-step edit-conflict risk on the same few lines.
- Did #259 (a one-line CHANGELOG wording correction) directly rather than
  delegating — consistent with the `/crush` skill's own "one- or two-line
  work: delegation overhead > the change" guidance, which applies equally
  to the `sh`-agent fallback transport.
- Did not re-run the full `internal/cmd` package suite a second time after
  #247's sub-agent already ran it in full (`-race -count=1`, 362s, ok);
  subsequent commits in the same package were verified via targeted
  `-run` subsets instead, to avoid burning ~6 more minutes per commit on a
  suite that was already known-green immediately prior for the same
  package with only small, non-overlapping diffs added since.

## Open questions

None blocking. The one live piece of state to track: the peak-hours
fallback (#261) will auto-revert to `/crush` at 12:00 local via the durable
cron `47e0880a` — no user action needed unless they want to end fallback
early with `/crush-fallback clear`.

## Repo state

```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-08-04-0716.md
```
```
37bf3d9e docs: correct CHANGELOG misattribution of timing-margin test fixes
b7aa0ec1 fix(cmd): give sessions why's PID-fallback reason text its actual case
4d69e5cd fix(server): honor configured data directory for saved attachments
89495720 fix(cmd): forward --data-dir to queue run's spawned children
f3b1425b docs: changelog entries for tasks #254-256 (sixth @oh review round)
```
