# Checkpoint — 2026-08-06 08:21

## Session summary

Closing checkpoint for the round documented in `2026-08-06-0000.md`. That round (tasks #320, #262–#276, #313, #319, #321, plus a follow-up #324) is fully committed and pushed to `fork/main` (PHPCraftdream/crush) as of `291d544e`, with an independent `@oh` review (11/11 CONFIRMED, three wording nits fixed in a follow-up commit) and CI running on the push.

After that push, the remaining item was task **#311** — the open design question about removing synchronous compaction from the response path. This was surfaced to the user directly (not attempted unilaterally, per the task's own explicit "do not start without operator decision" gate and the six-rounds-of-defects warning it carries for this exact mailbox-ownership-transfer pattern). The user's answer: **defer — "пока не делаем"** (not doing it for now). No code change resulted; the open question remains open, just not currently being pursued.

The user then asked, in one message, to: delete the entire TaskList, write this checkpoint, update the CHANGELOG, commit all docs, and push. Executed in that order:

1. **TaskList cleared** — all ten remaining entries (#261 sentinel, #264/#274/#275/#276/#313/#319/#321/#324 completed, #311 deferred) deleted via `TaskUpdate(status: deleted)`. Nothing left on the list.
2. **This checkpoint** — written now.
3. **CHANGELOG.md** — checked, not modified: no code behavior changed since the last CHANGELOG update (`79508bac`/`4a98c98c`, already pushed), because #311 was explicitly deferred rather than implemented. Nothing to add; fabricating an entry for undone work would misrepresent the changelog's own stated purpose ("tracks what actually changed in behavior").
4. **Docs commit** — this checkpoint file (plus one more code fix, see below).
5. **Push** — done, see below.

`/babysit`'s cron (`025b1422`) was already stopped in the prior turn (deliberately, after several identical "blocked on #311" ticks in a row made continued polling pointless — deleted rather than left to fire forever against a stall only the user could resolve). Not re-armed; the user did not ask for it and the TaskList is now empty, so there is nothing for a babysit tick to act on regardless.

**Unplanned but necessary detour before the push completed**: checked GitHub Actions CI on the `291d544e` push (from the prior round) before writing this checkpoint's "repo state" section, and found `build (ubuntu-latest)` had genuinely FAILED — not the macOS/Windows fail-fast cancellations, a real failure. Root cause: `TestProcessIsolation_GrandchildHoldingStdoutDoesNotWedgeForever`, the #313 regression test explicitly flagged at commit time as untestable on this Windows dev machine (`!windows` build tag), failed on its actual first execution on ubuntu-latest — `err == nil` when the test asserted `err != nil`. Investigated: this was a bug in the TEST's assertion, not in the underlying fix. `cmd.Wait()`'s return value reflects the DIRECT child's (`sh`'s) own exit status; in this test's `sleep 60 & echo done` scenario (no `wait`), `sh` exits cleanly almost immediately, well before ctx's 500ms timeout — the forced kill 2s later reaches the backgrounded GRANDCHILD (`sleep`) to unblock the stdout pipe, but doesn't change `sh`'s already-clean exit status, so `err == nil` is actually the CORRECT outcome. The sibling test (`TestProcessIsolation_ChildProcessGroupKill`, `sleep 60 & wait`) legitimately expects `err != nil` because `wait` keeps `sh` itself blocked in the foreground, so its cancellation kill hits `sh` directly — the new test had copied that assertion without accounting for the scenario difference. Fixed by replacing the wrong assertion with a `t.Logf` and keeping the real property under test (elapsed time bounded, ≤4s) — the CI failure's own observed numbers (elapsed ≈2.5s = 500ms ctx + 2s killTimeout) are exactly what the original fix's doc comment already predicted, so this is strong independent confirmation the underlying process-group-kill protection genuinely works; only the test assertion was wrong. Committed as `7a66f291`, cross-compile-checked the same way the original commit was (Windows machine still can't run the file natively).

## Active goal

None currently armed. The standing `/babygoal` contract that governed the prior round (checkpoint → CHANGELOG → commit `.md` → `@oh` review) was already fulfilled once for that round's work; this checkpoint is a second, smaller closing sequence for the "defer #311 + clear TaskList" tail end, done at the user's explicit request rather than under a live Stop-hook condition.

## TaskList

Empty. All prior entries deleted per explicit user instruction this turn.

## Decisions

- **#311 deferred, not implemented** — user's explicit choice ("пока не делаем") after being shown the full context (original lean toward removing sync compaction, `@fm`'s subsequent counter-argument verified against the code, the six-rounds-of-defects warning for any new mailbox-ownership-transfer pattern in this file, and the cheap UI-only alternative of showing "compacting…" instead of a silent busy state). No code touched for this task at any point.
- **CHANGELOG.md left unmodified this turn** — verified there is no new behavior change to document (the only candidate, #311, was explicitly not done). Updating it with a no-op entry would be dishonest bookkeeping.
- **babysit cron was already stopped** (previous turn, `025b1422` via `CronDelete`) once it became clear repeated ticks would keep reporting the same "blocked on #311" status indefinitely with no way to make progress autonomously. Not re-armed this turn since the user didn't ask and the TaskList is empty.

## Open questions

- **#311** (no longer tracked on the TaskList, but still a live open question if the user wants to revisit it later): should synchronous compaction be removed from the response path? Deferred, not resolved. If picked back up, the task's own prior analysis (see the `2026-08-06-0000.md` checkpoint's Open Questions section, or the conversation transcript around when this was surfaced) still applies: don't return to the old detached-goroutine shape (regression), consider the "turn returns to caller, mailbox stays owned by a detached goroutine that finishes compaction and calls drainOrReleaseFinal itself" third option instead, only as its own dedicated review round, and only if the amortized-cost argument stops holding for this fork's actual usage pattern.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
?? docs/checkpoints/2026-08-06-0821.md
?? internal/agent/mailbox.go.tmp.2524.ec6bfba9866c
```

(`web/dist/.gitkeep` deletion is long-standing and must NOT be committed. `dev/` and `internal/agent/mailbox.go.tmp.2524.ec6bfba9866c` are untouched stray artifacts from earlier in this session, ownership unclear, safe to ignore or delete at the operator's discretion — unchanged status since the last checkpoint.)

```
7a66f291 fix(shell): correct #313 regression test's wrong error-value assertion (real CI failure)
291d544e test(prompt): isolate internal/agent/prompt tests from the operator's real global config/skills (#324)
79508bac docs: correct wording per independent @oh review of this round (11/11 CONFIRMED)
4a98c98c docs: CHANGELOG and checkpoints for the #320/#262-#276/#313/#319/#321 round
a46659c1 fix(cmd): sessions locks no longer discards fresh top-level activity for offline verdict (#321)
```

`fork/main` was at `291d544e` (pushed and confirmed) when CI on that push surfaced the real `build (ubuntu-latest)` failure described above; `7a66f291` fixes it. This checkpoint file plus `7a66f291` are what remain to be pushed as of writing — both go out together right after this file is committed.
