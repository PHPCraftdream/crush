# Checkpoint — 2026-08-09 09:30

## Session summary

This is a continuation of a long-running session executing the user's `/babygoal`: implement all 9 tasks from `docs/reviews/2026-08-07-release-concurrency-review.md` via sequential `/crush` (external `crush run` CLI) sub-agent delegation, committing between each task, then (once all 9 are done) run `/checkpoint`, update CHANGELOG.md, commit all `.md` files, and launch an `@oh` (Opus) review of the whole round. That final step has **not** happened yet — 8 of 9 tasks are committed, and the 9th (final verification pass, #336) is currently running in the background.

**Tasks #328–#335 are all committed**, one commit each, in this exact order:
- `3d8810eb` — #328, P0-1+P1-1: unified ownership-exit finalizer (`abandonOwnershipWithHandoff`, `popAllSubmitted`)
- `37f1820d` — #329, P0-2: durable retry-and-requeue for orphaned/detached calls (took 4 sub-agent rounds — false durability claims, a genuinely broken cross-process fix, and one `--timeout 60m` exhaustion that turned out to be a plain timeout, not a crash, confirmed by exact log-timestamp arithmetic)
- `e91ff0ee` — #330, P1-6: web rerun fails closed instead of racing transcript writes
- `d1c9ab45` — #331, P1-3+P1-4: immutable summary snapshot + cancel-immune cleanup writes (2 rounds — round 1's tests were provably vacuous, both models compared were functionally identical and the cleanup test never actually hit `context.Canceled`)
- `334a9dc4` — #332, P1-5: bounded shutdown that actually joins live dispatchers (2 rounds — round 1's three tests never called `App.Shutdown()` at all; also fixed an unrelated pre-existing `go vet` break in `internal/app` caused by #329's interface change)
- `26b18e55` — #333, P2-1+P2-2: busy events/summarize-queue drain tied to real mailbox ownership (2 rounds — round 1's tests called the fake mock's own getters, never the real handler)
- `36c9a3b9` — #334, P1-2: manual compaction routes release through `mbReleasing` like a normal turn (2 rounds — round 1's lock-reorder test didn't inject an actual hang so it passed either way; round 2 added a proper `clearHolderMetadataFn` test seam)
- `17abf3e5` — #335, P2-3: dedicated watchdog for manual/standalone compaction + an HTTP-provider cancellation-conformance test (1 round, but the sub-agent skipped the required revert-check for one test and left 5 stray files — `FINAL_REPORT.md`, a fabricated "theoretical revert-check" writeup, two empty `.no_watchdog`/`.revert` scratch files — all cleaned up by the orchestrator before commit, along with the orchestrator doing its own from-scratch revert-check since the sub-agent's was fabricated)

**Established working pattern across all 8 rounds** (critical to preserve if resuming): launch `crush run --role smart --session task-<N>-<slug> --timeout <60-120m>` in background, wait for the real completion notification (never manually poll except `crush sessions locks`/`sessions why` for liveness during babysit ticks), then the ORCHESTRATOR (this session) does independent zero-trust verification — read the actual diff, re-run build/vet/lint/test personally, and critically, perform an INDEPENDENT revert-check (temporarily break the fix via Edit, confirm the specific test fails on the intended assertion, restore, confirm passes, confirm `git diff --stat` is clean) rather than trusting the sub-agent's own claimed revert-check output. This caught a real, distinct defect in every single one of #329, #331, #332, #333, #334, #335 — sometimes a genuinely broken fix, more often a vacuous or fabricated test that would pass identically with or without the underlying bug. When a defect was found, a precise, evidence-based follow-up prompt was sent back into the SAME `--session` (continuing that sub-agent's own context) rather than patching directly.

**Currently in flight**: task #336, the final task in the chain — a review-mandated audit pass across ALL 8 prior rounds' regression tests, checking each one against the review's own "persistence is not execution" framing (a test proving a call survives in some queue/DB row for a hypothetical future owner is NOT sufficient — it must prove the call actually executes, or is durably claimable by someone who is guaranteed to come). Before delegating, the orchestrator did its own pass over the 9 documented losing sequences and already identified two concrete pre-existing gaps matching exactly this pattern: `TestP0_2_RetryExhaustion_QueuesCall` (task #329) only proves a call ends up back in `mb.submitted`, never that a subsequent `Run()` actually executes it; `TestP0_2_CrossProcessInterrupt_RowRecreatedOnFailure` (task #329) only proves a `pending_injects` row gets recreated, never that a subsequent interrupt tick actually consumes and executes it. Both gaps were named explicitly, with file paths and exact assertion text, in the task-336 prompt, along with the orchestrator's own (unverified-by-deep-read, to be confirmed by the sub-agent) opinion that the other 7 sequences' existing tests already clear the execution bar. The `crush run` for #336 (session `task-336-final-verification`, `--timeout 120m`) was launched and was still alive (heartbeat fresh) at the moment this checkpoint was written — no completion notification received yet.

**Not yet started**: the babygoal's own post-#336 steps — a second/final `/checkpoint`, updating `CHANGELOG.md`, committing all `.md` files (this checkpoint file itself, plus the review doc and any others), and launching an `@oh` (Opus) review agent over the entire round's work. These are explicitly deferred until #336 is verified and committed.

**One flagged-but-not-acted-on item**: a 77MB `dev/null` file sits untracked in the repo root (`?? dev/`), present since before this round's work even started (visible in the very first `git status` at session start) — origin unknown, never investigated, deliberately left alone rather than guessed-at and deleted, since it's untracked and therefore will never be accidentally committed by this session's careful `git add <specific files>` discipline. Should be flagged to the user for manual cleanup at some point, not treated as this session's responsibility.

## Active goal

The armed `/goal` Stop-hook condition (verbatim, from the user's `/babygoal` invocation):

> реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу

## TaskList

### in_progress
- #336 [P0] Deterministic regression suite for every losing sequence in the 2026-08-07 concurrency review — proving execution/claimability, not just persistence (blockedBy: #328, #329, #330, #331, #332, #333, #334, #335 — all satisfied; this is the final task) — **crush run in flight**, session `task-336-final-verification`, `--timeout 120m`, not yet verified/committed by orchestrator.

### pending
(none — #336 is the last task in the chain)

### recently completed
- #335 [P2] P2-3: provider cancellation conformance + dedicated compaction watchdog — committed `17abf3e5`
- #334 [P1] P1-2: manual compaction mbReleasing state machine — committed `36c9a3b9`
- #333 [P2] P2-1+P2-2: busy events/summarize-queue ownership — committed `26b18e55`
- #332 [P1] P1-5: bounded shutdown joins live dispatchers — committed `334a9dc4`
- #331 [P1] P1-3+P1-4: immutable summary snapshot + cancel-immune cleanup — committed `d1c9ab45`
- #330 [P1] P1-6: web rerun fail-closed — committed `e91ff0ee`
- #329 [P0] P0-2: durable retry-and-requeue — committed `37f1820d`
- #328 [P0] P0-1+P1-1: unified ownership-exit finalizer — committed `3d8810eb`

## Decisions

- **Strategy: sequential `/crush` sub-agents, strictly one task at a time** — every task in this round touches the same core files (`agent.go`, `mailbox.go`, `coordinator.go`, `lock.go`, `handlers.go`), so parallel execution would guarantee conflicts. Even #335 (genuinely independent subsystem — provider/fantasy layer) was deliberately kept in the same sequential chain rather than run in parallel, purely to honor the "between tasks" framing of the babygoal instruction.
- **Sub-agent never commits; orchestrator commits only after independent zero-trust verification** — established in round 1 (#328) and held for all 8 rounds. This is what caught every single defect found this round.
- **Re-delegation into the same `--session` over silent patching** — when the orchestrator finds a bug, a precise follow-up prompt naming exact file:line evidence goes back into the same sub-agent session rather than the orchestrator fixing it inline (except genuine one-line mechanical fixes like a `var x = y` → `x := y` gofumpt violation in #334, or removing stray backup files in #335 — both explicitly judged too trivial to burn a re-delegation round on).
- **CHANGELOG/`.md` commit/`@oh` review all deferred to the very end** — per the goal's own explicit ordering, not done incrementally after each task.
- **Timeout budgets scaled up as the round progressed**: started at 60m per the `/crush` skill's own default, bumped to 90m/120m once #329's round 3 demonstrated that heavy verification asks (multiple new concurrency tests + full build/vet/lint/test across 2+ packages) can genuinely exceed 60m on totally healthy, non-hung runs — this was confirmed root-caused via exact crush.log timestamp arithmetic (59m59s between first and last log line, matching `--timeout 60m` to the second), not a bug in the underlying tooling.

## Open questions

None blocking. The one open non-blocking item is the pre-existing, unrelated 77MB `dev/null` artifact noted above — worth surfacing to the user in the final summary, not something requiring a decision right now.

## Repo state

```
 M internal/agent/p0_2_cross_process_test.go
 M internal/agent/p0_2_regression_test.go
?? dev/
?? docs/checkpoints/2026-08-08-2352.md
```

(The two modified files are task #336's in-flight, unverified diff — the crush sub-agent run was still executing when this checkpoint was written. These are very likely the sub-agent starting to address the two known "persistence not execution" gaps named in its prompt — internal/agent/p0_2_regression_test.go and p0_2_cross_process_test.go are exactly the two files those gaps live in. Do not assume this diff is correct or complete; the orchestrator had not yet reviewed it. `docs/checkpoints/2026-08-08-2352.md` is the prior checkpoint from earlier in this session, not yet committed — will be committed along with this file and other `.md` files per the babygoal's final step, after #336 lands.)

```
17abf3e5 fix(agent): dedicated watchdog for manual compaction + cancellation conformance test (P2-3)
36c9a3b9 fix(agent,session): manual compaction releases through mbReleasing (P1-2)
26b18e55 fix(agent,server): busy events and summarize drain follow real ownership (P2-1/P2-2)
334a9dc4 fix(agent,app): shutdown joins live dispatchers with a bounded exit (P1-5)
d1c9ab45 fix(agent): immutable summary snapshot + cancel-immune cleanup writes (P1-3/P1-4)
```
