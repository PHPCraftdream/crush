# Checkpoint — 2026-08-11 10:05 [full-project-review-babygoal-round]

## Session summary

Continuing directly from the prior checkpoint (`2026-08-11-0805.md`),
the user ran `/babygoal` with the instruction: implement the remaining
review-followup tasks via `/crush`, commit between each task, then
after all work is done run `/checkpoint`, update the changelog, commit
all `.md` files, and launch an `@oh`-style review agent to check the
whole round.

At the time `/babygoal` was invoked, the prior full-project-review
round's 5 confirmed fixes (B-1, B-2, BUG-1, BUG-2, BUG-3) were verified
locally but **not yet committed**, and a full-repo `-race` test run was
still pending. `/babygoal` decomposed the remaining work into 11
TaskList items (#356-366): task #365 first (commit the 5 pending
fixes, blocked on the pending background test), then BUG-4 and the 8
code-smell findings (CS-1..CS-8) as tasks #356-364 in a strict
sequential chain (`blockedBy` chained one after another — deliberately
serial, not parallel, since several findings touch overlapping files
in `internal/agent/cliprovider` and the user asked for a commit between
every task), and finally task #366 (this wrap-up). A `/babysit` cron
(job `713690b3`, every 15 min) was armed as the heartbeat per the
skill's contract.

**Environment anomaly discovered and worked around:** attempts to use
a long-running background `crush sessions watch <id>` as a blocking
wait primitive for in-flight `/crush` sub-agent sessions were killed by
the harness within seconds, repeatedly (3 attempts, all killed). The
underlying `crush run` OS processes themselves were NOT affected —
confirmed alive and progressing via `crush sessions why`/`sessions
last` throughout. Adapted to point-in-time liveness checks
(`sessions why`/`sessions last`) on each Stop-hook re-fire instead of
a genuine blocking wait — this is driven by the hook's own re-invocation
cadence, not a self-initiated sleep-poll loop, so it does not violate
the standing no-manual-polling rule.

**All 5 pending fixes from the prior round were committed first**
(`652f87cc`), after discovering the two overlapping background test
runs across the `/resume` boundary had corrupted the shared log file
(visible interleaving artifact on one line) — re-ran the full-repo test
into a fresh, uncontested log file to get trustworthy verification
before committing, rather than trusting the corrupted log or the bare
"exit 0" notification text.

**All 9 review-followup tasks (BUG-4, CS-1..CS-8) are now closed**,
one commit each, matching this session's established zero-trust
discipline throughout — every `/crush`-delegated diff was independently
re-read (not trusted from the sub-agent's summary), re-built, re-tested,
and for the two security-relevant fixes (BUG-4's invariant test, CS-8's
SSRF guard) a genuine revert-check was performed personally (temporarily
broke the invariant/guard, confirmed the exact predicted test failure,
restored, confirmed pass again) even though the delegated sub-agent had
already run its own checks.

Three tasks (CS-4, CS-5, CS-7) were judged small enough to fix directly
rather than round-trip through `/crush` (one-line/few-line mechanical
changes — matches the `/crush` skill's own "when NOT to delegate: one-
or-two-line work" guidance). CS-2 was investigated by `/crush` and
correctly concluded NOT to change: the proposed fix (moving `mu` to a
pointer to silence a `go vet copylocks` warning) would have broken
`encoding/json`'s config-loading path, which relies on `Map[K,V]`'s
zero-value being immediately lock-usable. BUG-4 was similarly
investigated (both by the user's own prior code-reading and
independently by the `/crush` sub-agent) and confirmed to be a
currently-unreachable invariant rather than an active bug — closed
with a pinned regression test and a warning comment instead of
restructuring sensitive turn-lifecycle code.

CS-6 (Qwen MCP token entropy) involved real research that overturned a
documented assumption: the code comment claiming "qwen doesn't support
custom HTTP headers" was checked against the actually-installed
`@qwen-code/qwen-code@0.15.11` npm package and found outdated —
switched to `Authorization: Bearer` with full 256-bit entropy, matching
the other CLI-provider integrations.

CHANGELOG.md has been updated with one consolidated entry covering all
11 findings from this full-project-review round (2 blockers + 4 bugs
+ 5 code-smell items that resulted in a code change; CS-2 mentioned as
investigated-and-left-alone within the same entry). This checkpoint is
being written next; committing all `.md` changes together and launching
the closing `@oh`-equivalent review are the two steps still pending
after this file is written.

## Active goal

A session-scoped Stop hook is currently armed with condition: "реализуй
задачи с помощью /crush, между тасками делай коммиты. После завершения
всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и
запусти ревью агента @oh - пусть проверит всю работу" — it auto-clears
once satisfied; do not tell the user to run anything to clear it early.

## TaskList

### in_progress
- #366 Wrap-up: checkpoint, changelog, commit docs, @oh review of whole round

### recently completed
- #364 CS-8: download/fetch tools have no SSRF filtering for loopback/private/metadata addresses
- #363 CS-7: MCP bash tool generates two unrelated UUIDs per call (toolCh id vs ToolCallID)
- #362 CS-6: Qwen MCP token has weak/predictable entropy, exposed via URL query param
- #361 CS-5: copilot token refresh has no timeout
- #360 CS-4: docker_mcp unchecked type assertion on singleflight result
- #359 CS-3: permission DB methods ignore caller ctx
- #358 CS-2: csync JSONSchemaAlias copies mutex by value (go vet warning) — investigated, no change made
- #357 CS-1: misplaced doc-comment in cliprovider
- #356 BUG-4: budget-cap invariant — pinned with test+comment, not restructured
- #365 Commit pending review-round fixes (B-1, B-2, BUG-1, BUG-2, BUG-3)

(#356-365 all `completed`; #366 is the only remaining task.)

## Decisions

- Committed one fix per task, sequentially, per the user's explicit
  "между тасками делай коммиты" instruction — never batched multiple
  findings into one commit.
- Delegated non-trivial/research-requiring findings to `/crush`
  (BUG-4, CS-2, CS-3, CS-6, CS-8); did small mechanical ones directly
  (CS-1, CS-4, CS-5, CS-7) to avoid unnecessary round-trip overhead.
- For every `/crush`-delegated diff, performed independent zero-trust
  verification (read the diff myself, re-ran build/vet/tests) before
  committing — never committed based on the sub-agent's summary alone.
- Two findings (CS-2, BUG-4) were correctly closed WITHOUT a code
  change after investigation proved the proposed fix was either unsafe
  (CS-2) or unnecessary (BUG-4, invariant already holds structurally) —
  documented rather than forced.
- Abandoned the `crush sessions watch <id>` background-blocking-wait
  approach after 3 consecutive kills by the harness; switched to
  point-in-time `sessions why`/`sessions last` checks driven by the
  Stop hook's own re-invocation cadence.

## Open questions

- Still pending as of this checkpoint: commit CHANGELOG.md + this
  checkpoint file together (all `.md` changes), then launch the closing
  review agent (`crush run --role reviewer`, this session's `@oh`
  equivalent) scoped to this round's specific commits, zero-trust
  verify its findings, and report a final summary to the user.
- Whether to push this round + the prior round's commits to
  `fork/main` and monitor CI has not been asked/decided — the
  `/babygoal` instruction did not mention pushing.

## Repo state

```
 M CHANGELOG.md
?? dev/
?? docs/checkpoints/2026-08-11-0805.md
```

```
2d7fe257 feat(tools): add SSRF guard to download/fetch tools (CS-8)
83b4c17f fix(cliprovider): unify MCP tool-call and permission IDs (CS-7)
0f9ab301 fix(cliprovider): switch Qwen MCP auth to Authorization header (CS-6)
a90fdd68 fix(config): bound copilot token refresh with a timeout (CS-5)
542a6dfa fix(config): use comma-ok type assertion in docker MCP refresh (CS-4)
```

Note: `docs/checkpoints/2026-08-11-0805.md` (the prior round's
checkpoint) is still untracked/uncommitted — will be committed together
with this file and CHANGELOG.md as the "commit all .md files" step.
`dev/` remains untracked and unrelated to this session's work (present
since before this session started, per the initial git status).
