# Checkpoint — 2026-08-11 23:30 [release-readiness-review-babygoal-round]

## Session summary

This is the closing checkpoint for the `/babygoal` round that implemented
tasks #367-381 from `docs/reviews/2026-08-11-release-readiness-concurrency-and-code-review.md`
(a NO-GO release-readiness review covering core session/mailbox/durable-
queue concurrency), plus two live-reported UI bugs (#383/#384, model
switching incorrectly touching global config instead of per-session
state — completed earlier and covered by the prior checkpoint).

**All 15 review tasks (#367-381) are now implemented and committed.**
Since the prior checkpoint (`2026-08-11-2230.md`), 3 more tasks landed:

- **#379 P2-4** (`98617da3`) — SSRF guard: added the missing IANA
  special-purpose CIDR ranges (CGNAT, benchmark, TEST-NET-1/2/3,
  reserved, IPv6 equivalents) that stdlib `net.IP` methods don't cover.
  The delegated diff and its 42 boundary tests were solid; I personally
  revert-checked it (disabled the new check, confirmed 8 tests failed
  with the predicted symptom, restored, confirmed green) before
  committing. The full `go test ./internal/agent/... -race -count=1`
  confirmation had been manually backgrounded by the user right before
  the prior checkpoint — verified clean by reading the actual log
  content (not the bare exit code) at the start of this segment, then
  committed.
- **#380 P2-5** (`785829c0`) — mailbox finalizer era-boundary reordering:
  `abandonOwnershipWithHandoff` used to call `abandonOwnership` and
  `popAllSubmitted` as two separate lock acquisitions, leaving a window
  where a new owner's queued work could be misrouted into detached
  recovery instead of staying for the new owner's own drain. Added
  `abandonOwnershipAndPopSubmitted`, one atomic critical section closing
  the gap. **The delegated diff's own regression test was vacuous**
  (goroutine B waited for goroutine A to fully finish before racing, so
  it could never have failed regardless of whether the fix was atomic —
  same pattern as several earlier vacuous tests this round). Rewrote it
  as two tests: a deterministic repro of the pre-fix bug using the
  still-existing split primitives directly, and a genuine concurrent
  stress test with no artificial ordering. Personally reverted the
  production fix and ran the full `internal/agent` suite — it stayed
  green, meaning no test exercises the *exact* real call site with real
  interleaving; documented this honestly in the commit (mirrors the
  P1-3 precedent of disclosing an inconclusive revert-check rather than
  manufacturing an artificial one) rather than building disproportionate
  DB-backed integration test infrastructure for a P2 finding.
- **#381 P2-6** (`dfb91e58`) — recovery-subsystem cleanup. The task
  explicitly required auditing which of 7 review bullet points were
  already resolved by earlier tasks before changing anything. Audit
  result: 3 already fixed (startDetachedRun's name/contract, Stop()'s
  graceful shutdown, a stale "durably enqueue before returning" comment
  — all verified by reading the current code, not assumed from the
  task description's prediction); 2 genuinely still open and fixed here
  (unused `RunQueuePumpConfig.DataDirectory` field removed, including
  two test call sites `go vet` caught after my first pass missed them;
  the "executed entry successfully" info log was printing
  unconditionally even after a failed `AckRunQueueEntry` — now
  conditioned on the ack actually succeeding); 2 deferred as legitimate
  future-scoped findings rather than force-fit into a mechanical
  cleanup pass (the file's line-count growth is honest complexity from
  this round's own concurrency fixes, not accidental; the dead
  `terminal_failure` column would need a schema migration on the
  exactly-once durable queue this entire round has been protecting,
  judged disproportionate here).

**Currently in progress: task #382 (final wrap-up)**, this checkpoint is
step 1 of that task's 6 steps. Remaining steps: update CHANGELOG.md,
commit all `.md` files (including the never-yet-committed review report
and both checkpoints) in one docs-only commit, launch a closing `@oh`
review of the entire round's work (Agent tool, subagent_type "oh"),
zero-trust verify its findings, then report a final summary to the user.

Every commit this round followed the same zero-trust discipline: every
delegated `/crush` diff was independently re-read, its tests scrutinized
for vacuousness, and (where the fix was safety/concurrency-critical) a
genuine revert-check performed personally. This caught real, non-trivial
bugs in the majority of delegated diffs (P0-3's unsynchronized
Add-vs-Wait race, P0-4/P1-1's vacuous shutdown-join tests, P1-5's
clobber-prevention regression, P2-2's dead code, P2-3's fully vacuous
test, P2-5's vacuous stress test) plus a genuine process failure of my
own (trusting misleading backgrounded exit codes let two commits ship
with `go test ./internal/agent/...` build-failing, caught and disclosed
mid-round, commit `639863f2`).

## Active goal

A session-scoped Stop hook is armed with condition: "реализуй задачи с
помощью /crush, между тасками делай коммиты. После завершения всей работы
сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента
@oh - пусть проверит всю работу" — auto-clears once satisfied. Babysit cron
(`eab417a2`, every 15 min) still armed, responding correctly to ticks.

## TaskList

### in_progress
- #382 Wrap-up: checkpoint (this file, step 1 done), changelog (next),
  commit docs, launch and zero-trust-verify closing @oh review, report
  to user — blockedBy: none (all #367-381 committed)

### pending
- (none — this is the last task in the chain)

### recently completed (most recent 10)
- #381 P2-6 (`dfb91e58`), #380 P2-5 (`785829c0`), #379 P2-4 (`98617da3`),
  #378 P2-3 (`6549b9b2`), build-fix (`639863f2`), #377 P2-2 (`50303b88`),
  #375 P1-5 (`948bc2df`), #373 P1-3 (`ea591976`), #374 P1-4 (`c46276a6`),
  #372 P1-2 (`56316d3a`)
- (also completed earlier: #371 P1-1, #370 P0-4, #369 P0-3, #376 P2-1,
  #368 P0-2, #367 P0-1, #383 UI-BUG-1, #384 UI-BUG-2)

## Decisions

- P2-5's stress test rewrite: chose to prove the pre-fix bug
  deterministically via the still-existing split primitives (no
  goroutines needed) rather than trying to force a race into the new
  atomic method (which is architecturally impossible by construction —
  the whole point of the fix). The atomic method's own stress test is
  honestly framed as a sanity check against accidental future early-
  unlock, not a proof.
- P2-5's residual gap (no test exercises the real `agent.go` call site
  with genuine concurrent interleaving) was disclosed in the commit
  rather than papered over — building DB-backed integration harness to
  force that exact interleaving was judged disproportionate for a
  one-line, easily-inspected call-site swap on a P2 finding.
- P2-6: honored the task's explicit "audit first, don't rewrite"
  instruction literally — verified via direct code reading (not
  trusting the task description's own predictions) which of the 7
  findings were already closed, fixed only the 2 that were genuinely
  still open and small/safe, deferred 2 as clearly-scoped future work
  rather than attempting a schema migration or file-level rewrite in
  this pass.
- P2-6's log-conditioning fix: no dedicated regression test added
  (would need slog-capture + DB-manipulation infrastructure for a
  purely cosmetic observability fix with zero state/behavior impact) —
  relied on code review + full suite staying green instead, disclosed
  explicitly in the commit rather than silently skipped.
- Continued the standing rule from earlier in the round: never trust a
  backgrounded test task's self-reported exit code alone; always read
  the actual log file content, or run critical verification
  synchronously. Applied when resuming after the user manually
  backgrounded the P2-4 confirmation run.

## Open questions

- None new. Whether to push + monitor CI for this round remains
  undecided/unrequested, unchanged from every prior checkpoint.

## Repo state

```
?? dev/
?? docs/checkpoints/2026-08-11-1530.md
?? docs/checkpoints/2026-08-11-2230.md
?? docs/reviews/2026-08-11-release-readiness-concurrency-and-code-review.md
```
(plus this file, `2026-08-11-2330.md`, itself untracked — all four `.md`
paths above are queued for a single docs-only commit in step 3 of #382.)

```
dfb91e58 fix(session,app): close 2 of 7 P2-6 findings, audit confirms the rest (P2-6)
785829c0 fix(agent): close era-boundary reordering gap in mailbox handoff (P2-5)
98617da3 fix(tools): close SSRF guard gap in IANA special-purpose ranges (P2-4)
6549b9b2 fix(cliprovider): move Codex MCP token out of argv into an env var (P2-3)
639863f2 fix(agent): update two more WithClearHolderMetadataFn call sites for P1-5's new signature
50303b88 fix(agentguard): close env -S recursion bypass, unify wrapper stripping (P2-2)
948bc2df fix(session,cmd): generation-versioned lock metadata prevents clobber, fix stale docs (P1-5)
ea591976 fix(db,session): add rowid tie-breaker for durable-queue FIFO ordering (P1-3)
c46276a6 fix(session): bound RunQueuePump's concurrent execution fan-out (P1-4)
56316d3a fix(session): cancel execCtx on lease loss, skip post-loss outcome writes (P1-2)
```
