# Checkpoint — 2026-07-06 08:17

## Session summary

Continuation of the `PHPCraftdream/crush` fork session. The bulk of the
work (reviewing + hardening contributor PRs #2 and #3, and shipping npm
release 0.1.3) is captured in the prior checkpoint
`2026-07-05-2039.md` — read that first for the full arc. This checkpoint
only adds what happened after it: an npm-cleanup thread and a deferred
feature proposal.

State of the release: **0.1.3 is live on npm** under `@phpcraftdream`
(meta + 5 platform packages), CI green on main, tag `npm-v0.1.3`
pushed, publish workflow verified `v0.1.3`. Both PR #2 (`22e672a1`) and
PR #3 (`753668b5`) merged.

Two things resolved/parked since the last checkpoint:

1. **npm old-version cleanup (parked, operator-side).** The operator
   wanted 0.1.0/0.1.1/0.1.2 removed from npm to avoid clutter. They ran
   the `npm unpublish --force` loop (18 = 3 versions × 6 packages) and
   npm's CLI printed success lines, BUT the registry kept serving the
   old versions (tarball crush-0.1.0.tgz → HTTP 200, packument still
   lists 0.1.0–0.1.3 6+ min later). The operator reported npm refuses to
   fully unpublish some "old" versions. I proposed `npm deprecate`
   (works on any version, no 72h limit, marks with a warning so nobody
   installs, `latest` stays 0.1.3) and gave the deprecate loop. UNCLEAR
   whether the operator ran deprecate — no confirmation. Reminder: I
   cannot run npm mutations (no npm auth here — `npm whoami` → ENEEDAUTH;
   publishing only via NPM_TOKEN in CI). All npm unpublish/deprecate is
   operator-run.

2. **Auto-install /crush into ~/.claude on npm install (DEFERRED).**
   The operator floated: when installing via npm/npx, auto-register the
   `/crush` slash-command into `.claude` if Claude Code is present. I
   investigated (did NOT implement anything): `crush claude-init
   --global` writes `~/.claude/commands/crush.md` idempotently; the meta
   package has no `scripts` block today and its description advertises
   "no download step at install time". I laid out three approaches
   (A: npm postinstall / B: auto-init on first `crush` run / C: just
   print a hint) with tradeoffs, and started an AskUserQuestion — but
   the operator INTERRUPTED and said "отложим. Не понятно хорошо это или
   нет" (defer it — unclear if it's a good idea). So this is explicitly
   PARKED, unresolved, no code written. The concern that killed it:
   auto-writing to ~/.claude on install/run is a real side effect many
   users won't expect from a package install. If revisited, my
   recommendation was B (first-run auto-init, skips CI/Docker builds,
   immune to --ignore-scripts) over A (postinstall).

Nothing is in flight right now — the last action was reverting the
AskUserQuestion (no changes made) and confirming nothing was touched.
Working tree is clean except the standing intentional `web/dist/.gitkeep`
deletion (never commit it) and untracked checkpoint files. No /goal or
/babysit active. TaskList empty.

## Active goal
none

## TaskList
empty — TaskList returns no tasks; whole session ran single-context,
no TaskCreate/TaskUpdate used.

## Decisions
- **Defer the npm-install → ~/.claude auto-init feature.** Operator
  judged it unclear whether auto-writing to ~/.claude on install is
  desirable; parked with no code. If resumed, approach B (first-run
  auto-init) recommended over A (postinstall) — B avoids firing in
  CI/Docker builds and survives `npm ci --ignore-scripts`.
- **Use `npm deprecate`, not unpublish, for the stuck old versions.**
  npm refused to unpublish 0.1.0–0.1.2 despite CLI success output;
  deprecate is the policy-clean way to hide them without deletion.
- (Carried from prior checkpoint) npm tag scheme is `npm-vX.Y.Z` to
  avoid colliding with upstream's inherited `vX.Y.Z` tags; the Go
  binary's embedded version stays clean `vX.Y.Z`.

## Open questions
- Did the operator run `npm deprecate` on 0.1.0/0.1.1/0.1.2? Unconfirmed.
  If clutter still bothers them and deprecate wasn't run, re-offer the
  loop; otherwise consider the cleanup closed as "deprecated, not
  deleted".
- The auto-init-into-.claude feature: revisit only if the operator
  decides it's worth it. Decision inputs already gathered (see summary
  item 2). No implementation exists.
- Two cosmetic nits in PR #3 (config.go doc-comment column alignment;
  glob's dead-stored `value` field) remain un-fixed — optional, operator
  never asked to fix.

## Repo state
```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-06-28-2158.md
?? docs/checkpoints/2026-07-03-1513.md
?? docs/checkpoints/2026-07-04-2234.md
?? docs/checkpoints/2026-07-05-2039.md
```
```
0328e33a chore(npm): bump to 0.1.3, changelog for restricted-run permissions
753668b5 Merge pull request #3 from ayleen/feat/run-bash-allowlist
0e67b16c refactor(shell): extract shared IsCompoundCommand; close bash safe-path chaining bypass
4bd66ebb fix(schema): escape commas in run-permission descriptions; refresh allow_bash text
aa44b63a fix(review): address run-allowlist review — interactive leak, chaining bypass, cross-platform glob
```
