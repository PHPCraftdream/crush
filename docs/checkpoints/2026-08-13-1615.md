# Checkpoint — 2026-08-13 16:15 [oh-review-findings-fixed-and-committed]

## Session summary

Continues directly from `2026-08-13-1529.md`. That checkpoint left the
H2/H3/H4/M6/M7 fixes (responses to `@oh`'s second, NO-GO review) sitting as
uncommitted working-tree changes, plus an in-flight 30-iteration
verification run of the M7 lease-watchdog test margin (reverted to the
original `ttl+5s` after tighter margins of 2s/3s both empirically flaked
under `-race`).

That verification run completed clean (0/30 failures). Before committing,
ran the full `internal/agent`, `internal/session`, `internal/config`,
`internal/app` suite once more under `-race` as a final gate and it
surfaced three failures — each investigated individually rather than
assumed innocent or guilty:

1. **`TestRunInternal_PinsResolvedOverridesOntoTheCall`** — a REAL gap,
   not flakiness: the test hand-builds a `pinned *resolvedOverrides`
   without populating `.providerCfg`, which used to pass only because
   `runInternal`'s pre-H2 live re-read of `c.cfg.Config().Providers.Get(...)`
   papered over the gap. Both real producers of `resolvedOverrides`
   (`resolveSessionModels`, `applyModelOverrides`) always populate
   `providerCfg` alongside `large`/`small` in production, so the test now
   does too. Fixed in `internal/agent/turn_config_test.go`; verified 5/5
   clean afterward.
2. **`TestOrphanOutbox_ConcurrentExecution`** — investigated the failure
   log closely: the orphan entry WAS drained successfully into the main
   queue (my M6 fix didn't block that), but the drain happened late enough
   in the test's fixed 500ms window that the one subsequent lease attempt
   on it raced against `pump.Stop()` and got cancelled. Verified 15/15
   clean in isolation — pre-existing timing sensitivity under heavy
   parallel `-race` load, not a regression from M6.
3. **`TestReleaseGate_P350_QueuedNotExecutedBacksOffWithoutAttemptPenalty`**
   and, on a SECOND full-gate re-run after fixing #1,
   **`TestRun_CancelDuringSecondTurnPreamble_ActuallyCancels`** — both in
   files untouched by any fix in this round. Verified 10/10 and 15/15
   clean in isolation respectively. Same environmental-flakiness class
   already documented multiple times earlier in this session
   (`TestReleaseGate_P0_2_GenuineFailureStillExhaustsAfterMaxAttempts` from
   the B1 investigation is a sibling of this class, not a coincidence —
   the whole combined-package `-race` run puts heavy simultaneous CPU
   contention on every timing-sensitive test in these packages at once).

With all three explained (one real fix, two confirmed pre-existing
flakiness), committed the work in two commits:

- `b0c35f58` — H2/H3/H4/M6/M7 fixes + the `turn_config_test.go` correction
  + `CHANGELOG.md` update, all together (they're one logical response to
  one review).
- `caf2c7b1` — the README.md rewrite (task #441), committed separately
  since it's unrelated subject matter to the review-response fixes.

Ran the full `.githooks/pre-push` gate after both commits (build, lint,
`go test -short -failfast ./...` across the entire module) — clean on the
first attempt, 0 lint issues.

`web/dist/.gitkeep`'s pre-existing deletion and the untracked `dev/`
directory were both deliberately left alone throughout, per the fork's
own CLAUDE.md instructions.

## Active goal

None (no `/goal` Stop hook armed). The `/babygoal`-established closing
sequence (checkpoint + changelog + commit md + `@oh` review) is being
followed for this round too. Task #435 (`@oh` review) is about to be
re-engaged for a third-round check now that the second round's findings
are committed — but see the B1 disagreement below, which should be
surfaced to the user rather than silently looped on indefinitely.

## TaskList

### in_progress
- #435 `@oh` review of this round's work — second review's actionable
  findings (H2/H3/H4/M6/M7) are now fixed, verified, and committed; B1
  remains an open, unreproduced disagreement (see Open questions)

### pending
- #440 P0-4 follow-up: orphan outbox attempts/exhaustion tracking (M5/M8)
  — non-blocking, unchanged this checkpoint
- **#421** [ВАЖНО, ОТЛОЖЕНО] P0-1 regression — unchanged, still awaiting a
  user architectural decision

### recently completed
- #424-#439 (the full round `@oh` reviewed)
- #441 README.md rewrite — now committed (`caf2c7b1`)

## Decisions

- Investigated every one of the three failures in the final `-race` gate
  run individually before either fixing or dismissing it — one was a real
  test gap (fixed), two were reproduced as pre-existing flakiness via
  isolated repeated runs (10-15/10-15 clean each), consistent with this
  whole session's zero-trust verification discipline.
- Split the commit into review-fixes-and-changelog vs. README, since they
  are unrelated changes bundled only by session timing, not by subject.
- Did not silently accept `providerCfg`'s absence in the H2 unit test as
  "test noise to loosen" — traced it to the exact mechanism (H2's fix
  removing the live-read fallback that used to mask the test's own gap)
  and fixed the test to match what production code actually produces.

## Open questions

- **B1 disagreement (carried over, still unresolved)**: `@oh` declared
  `TestP1_4_BoundedWorkerPoolRespectsLimit` still flakes ~10% with "no
  such table" errors. 85+ isolated reproduction attempts across this
  session did not reproduce that specific failure on that specific test.
  This needs the user's awareness before task #435 can be considered
  fully closed — it should not be resolved unilaterally either direction.
- **#421** — unchanged, still open, still awaiting a user architectural
  decision.
- Whether to launch a THIRD `@oh` review now (per the standing `/babygoal`
  closing-sequence instruction) risks an unbounded review-fix-review loop
  given B1 is already an unreproduced disagreement from round two — worth
  flagging to the user rather than mechanically repeating the sequence
  indefinitely.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
?? docs/checkpoints/2026-08-13-1529.md
?? docs/checkpoints/2026-08-13-1615.md
```

```
caf2c7b1 docs(readme): reposition as agent-driven CLI, point install/links at fork (task #441)
b0c35f58 fix(agent,session): close H2/H3/H4/M6/M7 from second @oh review (task #435)
f77c0195 docs: commit second static-audit review, checkpoint, and CHANGELOG for this round
b10f3377 fix(session): use QueryRowContext in P0-3 watchdog test (task #425 follow-up)
d4b9bcb5 fix(app): add DrainOrphanOutboxEntry to mockSessionService (task #426 follow-up)
```

Full `.githooks/pre-push` gate (build + lint + `go test -short -failfast
./...`) passed clean on the first attempt after both commits above.
