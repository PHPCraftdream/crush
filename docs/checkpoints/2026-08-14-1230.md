# Checkpoint — 2026-08-14 12:30 [ox-test-speed-round-plus-urgent-torn-generation-regression]

## Session summary

Continues directly from `2026-08-13-2100.md`, which closed out the `@oh`
second-review round (task #435) with B1 resolved (not reproduced on real
CI) and a handful of CI-only flake fixes shipped. This session picked up
from there with several distinct, user-directed threads, roughly in
order:

1. **GLM-5.3 atom** (#448): user asked to add the newly-released GLM-5.3
   model to the z.ai provider atom registry. docs.z.ai doesn't document
   it yet, so it was added by explicit analogy with GLM-5.2 (1M ctx,
   3-state reasoning), clearly marked UNVERIFIED in comments, with a
   pointer at the new `crush ping --model` flag as the way to confirm
   the real numbers later.
2. **`crush ping --model`** (#449): motivated directly by #448 — the only
   existing way to test-probe a model was `crush models use`, which (a)
   persists to config and (b) rejects any model id not already in the
   provider's known catwalk catalog, making it useless for testing a
   genuinely new model. Added `--model <atom-or-provider/model[@effort]>`
   to `crush ping`, resolved ad-hoc via a new `resolvePingModel` that
   validates only the provider prefix (not the model catalog), writes
   nothing to config.
3. **`@ox` test-speed investigation** (#450, explicit user request:
   "запусти @ox агента исследовать как нам ускорить тестовые прогоны").
   Delegated as a real Agent call (not isolated worktree — same shared
   working directory), which the orchestrator had to account for when
   staging commits (unrelated in-flight ox edits appeared in `git status`
   mid-session and were correctly excluded from #448/#449's commits).
   ox's findings, independently re-verified before committing:
   `cliprovider.detectAvailable()`'s uncached PATH walk (~360ms/call,
   called on every config reload), a real `npm install` executed by
   `internal/shell`'s TestCommandBlocking to prove a negative, and three
   fully-serial `internal/session` heartbeat tests — fixed, net **3m11s
   → 1m51s (-42%)** on `go test -short -count=1 ./...`. ox also filed 5
   follow-up recommendations rather than doing them itself (correctly
   judging them as needing more design care): #452-#456, all now closed
   too — `worktreeRoot` memoization (another ~21% off `internal/cmd`),
   an exported `WithHeartbeatInterval`/`TitleJoinGrace`/`CancelAllGrace`
   test-seam trio (extending the SAME per-instance-option pattern already
   established by `WithClearHolderMetadataFn`, deliberately NOT a global
   var, to avoid any TestMain-coordination requirement across parallel
   tests), a pre-push hook change to retry only the failed package
   instead of the whole suite (empirically validated live — a real flake
   mid-session retried in 46.75s instead of the full suite), and adding
   real `CRUSH_GLOBAL_DATA`/`CRUSH_GLOBAL_CONFIG` isolation to
   `internal/agent`'s `TestMain` (a genuine security-relevant gap: 13
   files in that package call `config.Init` directly, and without this,
   any of them could read the operator's real
   `~/.config/crush/crush.json`).
4. **A near-miss git-stash accident**, caught and cleanly recovered: ran
   a background `git stash && go test ... ; git stash pop` to compare
   before/after task #453's heartbeat changes, but manually ran a SECOND
   `git stash pop` myself while the background one was still mid-flight
   — the background command's own later `git stash pop` then popped a
   completely unrelated, pre-existing OLD stash (`#272 partial WIP from
   crush run p1-3-queue`, left over from a much earlier, unrelated
   session) into the working tree, producing a merge conflict in
   `internal/cmd/queue.go`/`internal/queue/queue.go` — files this session
   never touched. Diagnosed via `git stash list` (confirmed the old stash
   was still present, not dropped) and resolved with `git checkout HEAD
   -- <files>` (discarding the accidentally-popped content, leaving the
   old stash entry itself untouched in the stash list for whoever it
   actually belongs to). **Lesson recorded: never run manual git stash
   operations while a background command might also be mid-flight on the
   same repo.**
5. **`@ox`'s task #457 finding — a genuine bug, not a flake**: while
   monitoring CI after #456's push, `TestConcurrentReloadAndReads_
   NeverObservesTornGeneration` (internal/config) failed on ubuntu-latest
   with 88 torn-generation observations. Root-caused (not guessed): the
   test's readers called `Config()`, `Resolver()`, `KnownProviders()` as
   THREE SEPARATE `store.loadSnapshot()` calls — individually atomic, but
   with a real (if narrow) window for a concurrent publish to land
   between any two of them. Confirmed via a full grep that NO production
   caller reads them this way. Fixed the TEST (not production code) to
   read via a single `store.loadSnapshot()` call instead, matching what
   the atomic-publish design actually guarantees. Verified 30/30 clean
   locally AND confirmed green on the immediate next CI push (commit
   `a2f74c80`). **This diagnosis has since been shown WRONG or at least
   INCOMPLETE — see Open questions below, this is the most urgent open
   thread right now.**
6. **CI monitoring continued past #457**: pushed #452-#456 across
   several more commits, watching CI after every single one (per explicit
   "проконтролируй ci" instruction) — windows-latest kept failing on
   distinct, unrelated tests each time (now 6+ across the whole session:
   TestP0-338, ownership-handoff, P0_2/P350 margin family,
   TestOrphanOutbox_ConcurrentExecution, and now
   TestReleaseGate_P350_LeaseRenewedDuringLongExecution — task #447,
   fixed by widening TestLeaseTTL 1s→3s after tracing the actual
   renewal-interval mechanism, same discipline as #444/#445). After
   deciding (twice) to defer chasing every individual windows-latest
   flake and file #447 as a standing tracking task instead, the user's
   own explicit direction was **"отложим. Сначала решим все таски"**
   (defer it, resolve everything else first) — which is why #447 was
   picked up again, in order, once #452-#456 were all done.
7. **RIGHT BEFORE this checkpoint was requested**: the very next CI push
   after #447's TestLeaseTTL fix (commit `c293067d`) failed AGAIN — but
   this time on **ubuntu-latest**, not windows-latest, and on
   `TestConcurrentReloadAndReads_NeverObservesTornGeneration` AGAIN —
   the SAME test task #457 believed it had fixed, now with **147** torn
   observations (up from the original 88, not down). Confirmed the #457
   fix (`loadSnapshot()` single-call read) is still genuinely present in
   the file, not accidentally reverted — meaning the diagnosis that
   "torn observations are purely a cross-call read-timing artifact, not
   a real store bug" must have been wrong or incomplete: if readers now
   get one atomic snapshot per read, a STILL-torn observation implies the
   PUBLISHED snapshot itself can be internally inconsistent at
   construction/publish time, not just across separate reads. Filed as
   task #458, marked urgent, and this checkpoint was written immediately
   after rather than continuing to dig deeper solo.

## Active goal

None (no `/goal` Stop hook armed this session).

## TaskList

### in_progress
- #447 Investigate systemic windows-latest CI flakiness under -race
  (multiple packages) — broadened scope, 6+ distinct unrelated tests
  found across the session; most recent instance (lease-TTL) fixed and
  pushed, but the task itself stays open as a standing tracker per its
  own description, not meant to close on any single fix

### pending
- **#458 [СРОЧНО]** Re-investigate `TestConcurrentReloadAndReads_
  NeverObservesTornGeneration` — task #457's fix (verified 30/30 local +
  1 clean CI push) has now been proven insufficient by a SECOND CI
  failure with a HIGHER torn-count (147 vs 88), with the fix still
  genuinely present in the file. The real bug is very likely inside
  `ConfigStore.buildAndPublishReload`/`publishLocked` (internal/config/
  store.go) — a published snapshot's own fields disagreeing with each
  other, not a reader-side timing artifact. **This is the single most
  urgent open item** — a live, real, apparently-common data-consistency
  bug in the config store, which the entire app depends on.
- **#421** [ВАЖНО, ОТЛОЖЕНО] P0-1 regression — unchanged all session,
  still awaiting a user architectural decision, untouched since before
  this session even started

### recently completed (this session)
- #448 Add GLM-5.3 atom to z.ai provider (explicitly unverified pending
  real-world ping confirmation)
- #449 `crush ping --model` flag for ad-hoc, config-free model probing
- #450 `@ox` test-speed investigation — 3m11s → 1m51s (-42%)
- #451-#457 CI monitoring + the 5 follow-up perf/isolation tasks ox
  recommended, all closed with independent re-verification
- #440 orphan outbox dead-code cleanup (carried over from before this
  checkpoint's session start, closed early in this session)

## Decisions

- Chose to add GLM-5.3 as an atom NOW, unverified, by explicit user
  instruction ("добавим 5.3 по аналогии с 5.2"), rather than waiting for
  docs.z.ai to catch up — clearly marked as provisional in code comments
  and README, with a concrete path (the new `--model` flag) to confirm
  later.
- Chose to fix a hardcoded `atomKey == "glm5_2"` check in
  models_efforts.go (found while adding glm5_3) rather than leave it —
  it would have silently shown the wrong doc message for the new atom;
  small, clearly-scoped, in-line fix rather than a separate task.
- Chose to design `crush ping --model`'s raw-form resolution to skip
  catalog-membership validation entirely (unlike `crush models use`'s
  `app.ResolveModel`) — a deliberate, explained divergence from the
  existing pattern, since the whole point of the flag is testing models
  the catalog doesn't know about yet.
- Delegated the test-speed investigation to `@ox` per explicit user
  request, then independently re-verified every change before committing
  (build + -race + full package runs) rather than trusting the agent's
  own report — same zero-trust discipline as every other delegation this
  entire multi-day session.
- For the accidental git-stash conflict: resolved by restoring the two
  affected files to clean HEAD state via `git checkout HEAD --`, explicitly
  NOT touching (dropping/popping) the old, unrelated stash entry that
  caused it — it belongs to a different, earlier session's work and isn't
  mine to resolve or discard.
- For task #457 (now superseded): initially concluded the torn-generation
  test was exercising a stronger cross-call invariant than the store
  actually promises, and fixed the TEST rather than the store. This
  conclusion is now known to be at least incomplete — logged explicitly
  as an open question below rather than quietly amended, consistent with
  this session's standing zero-trust/honest-disclosure discipline (same
  as how the B1 disagreement was handled in the previous checkpoint).
- Continued honoring the user's explicit "проконтролируй ci" instruction
  by watching every single push's CI result through this whole round,
  not just the first one — this is what surfaced both the #447 lease-TTL
  bug and the #458 regression.

## Open questions

- **#458, urgent**: what is the ACTUAL root cause of
  `TestConcurrentReloadAndReads_NeverObservesTornGeneration`'s failures?
  Task #457's fix reduced but evidently did not eliminate it (147 torn
  observations on the very next reproduction after a "confirmed clean"
  CI push). Needs a full re-read of `ConfigStore.buildAndPublishReload`/
  `publishLocked`/`loadSnapshot` (internal/config/store.go) with the
  hypothesis that the PUBLISHED snapshot itself can be internally
  inconsistent, not just torn across reads — this is now suspected to be
  a real, live correctness bug in the config store, not test flakiness.
- **#421** — unchanged, still open, still needs the user's own
  architectural call.
- **#447** — standing tracker, not expected to fully close; is
  windows-latest's persistent unreliability a transient GH Actions
  capacity issue or does this repo's test suite have a genuine
  structural sensitivity to slow/contended runners? Not yet answered
  definitively despite ~7 distinct data points this session.
- Whether commit `c293067d` (task #447's lease-TTL fix) itself needs
  re-verification once #458 is resolved — it was pushed and is
  presumably fine on its own merits (the failure on that push was in a
  DIFFERENT file/test), but worth a clean CI confirmation once #458 no
  longer makes every run noisy.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
```

```
c293067d fix(session): widen TestLeaseTTL 1s->3s in lease-renewal test (task #447)
02334bad test(agent): isolate TestMain from real host global config (task #456)
00f8d39e ci(hooks): scope pre-push retry to only the failed package (task #455)
ed2b59e4 perf(agent): add titleJoinGrace/CancelAllGrace test seams, parallelize (task #454)
e0df6f16 perf(session,agent): add SessionLock heartbeat-interval test seam (task #453)
```

All commits above (plus 9c71a227, a2f74c80, 4e9e46c1, 979b74f6, 469abe1d
— the full run from this session) are pushed to `fork/main`
(`https://github.com/PHPCraftdream/crush`). Latest push's CI
(`31792081571`) is RED on both ubuntu-latest (task #458's torn-generation
regression) and, per the cascading matrix `fail-fast`, macos-latest was
cancelled as a result — not an independent failure. windows-latest for
that same run was also cancelled (not independently evaluated). The
immediately PRIOR push (`31790768115`, commit `02334bad`) had
ubuntu-latest and macos-latest green, with windows-latest failing on the
lease-TTL issue that `c293067d` fixed — so as of this checkpoint, no
single CI run has yet gone fully green since `#457`'s original fix
started showing cracks.
