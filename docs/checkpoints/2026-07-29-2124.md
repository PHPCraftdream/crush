# Checkpoint — 2026-07-29 21:24

## Session summary

Продолжение от чекпоинта `2026-07-29-2018.md` (все 11 задач ревью #127-137 реализованы+закоммичены, первый раунд ревью `@oh` прошёл, найденные им 2 серьёзные проблемы — data race в `internal/log/http.go` и gofumpt-фейл в `internal/shell/dispatch_windows_test.go` — исправлены и закоммичены в `82c51ca4`, вместе с попутно найденной утечкой temp-файлов в deploy). После этого пользователь попросил "все фейлки чини на месте" — имелись в виду 2 флейка, замеченных при финальном полном прогоне тестов перед закрытием batch'а, но тогда списанных на "окружение, не воспроизводится в изоляции" без реального фикса.

**Диагностика и фикс флейка #1 (`TestBackgroundShellManager_Kill_TreeKillsOrphanedGrandchild_Windows`, internal/shell/exec_windows_test.go)** — простой: порог `require.Less(t, elapsed, 10*time.Second, ...)` расширен до `30*time.Second` — при полном прогоне под нагрузкой сбой был на 10.85с (сам tree-kill РАБОТАЕТ, просто чуть медленнее обычных <2с под нагрузкой; реальная регрессия дала бы полные 60с, так что 30с — комфортный запас без потери чувствительности теста). Закоммичено в диффе, ЕЩЁ НЕ закоммичено отдельным коммитом на момент этого чекпоинта (лежит в рабочем дереве).

**Диагностика и фикс флейка #2 (`TestModelsBump_*`, internal/cmd) — потребовал двух итераций:**
- Первая попытка (ОШИБОЧНАЯ, потом откачена): добавил `t.Cleanup(func(){ db.Release(filepath.Dir(globalPath)) })` в `setupBumpEnv` (internal/cmd/models_bump_test.go), решив что причина — незакрытое SQLite-соединение после финального `modelsStateCmd`-вызова в некоторых тестах. Это ПОМОГЛО (TestModelsBump_TopBoundary_UpIsInformationalNotError стал стабильно зелёным на 25 прогонах), но НЕ было настоящей причиной — оказалось избыточным дублированием уже существующей, гораздо более тщательной защиты в `isolatedModelsEnv` (internal/cmd/models_use_test.go) — `waitForSQLiteHandleRelease(t, tmp)`, активный retry-цикл `os.RemoveAll` с бюджетом 30с, специально настроенным под этот же файл предыдущей сессией (комментарий прямо называет `TestModelsBump_GLM52FullStepUp`).
- Стресс-тест (15-25 прогонов) всё равно поймал `TestModelsBump_GLM52FullStepUp` падающим ОДИН раз с той же ошибкой `TempDir RemoveAll cleanup: unlinkat ...\002: The process cannot access the file`. **Ключевая улика**: суффикс пути `\002` — это ВТОРОЙ вызов `t.TempDir()` внутри `isolatedModelsEnv` (переменная `workDir`, используется как cwd теста через `os.Chdir`), а НЕ первый (`tmp`, где живёт SQLite и куда целился весь существующий `waitForSQLiteHandleRelease`-механизм). То есть существующий фикс защищал не тот каталог для этого класса сбоя — реальная блокировка происходит на файле ВНУТРИ `workDir`, не в `tmp`.
- **Настоящий фикс**: откатил ошибочное добавление в `models_bump_test.go` (оно осталось безвредным, но было решением не той проблемы). В `internal/cmd/models_use_test.go`, в тот же cleanup-closure `isolatedModelsEnv`, добавлен второй вызов `waitForSQLiteHandleRelease(t, workDir)` СРАЗУ после существующего `waitForSQLiteHandleRelease(t, tmp)` — механизм уже общий (несмотря на "SQLite" в названии, он просто ретраит `os.RemoveAll` на любом пути), просто раньше вызывался только для `tmp`.
- **НЕ ДОВЕРЕНО**: `go build`/`go vet` прошли, но финальный стресс-тест (`go test ./internal/cmd/... -run TestModelsBump -count=15 -v`, запущен как `bfbytq3zr`) был ЗАПУЩЕН, но пользователь прервал сессию (`/checkpoint`) ДО того как я успел проверить его результат. **Это первое, что нужно сделать при возобновлении** — прочитать вывод `bfbytq3zr`, и если всё ещё падает — разобраться дальше (возможно есть третий локус, или 15-25 прогонов всё ещё недостаточно чтобы поймать редкий флейк).

**Активная цель `/babygoal`** (установлена явно пользователем в этой части сессии, заменяет более раннее автоматическое завершение партии #127-141): *"почини все флейки, доделай все задачи - используй агентов @sh, потом запусти ревью агента @oh, и так же реши все правки по его замечаниям"*. Декомпозирована в задачи #145-#151 (см. TaskList ниже) — партия caveat'ов из первого ревью (бывшая единая задача #144) разбита на 4 отдельные задачи по подсистемам (#146-#149), CLI sessions fork (#143) оставлена как есть но добавлена в цепочку зависимостей, плюс второй раунд ревью `@oh` (#150) и применение его находок (#151). Стратегия: последовательные `Agent(subagent_type: "sh")`-вызовы (НЕ `/crush`, как в предыдущей арке этой сессии — пользователь явно попросил "используй агентов @sh", что указывает на прямой Agent-тул, а не на внешний `crush run`). Ни один суб-агент этой новой партии ЕЩЁ НЕ запущен — я застрял на верификации флейков (задача #145) перед переходом к #146.

`/babysit` (`d700ee11`, каждые 15 мин) активен и продолжает тикать (был установлен раньше в сессии, переиспользован — НЕ переустанавливался повторно, `/babygoal` корректно нашёл существующий и не стал дублировать).

Также в этой части сессии: пользователь показал вставки из СОВСЕМ ДРУГИХ параллельных сессий (task-номера #448/#449/#894), где `/crush-fallback` вызывался без реального сигнала отказа — это привело к трёхкратному переосмыслению семантики `/crush-fallback` в предыдущей части сессии (уже завершено, задокументировано в чекпоинте `2026-07-29-2018.md`, изменения лежат в рабочем дереве незакоммиченными — `coder.md.tpl`, `claude_crush_fallback_command.md`, `claude_slash_command.md`).

## Active goal

`/babygoal`-цель (НЕ `/goal` Stop-hook — babygoal явно не использует goal-машинерию): "почини все флейки, доделай все задачи - используй агентов @sh, потом запусти ревью агента @oh, и так же реши все правки по его замечаниям". Прогресс отслеживается через TaskList (#145-#151), не через Stop-hook.

## TaskList

### in_progress
- #145 Верифицировать и закоммитить фикс двух флейков (models_bump, exec_windows) — фикс написан для ОБОИХ флейков, ждёт результата стресс-теста `bfbytq3zr` для models_bump-флейка; exec_windows-флейк ещё не стресс-тестирован повторно после расширения порога

### pending
- #146 session/db: caveats из ревью @oh (dead code, NULL-семантика, fork pubsub, tie-breaker)  (blockedBy: #145)
- #147 grep.go: спурьезный partial-match перед возвратом ошибки отмены контекста  (blockedBy: #146)
- #148 config store.go: doc-комментарии, связность, lock cleanup, тестовый паттерн  (blockedBy: #147)
- #149 shell/dispatch.go: wslLauncherPaths должен читать %SystemRoot%, не хардкодить C:\Windows  (blockedBy: #148)
- #143 CLI `crush sessions fork` тоже нетранзакционен (найдено при #136)  (blockedBy: #149)
- #150 Запустить второй раунд ревью @oh по задачам #145-#143 (флейки + caveats + CLI fork)  (blockedBy: #143)
- #151 Применить находки второго раунда ревью @oh (#150)  (blockedBy: #150)
- #142 crush-fallback state (persistent — do not complete): sh — armed, switches on peak-hours or hard-limit — вечный sentinel, никогда не завершается по дизайну

### recently completed
- #141 Запустить агента @oh для ревью всей проделанной работы (#127-137)
- #140 Выполнить /checkpoint после завершения всей работы по задачам #127-141
- #139 Закоммитить все обновлённые markdown-файлы (changelog, чекпоинты, планы)
- #138 Обновить CHANGELOG.md по итогам задач #127-137
- #137 ConfigStore не защищён от двух параллельных процессов crush
- #136 fork сессии нетранзакционен, может оставить частично созданный fork
- #135 grep fallback проверяет отмену контекста только между строками
- #134 различать WSL bash.exe от Git Bash/MSYS при резолвинге shebang-интерпретатора
- #133 HTTP debug-логгер буферизует стриминг целиком даже когда включён только уровень выше debug
- #132 повторный/конкурентный ответ на permission request может заблокировать горутину

(#144 была удалена — разбита на #146-#149)

## Decisions

- Флейки чинить по существу (найти реальный root cause), а не просто помечать "environment flake" — прямое указание пользователя после первой попытки списать их на нагрузку.
- Первая попытка фикса models_bump-флейка (db.Release в setupBumpEnv) была ОШИБОЧНОЙ — целилась в `tmp` (SQLite dir), а реальная проблема (по улике `\002` в пути ошибки) — в `workDir` (cwd теста). Откачена, заменена на распространение уже существующего `waitForSQLiteHandleRelease`-механизma на `workDir` тоже.
- exec_windows-флейк — не функциональная проблема (tree-kill реально работает, укладывается в 2с в изоляции), а слишком тесный порог assertion под нагрузкой полного прогона — расширен 10s→30s с комментарием-обоснованием.
- Партия caveat'ов из первого ревью (#144) разбита на 4 задачи по подсистемам (#146 session/db, #147 grep, #148 config store, #149 shell dispatch) вместо одной большой — для последовательного делегирования через `Agent(subagent_type: "sh")` по одной штуке.
- Стратегия исполнения новой партии — `Agent(subagent_type: "sh")` напрямую (НЕ `/crush`/`crush run`, как раньше в сессии) — по явной формулировке пользователя "используй агентов @sh".

## Open questions

- Результат стресс-теста `bfbytq3zr` (`go test ./internal/cmd/... -run TestModelsBump -count=15 -v`) не проверен — первое действие при возобновлении.
- Если models_bump-флейк всё ещё воспроизводится после фикса `workDir` — нужно расследовать глубже (возможно третий каталог/файл, или редкость требует больше прогонов чтобы поймать).
- Оба фикса флейков (exec_windows расширение порога, models_use.go workDir-фикс) ЕЩЁ НЕ закоммичены — лежат в рабочем дереве, ждут финальной верификации перед коммитом (часть задачи #145).

## Repo state
```
 M internal/agent/templates/coder.md.tpl
 M internal/cmd/claude_crush_fallback_command.md
 M internal/cmd/claude_slash_command.md
 M internal/cmd/models_use_test.go
 M internal/shell/exec_windows_test.go
 D web/dist/.gitkeep
?? docs/checkpoints/2026-07-29-2018.md
```
```
82c51ca4 fix: address findings from independent review of the #127-137 batch
112779e5 docs: add release changelog, checkpoint the concurrency remediation arc
b53cbf3d fix(config): add a cross-process file lock around config read-modify-write
d369bec0 fix(session): make session fork transactional, stop swallowing errors
ace69e8f fix(grep): check context cancellation inside readBoundedLine, not just between lines
```

Примечание: `internal/cmd/models_bump_test.go` НЕ в диффе — ошибочная правка там была написана И откачена в рамках этой же сессии, итоговое состояние файла идентично коммиту `82c51ca4`. `web/dist/.gitkeep` — намеренно нетронутое удаление (CLAUDE.md), не коммитить без явного запроса. Незакоммиченные `coder.md.tpl`/`claude_crush_fallback_command.md`/`claude_slash_command.md` — из предыдущей части сессии (`/crush-fallback` семантика), решение о коммите за пользователем, не трогать без явного запроса.
