# Checkpoint — 2026-08-02 20:59

## Session summary

Closes out the extended `/babygoal` run that started at checkpoint `2026-08-02-1054.md` (task decomposition for the heartbeat/pulse feature) and continued through `2026-08-02-1406.md` (round 1: tasks #213-221 implemented, first `@oh` review launched) and `2026-08-02-1630.md` (round 2: the first review's 10 findings, F1-F10, decomposed into tasks #222-226 and fixed, second `@oh` review launched).

The second `@oh` review came back with a mostly-clean verdict — all 10 first-round findings (F1-F10) confirmed genuinely fixed by round 2's commits — plus 7 NEW findings of its own (NEW-1 through NEW-7), since a fresh adversarial pass on fix commits is exactly how this kind of latent bug surfaces. Two were consequential enough to act on immediately without asking:

- **NEW-2/NEW-3 (MEDIUM)**: `stream_watchdog.go`'s `onFire` callback fired AFTER `cancel()` at all 5 sites, contradicting its own doc comment ("onFire ... AFTER stalled ... BEFORE cancel()") — a pre-existing ordering bug that became consequential specifically because of this session's own `7b48f75a` (task #223): once `onFire` started actually storing a meaningful 3-way cause (used to pick between "Tool timeout"/"Turn timeout"/"Stream stalled"), the race between `cancel()` unblocking the main stream-consuming goroutine and `onFire`'s slow disk-writing goroutine-dump-then-store could silently produce the WRONG message on a genuine hard-cap fire — defeating the very fix just landed. Verified this personally by reading the exact code (confirmed `cancel()` precedes `onFire()` at all 5 sites) before creating task #227 and dispatching it. Fixed in `f30636d8`: reordered to `onFire()` then `cancel()`, matching the doc's always-correct contract; also fixed a second hardcoded model-facing message with the same root-cause bug.
- **NEW-6 (LOW, doc-only)**: `ResolveDataDirectory`'s (task #224) doc comment claimed a "known gap" vs `Load`'s workspace-config-merge step that, on independent verification by reading `Load` itself, turns out not to exist — `Load` can never honor a workspace-scoped `data_directory` override either (its own merge logic restores the pre-merge value unconditionally). Fixed directly in `bebf712e` (trivial, no sub-agent round-trip needed).

The remaining 4 findings (NEW-1: web UI's `OwnedExternal` gating still mtime-only; NEW-4: `sessions inject`'s liveness status still mtime-only; NEW-5: a residual TOCTOU + transient-lock-contention risk in the new `lockHolderProvablyDead` probe; NEW-7: `sessions locks` still hardcodes `cwd` instead of the resolved data directory, plus reads PID without the Windows sidecar fallback) are all MEDIUM/LOW, same root-cause family (mtime-as-liveness-proxy gaps) as F1/F2 but NOT correctness-critical the way those were — reporting-only or narrow-window risks, not data corruption or false session-termination. Given the run had already gone through two full review-fix cycles, asked the user whether to continue autonomously or pause and file tasks; user chose to file tasks and stop. Created #228-231 with full architectural context (exact fix pattern to reuse — `combinedLockLiveness`/`lockHolderProvablyDead`'s existing precedent — and required tests) so they're resumable without re-investigation.

This is genuinely the end of this `/babygoal` run: `CHANGELOG.md` has been kept current throughout (amended in place as each round's findings were fixed, not just appended), every commit passed independent verification (full diff read, rebuild, re-vet, full-package `-race` re-run) before being made, and the two `@oh` reviews this run commissioned were both genuinely adversarial (the second one caught something the first review's own fix had introduced, which is exactly the point of a second pass).

## Active goal

None. The `/babygoal` contract ("implement via @sh with commits between tasks, then /checkpoint + changelog + commit all .md + @oh review") has now been satisfied twice in this run (round 1 → review → round 2 → review), with a deliberate, user-confirmed decision to stop at the second review's remaining low-severity findings rather than open a third round.

## TaskList

### in_progress
(none)

### pending
- #228 Harden web UI's `OwnedExternal` gating against the same mtime-staleness gap (MEDIUM)
- #229 Harden `sessions_inject.go`'s `isSessionLockAlive` against the same gap (LOW)
- #230 Document `lockHolderProvablyDead`'s residual TOCTOU + transient contention (LOW)
- #231 Fix `sessions locks`' cwd-hardcoded data dir + raw PID read (LOW)

### recently completed
- #227 Fix onFire-vs-cancel ordering race (MEDIUM) — commit `f30636d8`
- #222-226 — see `2026-08-02-1630.md` for the full list (heartbeat activity broadening, hard-cap message, sessions-kill data-dir, stdin drain testability, require.* goroutine safety)
- #213-221 — see `2026-08-02-1406.md` for the full first-round list

## Decisions

- Treated the second `@oh` review's two most consequential findings (NEW-2/NEW-3, directly undermining an already-shipped fix from the SAME run) as requiring immediate action, not deferral — independently re-verified both by direct code reading before creating a task or dispatching a sub-agent, consistent with this session's zero-trust discipline applied to review findings, not just to sub-agent output.
- Fixed NEW-6 (a doc-only correction, also independently re-verified by reading `Load`'s workspace-merge logic myself) directly rather than via a sub-agent round-trip — same proportionality judgment used earlier in this session for single-line/doc-only fixes (task #218, the `agentic_fetch_tool.go` `IsSubAgent` fix, etc.).
- After NEW-2/NEW-3/NEW-6 were resolved, explicitly asked the user whether to continue into a third autonomous review-fix round for the remaining 4 MEDIUM/LOW findings or pause and track them as tasks — this was a genuine decision point (not mechanically obvious) given the scope this run had already reached, and the user's own instruction never specified an iterate-until-perfectly-clean contract. User chose to pause; honored that directly rather than continuing on the assumption that "more fixing is always better."

## Open questions

None blocking. Tasks #228-231 are fully scoped and independently actionable whenever picked up — no user decision pending on any of them beyond one explicitly-flagged design question inside #230 (whether `sessions locks`' unconditional probing of stale locks should be gated behind a flag), left for whoever picks up that task to use judgment on or ask about.

## Repo state

```
 D web/dist/.gitkeep
```
```
f30636d8 fix(agent): fix onFire-vs-cancel ordering race that could defeat cause attribution
bebf712e docs(config): correct ResolveDataDirectory's overstated "known gap"
63a7269a docs: changelog + checkpoint for the second @oh review-fix round
16316635 test(agent): stop calling require.* from non-test goroutines in heartbeat tests
eaf22ece fix(cmd): make stdin boundary-drain fix genuinely testable; fix error-note wording
```
