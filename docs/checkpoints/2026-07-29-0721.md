# Checkpoint — 2026-07-29 07:21

## Session summary

This session continued directly from `docs/checkpoints/2026-07-28-0900.md` (Arc 1: Windows cmd-window investigation, closed as unreproducible; Arc 2: first 9 concurrency-review remediation tasks #97-105, all committed). This session's own work was a **third arc**: a fresh AI-generated code review (invoked via `/oh`) of the 11 commits since the previous review. I personally verified every one of its 7 findings by reading the actual code before accepting any of them — all 7 held up, with one (#1, checkpoint lifecycle) turning out worse than described: measured 10.46s of dead time per agent turn via a real `agent.Run()` against a local httptest SSE server (no network/VCR), versus the review's vaguer "up to 5s per step" claim; a two-step probe proved the stall was per-TURN not per-step (~10.3s regardless of step count).

Built a remediation plan (9 tasks, #106-114) and executed it end-to-end via `/babygoal` using the **real system-installed `crush` CLI** (`/crush` skill, confirmed via `which crush` to be the npm-installed binary, never the repo's own dev build) — one task at a time, sequential `crush run --role smart` sessions, each verified zero-trust (full diff read personally, rebuilt, `-race`-tested by me, not trusted from the sub-agent's own report) before committing. All 9 tasks are now `completed` and committed; the babysit cron (`12b8e668`) self-deleted this tick once the TaskList went empty.

**All 9 commits landed:** `8b1ffa97` (checkpoint lifecycle, 10.46s→~0.5-1s per turn), `45c497da` (pubsub partial-vs-terminal delivery split), `1481be78` (persistent transactional sub-agent cost ledger, migration `20260728000001` with a required backfill step), `1b8c6eb5` (grep fallback ctx-awareness + bounded line reads), `a858e5a8` (delegation-transcript SQL pagination + hard byte/message ceilings), `1082777e` (ConfigStore: unified `publishMu` replacing separate `writeMu`/`reloadMu`, plus new `diskWriteMu` for on-disk read-modify-write), `3d6ec130` (ConfigStore: closed remaining mutable-pointer leaks — `Overrides()` now returns by value, `SetProviderRuntimeConfig` closes a TOCTOU window on the `Providers` `*csync.Map` swap-on-reload), `64d9c09e` (background-job buffer real memory release + timer-based scheduling, not just next-Cleanup-call), `5707e40f` (DB: `idx_sessions_parent_session_id` index, batch call-tree query chunking at 500 roots, removed a dead constant). Plus one incidental fix, `37b0acd6`, for a pre-existing (unrelated) broken mock in `internal/app/resolve_session_test.go` missing `GetCallTreeActivity`/`GetCallTreeActivityBatch`, discovered while running the full test suite for #111.

**Two real problems were caught during personal verification, not accepted from the sub-agent's report:**
1. Task #108's first `/crush` run timed out with a migration that added `parent_cost_accounted REAL NOT NULL DEFAULT 0` **without a backfill UPDATE** — would have double-charged parents for every pre-existing session's already-accounted cost on the first post-upgrade resume. Caught by reading the migration file directly (not trusting the timed-out run's absence of a final report); re-delegated into the same `crush --session` with the exact gap named, and the continuation added the backfill correctly (`UPDATE sessions SET parent_cost_accounted = cost WHERE cost > 0`).
2. Task #111's own new regression test (`TestUpdateConfigVsReload_ReloadDiskStateNotLostByStaleClone`, sleep-based) was verified BY HAND — via a disposable `git worktree` checked out at the pre-fix commit — to **pass unconditionally even on unfixed code**: under `go test -race`, `ReloadFromDisk`'s real work (config parsing + provider setup) is slowed by race instrumentation enough (measured up to ~2.17s) that it reliably outlasted any writer sleep short enough to keep the test fast, so the reload always happened to publish last regardless of whether the two mutexes were unified — the test caught nothing. Rewrote it to use channel-based deterministic synchronization (park the writer mid-mutate on a channel, observe directly whether a concurrent reload can complete while the writer still holds the lock) instead of guessing at relative timing. Manually confirmed the rewritten test fails 5/5 against the pre-fix worktree and passes 10/10 against the fix before folding it into the commit.

**Tangential, non-task work done at the user's explicit direction mid-session (not part of the 9-task plan):**
- Investigated GLM/Z.AI model pricing: found the user's REAL global `crush.json` (`%LocalAppData%\crush\crush.json`) had a `providers.zai.models` override block that fully replaced the embedded catwalk model list but carried NO `cost_per_1m_*` fields — meaning cost tracking for all 7 configured GLM models was silently always zero. Filled in real pricing from `docs.z.ai/guides/overview/pricing` (fetched twice: once for the table, once to resolve a genuine "GLM-4.7-Flash" (free) vs "GLM-4.7-FlashX" ($0.07/$0.01/$0.4) naming ambiguity, resolved by cross-checking against already-trusted catwalk cache data rather than guessing) via `crush providers patch zai --global --json-file ...` (validated write, not hand-edited JSON).
- Discussed GLM Coding Plan subscription economics with the user (prompt-based quota, not token-based; peak/off-peak multipliers; whether sub-agent/worker delegation is currently quota-efficient). User asked me to clear the `worker` model slot (`crush models unset worker --global`), then later asked to restore it to `zai/glm-4.7` (`crush models use glm5_2 glm4_7_flash --worker glm4_7 --global`) — final state: `worker = zai/glm-4.7`, `large`/`small`/`reviewer` untouched throughout.

No `/loop` or other standing timers are armed. The babysit cron that drove this session's task execution has self-deleted (TaskList is empty).

## Active goal

The `/babygoal` Stop-hook condition in force for this session was: **"продолжай решать задачи с помощью агентов /crush, между задачами делай коммиты"**. TaskList is now fully empty (all 9 tasks completed and committed) — this condition's practical work is done. Per the babygoal skill's own semantics the hook auto-clears once satisfied; no further action needed unless the user wants to re-arm it for new work.

To re-arm this exact goal in a fresh session:
```
/goal продолжай решать задачи с помощью агентов /crush, между задачами делай коммиты.
```

## TaskList

Empty — all 9 tasks from this session's plan (#106-114) are `completed`. No pending/in_progress work.

### recently completed
- #114 БД: индекс на parent_session_id, ограничение fan-out CTE, мёртвая константа
- #113 background-буферы: реально освобождать память и планировать release
- #112 ConfigStore: закрыть mutable-лазейки в модели immutable snapshot
- #111 ConfigStore: сериализовать всех писателей и дисковый read-modify-write
- #110 read_delegation_transcript: SQL-пагинация вместо загрузки всей истории
- #109 grep: не запускать fallback после отмены ctx, ограничить длину строки
- #108 Персистентный транзакционный ledger стоимости sub-agent
- #107 pubsub: partial-checkpoint публиковать best-effort, must-deliver оставить терминальным
- #106 P0: починить lifecycle checkpoint-горутины (10 с мёртвого времени на оборот)

(Earlier session's tasks #92-105 also completed — see prior checkpoints for detail.)

## Decisions

- Delegated all 9 remediation tasks to the real system-installed `crush` CLI (`/crush` skill), one at a time sequentially, per the user's explicit `/babygoal` instruction — never the repo's own dev-build binary.
- Treated every `/crush` sub-agent diff as zero-trust: read the full diff personally, rebuilt, ran `-race` tests myself before committing. This caught the #108 missing-backfill gap and the #111 non-discriminating test — both would have shipped silently otherwise.
- For ConfigStore's writer/reload race (#111), chose ONE unified `publishMu` over separate locks (already the plan going in) — confirmed via the store's own pre-fix comment, which conceded "no tearing" but not "no lost update," and via a targeted `git worktree`-based before/after test comparison.
- Chose to leave the recursive-CTE fan-out (tree width) explicitly UNBOUNDED in `#114` (documented, not silently ignored) rather than attempt a SQL-side row-count cap — SQLite's recursive CTE has no mechanism to bound total materialized rows before its final `ORDER BY`+`LIMIT` applies, and a session realistically never has more than a handful of concurrent sub-agent children. Verified this holds at a 2000-child synthetic width via a new test.
- On the tangential GLM pricing task: wrote to the REAL global `crush.json` (not an isolated `CRUSH_GLOBAL_DATA`/`CRUSH_GLOBAL_CONFIG` sandbox) because the user's own request was to fix their actual, currently-broken cost tracking — this was a deliberate, explicit exception to the "isolate manual CLI exercising" caution in CLAUDE.md, not an oversight; confirmed with the user first via AskUserQuestion (scope: global vs local) before writing.

## Open questions

None outstanding. All 9 planned tasks are closed; no user question is pending an answer. If the user wants further review passes (a 3rd-generation review of these 9 commits, similar to how this session's work was itself a response to reviewing the prior 11 commits) that would be a new, not-yet-requested arc.

## Repo state
```
(clean — no output from git status --short)
```
```
5707e40f perf(db): index sessions.parent_session_id, chunk batch call-tree queries
64d9c09e fix(shell): actually free background-job buffer memory on release
3d6ec130 fix(config): close remaining mutable-pointer leaks out of ConfigStore
1082777e fix(config): unify ConfigStore writer/reload locks, serialize disk RMW
37b0acd6 test(app): implement session.Service.GetCallTreeActivity(Batch) on the mock
```
