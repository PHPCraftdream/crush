# Checkpoint — 2026-08-03 09:15

## Session summary

Continuation of the extended heartbeat/pulse review-fix run, picking up right after checkpoint `2026-08-02-2059.md` (which closed round 2 with 4 remaining MEDIUM/LOW findings — NEW-1, NEW-4, NEW-5, NEW-7 — from the second `@oh` review, filed as tasks #228-231 per the user's explicit choice to pause rather than continue a third autonomous round).

The user then invoked `/babygoal` again with the same contract (implement via sub-agent delegation, commit between tasks, then checkpoint + changelog + markdown commit + `@oh` review), but this time specifying `/crush` (the `crush run` CLI delegation mechanism) instead of `@sh` (the local `Agent` tool). First attempt: launched `crush run --role smart --session task-228-live-consumers ...` for task #228 in the background. It failed immediately — the configured provider (`zai`) refused with a peak-hours window (08:00–12:00, would reopen at 12:00). Per the `/crush` skill's explicit standing rule ("never self-add `--allow-peak-hours`... report the refusal and wait for their decision"), did NOT retry, bypass, or switch provider unilaterally — surfaced the refusal to the user via `AskUserQuestion` with three options (wait until 12:00, authorize `--allow-peak-hours` now, or switch to `@sh`). User chose to switch to `@sh` for all four remaining tasks.

All four tasks were then implemented via `@sh`, each following this session's established discipline throughout (precise architecture-informed briefs written after the orchestrator's own code reading, independent verification of every returned diff — full read, rebuild, re-vet, full-package `-race` re-run — before committing):

- `49717b61` — **#228**: `internal/session.InspectSessionLock` (the sole function behind both of `internal/server/handlers.go`'s external-ownership call sites — confirmed via a repo-wide grep, no other callers) now falls back to a real `session.IsProcessAlive(pid)` probe when mtime looks stale, closing the same gap already fixed for `sessions locks`/`sessions watch` in round 2. 4 new tests including a real spawned-child-process regression case.
- `91f094dd` — **#229**: `internal/cmd/sessions_inject.go`'s `isSessionLockAlive` — a third, independent, still-pure-mtime liveness implementation — now simply delegates to `InspectSessionLock` (just fixed in #228) instead of re-deriving the same logic a third time. 2 new tests.
- `6aac8415` — **#230**: documentation-only. `lockHolderProvablyDead`'s doc comment now honestly documents two narrow residual risks (a post-probe TOCTOU before the caller's `os.Remove`, and brief probe-induced lock contention with a concurrently-starting `crush run` on the same session id), matching `probeThenKillHolder`'s existing honesty bar. Evaluated and explicitly rejected a structural fix (holding the lock across removal) — it's cross-platform-fragile on Windows and would directly widen the second risk, a real tradeoff worked through and explained in the doc comment. Also evaluated and explicitly declined gating `sessions locks`' auto-delete behind an opt-in flag — judged as over-engineering an already-narrow, unobserved edge case; reasoning documented in the command's own `--help` text.
- `5aaba24f` — **#231**: `sessionsLocksCmdRun` (the last of the pure-mtime-liveness consumers) still hardcoded `cwd`-based data-directory resolution — same bug class tasks #219/#224 already fixed for `sessions kill`/`reset --force`, confirmed as a known-but-previously-out-of-scope gap. Fixed both call sites (the `locksDir` computation and `lockHolderProvablyDead`'s argument) to use the already-resolved `a.Config().Options.DataDirectory`. Also fixed a raw `os.ReadFile`+`Sscanf` PID read to use `session.ReadLockPID` instead, restoring the Windows PID-sidecar fallback (independently confirmed by reading `readLockFile`'s implementation: it unconditionally tries the sidecar first, not only as a fallback-on-unreadable). 2 new tests.

After all four commits, `CHANGELOG.md`'s existing heartbeat-activity entry (from round 2) was extended in place (not a new bullet block) to describe this third pass's findings and fixes, keeping one coherent narrative per feature rather than fragmenting the story across multiple bullets. This checkpoint file is being written next; committing the markdown and launching a fresh `@oh` review of the full accumulated range are the two remaining steps.

## Active goal

The `/babygoal` contract from this invocation ("implement via /crush [→ @sh after the peak-hours redirect] with commits between tasks, then /checkpoint + changelog + commit all .md + @oh review") is at its checkpoint step now; changelog is done; markdown commit and the review are next, immediately after this file is written.

## TaskList

### in_progress
(none)

### pending
(none)

### recently completed
- #228 Harden web UI's OwnedExternal gating (MEDIUM) — commit `49717b61`
- #229 Harden sessions_inject.go's isSessionLockAlive (LOW) — commit `91f094dd`
- #230 Document lockHolderProvablyDead's residual TOCTOU (LOW, docs-only) — commit `6aac8415`
- #231 Fix sessions locks' cwd-hardcoded data dir + PID read (LOW) — commit `5aaba24f`
- #213-227 — see `2026-08-02-{1406,1630,2059}.md` for the full prior history of this run

## Decisions

- When `crush run` refused due to a peak-hours window, followed the `/crush` skill's explicit standing rule to the letter: did not retry, did not add `--allow-peak-hours`, did not silently fall back to another provider — surfaced the refusal and the three real options to the user and waited for an explicit choice, exactly as the skill mandates ("only pass [--allow-peak-hours] when a human operator has explicitly asked... in this specific request").
- For #228, chose to fix `internal/session.InspectSessionLock` directly (rather than duplicating the PID-fallback logic a third time inside `handlers.go`) after confirming via a repo-wide grep that it has exactly two callers, both in the file the bug was reported against — the lowest-risk, most reusable fix location.
- For #229, chose to have `isSessionLockAlive` delegate to the just-fixed `InspectSessionLock` rather than reimplementing the same PID-fallback pattern a third time — direct reuse of #228's fix, avoiding a third independent (and inevitably-diverging-over-time) copy of the same liveness logic.
- For #230, deliberately did NOT implement a structural fix for the TOCTOU window after working through the real tradeoff (closing it would widen a different, related risk) — chose honest documentation over a fix that trades one narrow problem for a different one, consistent with this session's established "document accepted, narrow limitations rather than silently assume airtight" pattern (task #221's precedent).
- For #230's second design question (whether to gate `sessions locks`' auto-delete behind a flag), explicitly decided against it and documented why — avoiding reflexive "add a flag" as a default response to every edge case, weighing the real cost (a behavior change for existing scripts) against the risk (an already-narrow, unobserved collision window).

## Open questions

None blocking. All 4 findings from the second `@oh` review are now closed. Awaiting the outcome of the third `@oh` review (about to be launched) before considering this run's third round fully closed.

## Repo state

```
 D web/dist/.gitkeep
```
```
5aaba24f fix(cmd): fix sessions locks' cwd-hardcoded data dir and raw PID read
6aac8415 docs(cmd): document lockHolderProvablyDead's residual TOCTOU and contention
91f094dd fix(cmd): make sessions inject's liveness check honor the PID fallback
49717b61 fix(session,server): fall back to PID liveness in InspectSessionLock
91f5c829 docs: final checkpoint for the extended heartbeat/pulse review-fix run
```
