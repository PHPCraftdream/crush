# Checkpoint — 2026-07-30 15:45

## Session summary

Продолжение сессии (предыдущие чекпоинты: 2026-07-29-*, 2026-07-30-0730,
2026-07-30-1400), выполняющей `/babygoal`-батч по устранению находок из двух
независимых ревью-отчётов (`docs/reviews/2026-07-30-full-project-review.md`
от @oxx — задачи #152-162, и `docs/reviews/2026-07-30-project-review.md` —
задачи #163-171). Обе цепочки объединены в один `blockedBy`-чейн от #152 до
#178. **Задачи #152-173 теперь ПОЛНОСТЬЮ завершены и закоммичены** (20
коммитов от `262b1be7` до `8b7dfc74`).

С момента предыдущего чекпоинта (2026-07-30-1400) сделано: дождался
завершения фонового суб-агента `afe9d881b89f0c630` по задаче #170 (P3.1+P3.6+
P3.4), лично проверил весь дифф (прочитал построчно все 5 изменённых файлов),
прогнал `go build ./...`, `go vet`, `gofmt -l`, `go test ./internal/log/...
-race` (8/8), `go test ./internal/session/... -race` (полный пакет),
`node --test npm/crush/bin/crush.cache.test.js` (5/5) — всё зелёное, закоммитил
(`3b0169c8`). Затем взялся за #171 (P3.9): перечитал сам незакоммиченный дифф
`coder.md.tpl` и обнаружил, что он содержит РЕАЛЬНУЮ регрессию, совпадающую
дословно с находкой ревью — правило 7 (Orchestrator mode) откладывало
zero-trust проверку делегированных worker-чанков на один финальный проход
("not after each individual chunk"), хотя раньше проверка шла на каждом
чанке. Исправил: вернул проверку на границе каждого чанка, плюс сделал
исключение "under 4 lines of prose" для diagnosis/security-review/handoff
явным в самом правиле 3 (а не подразумеваемым через правило 6). Добавил два
регрессионных теста в `internal/agent/prompt/prompt_test.go`
(`TestBuild_IOContract_ExemptsDiagnosisFromLineLimit`,
`TestBuild_WorkerConfiguredAndSmart_VerifiesPerChunkNotJustAtEnd`) — оба
проходят, вместе со всем пакетом (9/9). Закоммитил (`6ee6fa98`). Решил, что
два других незакоммиченных .md файла (`claude_crush_fallback_command.md`,
`claude_slash_command.md`) уже связны и готовы (совпадают с уже
задеплоенным и рабочим текстом skill'ов в этой же сессии) — не требуют
пересмотра, только коммита. Затем #172: дописал CHANGELOG.md сводной секцией
по задачам #152-171 (не коммитил отдельно, по инструкции задачи — войдёт в
общий коммит). Затем #173: закоммитил все выходные .md-файлы одним коммитом
(`8b7dfc74`) — CHANGELOG.md, оба claude_*.md, 4 файла чекпоинтов,
docs/reviews/ (оба отчёта) — **явно НЕ включая** удаление
`web/dist/.gitkeep` (остаётся неоткоммиченным по CLAUDE.md).

**Сейчас**: выполняю задачу #174 (этот самый `/checkpoint`). Следующий шаг —
#175: запустить ревью-агента `@oh` (`Agent(subagent_type: "oh")`) по всей
проделанной работе #152-174, затем #176 — применить его находки по тому же
zero-trust протоколу. После этого в очереди #177/#178 — новые задачи из
побочного расследования двойного краша `crush run` в другом репозитории
(`D:\dev\rust\tor-socks5\.crush`): отсутствие `recover()` в горутине агента
(`internal/app/app.go:1150`), в пути саб-агента (`coordinator.go:2414-2489`
`runSubAgent`) и в колбэках `internal/shell/background.go` (`OnDone`,
`time.AfterFunc`), плюс гарантия что паника попадает в `crush.log` через slog,
а не молча теряется в `os.Stderr`.

Babysit-cron `33ae5347` (`7,22,37,52 * * * *`, session-only) подтверждён
активным на протяжении всей сессии через регулярные тики (последний тик
ответил "still running #170" перед завершением той задачи).

## Active goal

Нет `/goal` Stop-hook. Babysit-cron `33ae5347` активен и мониторит TaskList.

## TaskList

### pending
- #174 Выполнить /checkpoint после завершения всей работы — ВЫПОЛНЯЕТСЯ ПРЯМО СЕЙЧАС этим файлом
- #175 Запустить ревью агента @oh по всей проделанной работе (#152-174) (blockedBy: #174 — разблокируется этим чекпоинтом)
- #176 Применить находки ревью @oh (#175) (blockedBy: #175)
- #177 Добавить recover() в горутину агента crush run (app.go) и путь саб-агента (blockedBy: #176)
- #178 Гарантировать что паника в crush run попадает в crush.log, а не только в stderr (blockedBy: #177)
- #142 crush-fallback state (persistent sentinel — не завершать)

### recently completed (последние 10)
- #173 Закоммитить все обновлённые .md файлы (коммит 8b7dfc74)
- #172 Обновить CHANGELOG.md по итогам задач #152-171
- #171 P3.9: пересмотреть незакоммиченные правки coder.md.tpl (коммит 6ee6fa98) — найдена и исправлена реальная регрессия
- #170 P3.1+P3.6+P3.4: приватность body-логов, require() в горутинах, SHA-256 npm cache (коммит 3b0169c8)
- #169 P2.6+P2.7: межпроцессный deploy lock + keyset-пагинация transcript
- #168 P2.1: контракт immutable config snapshot
- #167 P2.3: Grant/Deny outcome vs atomic Take
- #166 P1.7: cliprovider выбирает WSL bash.exe
- #165 P1.6: учёт токенов недетерминирован
- #164 P1.2+P1.3: config reload publishMu + PushPopCrushEnv

(Полный список #127-173 — все completed; см. TaskList напрямую для деталей.)

## Decisions

- Задача #171 потребовала РЕАЛЬНОГО содержательного вмешательства, а не просто
  коммита as-is: обнаруженная в незакоммиченном диффе регрессия правила 7
  (verification deferred to one final pass) была исправлена лично, с двумя
  новыми регрессионными тестами, закрепляющими оба свойства (line-limit
  exemption + per-chunk verification) против будущих правок этого шаблона.
- Два других незакоммиченных .md файла (`claude_crush_fallback_command.md`,
  `claude_slash_command.md`) признаны уже завершёнными и не требующими
  пересмотра — их содержимое дословно совпадает с уже загруженными и
  используемыми в этой сессии определениями skills `/crush-fallback` и
  `/crush`, то есть это просто синхронизация репозиторного источника с уже
  проверенной версией.
- CHANGELOG.md для #152-171 сделан НЕ отдельным коммитом (по явной инструкции
  в описании задачи #172), а объединён в общий коммит #173 вместе с
  остальными markdown-файлами.
- `web/dist/.gitkeep` осознанно оставлен неоткоммиченным во всех коммитах
  этой сессии (сверено `git status --short` перед каждым `git add`) — по
  CLAUDE.md, никогда не коммитить это состояние без явного запроса.

## Open questions

- Нужно ли после #178 (новые задачи про crash-safety в crush run) продолжать
  искать другие непокрытые recover()-пробелы в проекте, или ограничиться
  найденным расследованием — не решено, будет видно по факту после #177/#178.
- Ревью @oh (#175) ещё не запущено — не решено, какие новые задачи оно
  добавит поверх #176 и далее.

## Repo state
```
 D web/dist/.gitkeep
```
```
8b7dfc74 docs: changelog for #152-171, /crush-fallback command sync, checkpoints, review reports
6ee6fa98 fix(agent): stop coder.md.tpl from deferring worker-chunk verification to one final pass
3b0169c8 fix(log,session,npm): P3.1+P3.6+P3.4 nit batch
5e36eb50 fix: cross-process deploy lock, keyset transcript pagination, SetupAgents regression
a6871307 fix(config): stop mutating published config snapshots in place
417c6510 fix(permission): take before publish/persist so only the race winner acts
4ddfc635 fix(cliprovider): resolve bash via the WSL-aware PATH lookup, not bare exec.LookPath
07f0cd4b fix(agent): stop title generation from racing main-turn token counters
```

Рабочее дерево ЧИСТОЕ, кроме преднамеренного удаления `web/dist/.gitkeep`.
Ни один из 20 коммитов этой сессии (`262b1be7`..`8b7dfc74`) не запушен —
push не запрашивался явно.
