# Checkpoint — 2026-08-04 19:15

## Session summary

Continuation of `2026-08-04-1830.md`. That checkpoint closed with `8ba3d3f8`
(round-11 review's HIGH-1/MEDIUM-1 fix) and a round-12 `@oh` review about to
launch. This checkpoint covers what that review found and the fix landed in
response — `8c975400` + `c30a45e5`.

**Round-12 review verdict**: unlike round 11 (which found nothing inside
the mailbox's own state machine but two seam-level bugs), round 12 found
NOTHING new in `drainOrReleaseFinal`'s atomicity, epoch handling, or lock
ordering — all independently re-derived and confirmed correct by the
reviewer. It found exactly one real bug, and it is directly explained by
round 11's own fix: **finding A** — the MEDIUM-1 shape round 11 fixed on
the legacy-`messageQueue` reclaim branch was still present, unfixed, on
the adjacent `mb.submitted` branch (the branch hit by the CURRENT,
non-legacy `submit()` path — far more commonly than the legacy fallback).
By the time that branch runs, `mb.current.cancel` still held the
just-finished turn's own spent-but-non-nil cancel func, defeating
`Cancel()`/`InterruptAndReplace()`'s fallback gate the same way, for the
window until the next turn's own `beginGeneration`.

Independently confirmed via direct code read before fixing (same
discipline as every round). Fixed in `8c975400` with the identical
one-line shape as round 11's fix (`mb.current.cancel = nil` on that
branch too), plus three smaller items the same review raised:
- **Finding B**: a new test assertion (`require.NotNil(t,
  mb.dispatcherCancel, ...)`) was vacuous — the fixture's own initial
  value was also non-nil, so the check passed regardless of whether the
  real assignment happened. Replaced with an identity check (call the
  field, assert the NEW closure fired).
- **Finding C**: hardened the legacy-reclaim branch to explicitly set
  `mb.state = mbOwned` rather than relying on an invariant that isn't
  locally re-asserted — safe no-op today, cheap insurance against a
  future second call site (once #281/#284/#285 land) landing here with
  `state == mbIdle`.
- **Findings D/E/G** (all doc-only, no behavior change): documented two
  accepted trade-offs directly in `drainOrReleaseFinal`'s doc comment
  (the OS lock's hold-time no longer covers `runTurn`'s trailing
  cleanup — title rename, cost increment, both narrowly-scoped/additive
  so worst case is cosmetic not lossy; `release()`'s real disk I/O now
  runs while `mb.mu` is held, briefly coupling mailbox reads to disk
  latency) and corrected `testDrainSeam`'s doc, which still only
  described the older `drainOrRelease` seam point.
- Finding F (dead `releaseSessionReservation` wrapper) — left as-is, not
  a correctness issue, already self-documented.

New regression test
`TestMailbox_DrainOrReleaseFinal_SubmittedBranchClearsStaleCancelHandle`
mirrors the existing legacy-reclaim test for the `mb.submitted` branch;
sanity-checked via revert (removing the fix line made it fail on the
exact target assertion, confirmed, restored). Full `go build`/`go
vet`/`internal/agent` suite under `-race -count=1` clean (~145s);
adjacent regression tests repeated 5x under `-race`, no flakes.

**On the meta-question this cycle keeps circling**: round 12 found
nothing new inside the core mailbox logic for the second round running —
11 found nothing there (only seam bugs), 12 found nothing there either
(just an unfixed sibling of the SAME seam bug round 11's fix should have
also covered). This reads as genuine convergence, not a new open-ended
tail: the "core state machine" question looks settled after 3+ rounds of
adversarial reading; the remaining risk is specifically at the seams the
still-incomplete stage-2 migration (#281/#284/#285) leaves exposed, which
round 12 (like round 11) explicitly named as the more promising place to
look than another blind read-through.

## Active goal

Standing `/babygoal` contract, same text as prior rounds. Commits ✅
(`8c975400`, `c30a45e5`), CHANGELOG ✅, checkpoint ✅ (this file, about to
commit). Round-13 `@oh` review about to launch to close this round's
contract — this is the 13th round of the same review-fix-review cycle;
given round 12's own more-convergent verdict (nothing new in core logic,
one narrowly-scoped sibling bug), this round's review is scoped tightly
to the new commit rather than re-litigating the whole subsystem again.

## TaskList

- #290 (round-12 review tracking task) — completed.
- All other backlog unchanged from `2026-08-04-1830.md`'s snapshot,
  including #289 (flaky end-to-end MEDIUM-1 test, still unscheduled LOW
  backlog, not release-blocking).

### pending — still backlog, unchanged from last checkpoint
- #265 [P0-1], #268 [P0-4], #269 [P0-5], #286 [P0-6]
- #284 [P1-2], #285 [P1-1], #281 (mailbox stage 2.5 cleanup)
- #272 [P1-3], #273 [P1-4]
- #274/#275/#276 [P2]
- #262/#263/#264 — round-8 LOW backlog
- #277 — release gate, blocked on all of the above
- #261 — crush-fallback sentinel (armed: sh)
- #289 [LOW] — flaky end-to-end MEDIUM-1 test, not release-blocking

## Decisions

- Fixed finding A immediately (same round, same pattern as every prior
  round) rather than filing + stopping — it's a real, confirmed defect
  in code from earlier the same day, and the user's standing priority
  has been working software over ceremony.
- Did NOT build the invariant-based table test round 12's review
  explicitly recommended (a postcondition check run after every mailbox
  mutator, which the reviewer argued would have mechanically caught
  findings A, MEDIUM-1, and BLOCKER-2 all at once, rather than one
  instance at a time per round). Given this cycle has already run 12
  rounds and the user has expressed real time pressure, chose to land
  the confirmed, narrowly-scoped fix now and surface the invariant-test
  recommendation to the user directly rather than unilaterally
  expanding scope further under time pressure — the user's call whether
  to invest in it before or after shipping.

## Open questions

- Whether to invest in the invariant-based table test round 12
  recommended (would need to be scoped and built) before declaring the
  mailbox subsystem release-ready, or treat the current state (4 rounds,
  narrowing findings, 2 consecutive rounds finding nothing new in the
  core state machine) as sufficient and ship, with #289 and the
  invariant-test idea as post-ship backlog.
- The deployed binary still lags git — last deploy was `23004ec3`, well
  before rounds 9-12's fixes. Given the user's stated urgency, a
  `go run deploy.go` is likely wanted once this round's review concludes.
- Round-13's own verdict (in flight) will inform whether this cycle can
  be declared closed or needs a further round.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
```
```
c30a45e5 docs(changelog): document round-12 finding-A fix (stale cancel on submitted branch)
8c975400 fix(agent): close round-12 review's finding A (stale cancel on the mb.submitted reclaim branch)
88639c59 docs(changelog): document round-11 HIGH-1/MEDIUM-1 mailbox/OS-lock fix
8ba3d3f8 fix(agent): close round-11 review's HIGH-1 (mailbox/OS-lock ordering) + MEDIUM-1 (nil cancel after reclaim)
0845174b docs: checkpoint for the round-10 review + its HIGH-1/HIGH-2/MEDIUM-1 fixes
```
