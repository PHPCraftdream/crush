# Checkpoint — 2026-08-10 11:30

## Session summary

Continuation of the long `/babygoal` round for tasks #337-349 (session-concurrency
fixes). Both rounds of `@oh` review-finding fixes are now committed. The first
final review (6 findings: P0-1, P0-2, P1-1, P2-1, P2-2/P2-3, P2-4, P3-nits) was
closed in commit `781e6413`. A second, confirming `@oh` review of that very commit
found 6 NEW defects introduced by the fixes themselves (NEW-1 through NEW-6) — all
independently re-verified by reading code directly (never accepted from review
narration alone) and closed in commit `41487b2e`:

- NEW-1 (BLOCKER): data race on `app.AgentCoordinator` between `InitCoderAgent`'s
  write and the run queue pump's lazy read — pump started before the coordinator
  was assigned. Fixed by moving pump construction/`Start()` to strictly after
  `InitCoderAgent(ctx)` succeeds in `App.New`. New regression test
  `TestAppNew_RunQueuePump_OrderingRace` pre-seeds durable queue work before
  calling `App.New`, simulating a restart-recovery scenario the existing wiring
  test cannot catch (it only enqueues work after `App.New` already returned).
- NEW-2 (BLOCKER): new pump-wiring test called real `app.New()` without
  environment isolation (same failure class documented in
  `internal/cmd/providers_test.go` — a 9+ minute hang precedent). Fixed with the
  same `t.Setenv` isolation block (`XDG_DATA_HOME`/`CRUSH_GLOBAL_DATA`/
  `XDG_CONFIG_HOME`/`CRUSH_GLOBAL_CONFIG`/`CRUSH_PROVIDER_CACHE_ONLY`).
- NEW-3 (MEDIUM): removing the `attempts` increment from `LeaseRunQueueEntryByID`
  (part of the first pass's P0-2 fix) left crashed/hung executions never
  accumulating attempts via `CleanupExpiredLeases`, risking an endless retry loop
  for a poison entry. Fixed by adding the increment to `CleanupExpiredLeases`.
- NEW-4/NEW-5 (nits): stale "200ms" doc reference in `lock.go` corrected to
  500ms; imprecise "would either panic or hang" comment on
  `TestRunQueue_PumpLifecycle_GracefulShutdown` reworded.
- NEW-6 (minor): `docs/release_gate_summary.md`/`docs/release_gate_report.md`
  still asserted the P1-1 regression was "confirmed PRE-EXISTING via git stash"
  — a methodologically invalid check performed after the regressing commits were
  already on `main`. Corrected; also removed an obsolete ~122-line "Known Issues
  and TODOs" section that contradicted the file's own corrected conclusion.

Final verification (independently run, not accepted from narration): `gofmt -l`
clean, `go build ./...` clean, `go vet ./...` clean (one pre-existing unrelated
warning in `internal/csync/maps.go`), `golangci-lint run` 0 issues on touched
packages, `go test -run TestReleaseGate` all PASS/SKIP as expected, all new/
changed regression tests individually PASS with genuine FAIL→PASS revert-checks,
`go test ./internal/agent/... ./internal/session/... ./internal/app/... -race`
clean (zero race warnings across all three packages), `go test
./internal/agent/... -count=2` clean except one unrelated pre-existing flake
(`TestFileMatchesHonoursDeadlineMidHugeLine` in `internal/agent/tools` — a
wall-clock deadline test not touched by this diff, confirmed to pass 3/3 in
isolation; only trips under `-count=2`'s heavier system load).

`CHANGELOG.md` updated with a new "Fixed" entry summarizing both `@oh` review
rounds (first pass's 6 findings + second pass's 6 new findings), appended at the
end of the Fixed section matching the file's existing chronological-append style.

Per the standing `/babygoal` contract's closing steps (`/checkpoint`, update
changelog, commit all `.md` files, run `@oh` review): checkpoint and changelog
are done; next is committing the `.md` changes (this checkpoint file + the prior
`2026-08-10-1109.md` + `CHANGELOG.md`), then launching a third confirming `@oh`
review pass over the full `#337-349` range per the contract's explicit
instruction, weighing that both prior passes found genuine new issues each time
against the very large amount of orchestrator time already spent this session.

Earlier in this session the user instructed switching future delegation from
`/crush` to the local `@sh` agent — noted but not yet exercised, since all
remaining work in this round was done directly (fixes needed deep conversational
context already accumulated, not fresh delegation).

## Active goal

Armed `/goal` Stop-hook condition (verbatim, from the original `/babygoal`
invocation):

> реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу

## TaskList

### in_progress
- #350 Address @oh final review findings for #337-349 (no blockedBy) — NEW-1..NEW-6
  fixes committed and verified; remaining steps: commit `.md` files, launch third
  confirming `@oh` review pass, then close this task.

### recently completed
- #337-#349 — all closed earlier in this session (see `git log`, commits `ac0536c4`
  through `38c00b14`)

## Decisions

- **Pump-start moved to strictly after `InitCoderAgent`** chosen as the PRIMARY
  fix for NEW-1 (architectural removal of the hazard), not just the lazy-read
  defense-in-depth already in place from the first review pass.
- **Wrote a separate test (`TestAppNew_RunQueuePump_OrderingRace`)** rather than
  extending the existing wiring test — experimentally confirmed the existing test
  structurally cannot catch this race (it enqueues work only after `App.New`
  returns).
- **CHANGELOG entry appended at the end of the Fixed section**, matching this
  file's established chronological-append convention rather than inserting near
  the topically-related 2026-08-09 entries higher up.
- **Treated `TestFileMatchesHonoursDeadlineMidHugeLine`'s single failure under
  `-count=2` as an unrelated pre-existing flake, not a regression** — verified via
  git diff (file untouched by this round) and 3/3 clean reruns in isolation,
  rather than assuming it needed investigation as part of this round's work.

## Open questions

None blocking for the user. Still open technically: whether to actually launch
the third confirming `@oh` review pass the `/babygoal` contract calls for, given
both prior passes found genuine new defects each time — leaning yes, per the
contract's explicit instruction, but noting the diminishing-returns tradeoff
against session length already spent.

## Repo state

```
 M CHANGELOG.md
?? dev/
?? docs/checkpoints/2026-08-10-1109.md
```

```
41487b2e fix(app,session,db): close NEW-1..NEW-6 from second @oh review pass
781e6413 fix(agent,app,session,db): close all NO-GO findings from the final @oh review of #337-349
38c00b14 docs: changelog, checkpoints, and #349 deferral plan for the #337-349 round
3a8d3253 test(agent,app,session): executable release gate suite for #337-347 (#348)
21c20f90 docs(agent): correct doc comments that contradicted the code after #339/#340 (#347)
```
