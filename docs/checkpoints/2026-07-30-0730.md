# Checkpoint — 2026-07-30 07:30

## Session summary

Продолжение сессии, начавшейся ещё с чекпоинта `2026-07-29-2124.md` (флейки #145 + caveats #146-149 + CLI fork #143 + второй раунд ревью @oh #150-151 — всё это было выполнено и закоммичено в начале ЭТОЙ части: коммиты `262b1be7`..`054d23d6`). Затем пользователь явно попросил "сделай пуш, проконтролируй CI" — это разрешение на push было использовано.

**Push и CI:** первый `git push fork main` прогнал pre-push hook (build+lint+test) и запушил `054d23d6` в `PHPCraftdream/crush`. Реальный GitHub Actions CI (не только локальный hook) поймал реальную проблему: job `build` (ubuntu-latest) упал на шаге `go mod tidy && git diff --exit-code`, потому что `go.sum` содержал 183 лишние строки — побочный эффект того, что второй раунд ревью (@oh, задача #151) запускал `golangci-lint run` через `go run` в этом окружении, что добавило чек-суммы транзитивных зависимостей golangci-lint, не относящихся к самому модулю проекта. Исправлено: `go mod tidy` локально (чисто -183 строки, go.mod не тронут), закоммичено `33ec9570`, запушено вторым `git push` — эта часть уже подтверждена завершённой (`task-notification` пришла с `exit code 0`), но я ЕЩЁ НЕ проверил реальный GitHub Actions статус после ВТОРОГО пуша (первая проверка через `gh run list`/`gh run view` была только для ПЕРВОГО, сломанного пуша) — **это первое, что нужно сделать при возобновлении**.

**Два независимых широких ревью проекта запущены параллельно с push:**
1. Пользователь явно попросил запустить `@oxx` (Opus, max effort) с широким ревью проекта + недавних доработок, отчёт в файл. Результат: `docs/reviews/2026-07-30-full-project-review.md` (681 строка). 0 Critical, 4 High (H-1..H-4), 7 Medium (M-1..M-7), 8 Low (L-1..L-8), 10 Nit (N-1..N-10). Из него заведены задачи **#152-#162** (последовательная цепочка `blockedBy` по рекомендованному в отчёте порядку работ).
2. Позже (через `/ox` слэш-команду с текстом от пользователя, вставленным из ДРУГОЙ уже готовой работы другого агента) обнаружился ВТОРОЙ, независимый отчёт: `docs/reviews/2026-07-30-project-review.md` (618 строк) — судя по содержанию, написан другим ревьюером/агентом в параллельной работе (не мной запущен в этой видимой части сессии). Я его прочитал целиком, ЛИЧНО ПРОВЕРИЛ две ключевые находки (P1.1 — sub-agent lock, подтверждено чтением internal/agent/agent.go:484-524; P1.7 — cliprovider WSL bash, подтверждено что на ЭТОЙ машине тесты проходят, но `where bash` показывает Git Bash первым, то есть находка реальна для машин с обратным порядком PATH) и завёл задачи **#163-#171** по его находкам, которые НЕ дублируют первый отчёт (P1.1 sub-agent lock, P1.2+P1.3 config env/publishMu shell-resolve, P1.6 token accounting race, P1.7 cliprovider WSL, P2.1 immutable config contract, P2.3 Grant/Deny order-of-operations, P2.6+P2.7 deploy lock + keyset pagination, P3.1+P3.6+P3.3+P3.4 пачка мелких, P3.9 пересмотр coder.md.tpl "4 lines" правила).

**НЕЗАВЕРШЕНО на момент /checkpoint:** я был в процессе "дополняю существующие задачи, которые второй отчёт расширяет" — планировал добавить `blockedBy`/перекрёстные ссылки между #163-171 и уже существующими #152-162 (некоторые находки второго отчёта пересекаются по теме с первым — например P2.3 про permission соседствует с #156/H-4, P1.2 про publishMu соседствует с #153/H-2). Эта работа была прервана `/checkpoint` ДО того как я успел проставить `addBlockedBy` для #163-171 — сейчас они все висят БЕЗ зависимостей друг от друга и от #152-162, что технически позволяет начать любую из них немедленно, но не отражает порядок приоритета, который дали оба отчёта.

Ни одна из задач #152-171 ещё не взята в работу — это чистый backlog, заведённый, но не начатый.

## Active goal

Нет активного `/goal` Stop-hook. Babysit-таймер (`d700ee11`) был явно остановлен мной в конце предыдущей части сессии, когда весь `/babygoal`-батч (#127-151) завершился — он НЕ переустановлен для нового backlog (#152-171). Если пользователь хочет автоматическую доработку этого backlog — нужно заново `/babygoal` или `/babysit`.

## TaskList

### pending (backlog от первого отчёта @oxx, цепочка blockedBy)
- #152 H-1+M-6: изолировать CRUSH_GLOBAL_CONFIG в тестах (coordinator_test.go, internal/config)
- #153 H-2: withConfigWriteLock держит publishMu до 30с — регрессия из b53cbf3d  (blockedBy: #152)
- #154 H-3: паника в WS-хендлере роняет весь процесс  (blockedBy: #153)
- #155 M-1+L-1: единая ForkSessionTx (web теряет reasoning effort, CLI теряет todos)  (blockedBy: #154)
- #156 H-4: permissionService.Request держит requestMu весь ожидание  (blockedBy: #155)
- #157 M-2: projects.Register неатомарен  (blockedBy: #156)
- #158 M-3+M-4: search.go sleep под локом + unbounded ReadAll  (blockedBy: #157)
- #159 M-7: WebSocket CheckOrigin всегда true — CSWSH  (blockedBy: #158)
- #160 Пачка Low (L-2,3,5,6,7,8)  (blockedBy: #159)
- #161 M-5: разделить read/write пулы SQLite (архитектурная)  (blockedBy: #154, #160)
- #162 Ниты N-1..N-10  (blockedBy: #161)

### pending (backlog от второго отчёта, БЕЗ проставленных blockedBy — открытый пункт)
- #163 P1.1 [КРИТ, лично подтверждено]: sub-agent не берёт межпроцессный lock своей дочерней сессии
- #164 P1.2+P1.3: config publishMu держится во время 5-минутных shell-resolve + PushPopCrushEnv меняет глобальный env
- #165 P1.6: учёт токенов недетерминирован (title-горутина vs основной turn)
- #166 P1.7 [лично проверено]: cliprovider выбирает WSL bash.exe, зависит от порядка PATH
- #167 P2.3: Grant/Deny публикуют outcome до atomic Take
- #168 P2.1: контракт immutable config snapshot нарушается
- #169 P2.6+P2.7: межпроцессный deploy lock + keyset-пагинация transcript
- #170 P3.1+P3.6+P3.3+P3.4: пачка мелких (body-логи, require() в горутинах, ширина call tree, npm cache hash)
- #171 P3.9: пересмотреть незакоммиченные правки coder.md.tpl

### pending (permanent sentinel)
- #142 crush-fallback state (persistent — do not complete)

### recently completed (весь предыдущий backlog, 8 коммитов за сессию)
- #151 Применить находки второго раунда ревью @oh (#150)
- #150 Второй раунд ревью @oh
- #149, #148, #147, #146, #143, #145 — все caveat-фиксы

## Decisions

- Реальный GitHub Actions CI (не только локальный `.githooks/pre-push`) — источник истины; локальный hook прошёл на первом пуше, но настоящий CI поймал `go.sum` drift, который hook не проверяет одинаково (или проверяет иначе в CI-окружении). Исправлено немедленно (`go mod tidy`, коммит `33ec9570`), запушено повторно.
- Оба отчёта ревью (первый `@oxx` full-project, второй — независимый, обнаруженный через `/ox` с чужим текстом) трактуются как ДОПОЛНЯЮЩИЕ, не конкурирующие — находки не задублированы в TaskList, каждая заведена один раз со ссылкой на источник в описании.
- Находки из отчётов не применялись слепо — минимум две (P1.1, P1.7) лично перепроверены оркестратором чтением кода/прогоном тестов перед тем как заводить задачи, по установившемуся в сессии паттерну "zero-trust перед действием".
- P1.7 (cliprovider WSL bash) — важный нюанс: на машине оператора тесты ПРОХОДЯТ (Git Bash в PATH раньше System32), но находка всё равно реальна и заведена, т.к. зависит от машины/порядка PATH, а не является ложным срабатыванием ревьюера.

## Open questions

- Нужно ли проставить `blockedBy`/приоритетные связи между #163-171 и #152-162 (прервано `/checkpoint`) — или backlog из двух отчётов оставить как два параллельных, независимо приоритезируемых списка?
- Нужно ли устанавливать `/babysit`/`/babygoal` заново для проработки backlog #152-171, или пользователь будет управлять этим вручную/другой командой?
- Судьба трёх незакоммиченных .md-файлов (`coder.md.tpl`, `claude_slash_command.md`, `claude_crush_fallback_command.md`) — лежат в рабочем дереве с более ранней части сессии, решение за пользователем (см. также задачу #171, которая предлагает содержательно пересмотреть `coder.md.tpl`, а не просто закоммитить как есть).
- Статус ВТОРОГО push (`33ec9570`) на реальном GitHub Actions CI не проверен после отправки — открыт.

## Repo state
```
 M internal/agent/templates/coder.md.tpl
 M internal/cmd/claude_crush_fallback_command.md
 M internal/cmd/claude_slash_command.md
 D web/dist/.gitkeep
?? docs/checkpoints/2026-07-29-2018.md
?? docs/checkpoints/2026-07-29-2124.md
?? docs/reviews/
```
```
33ec9570 chore: run go mod tidy to drop stray go.sum entries
054d23d6 fix(test): apply second-round @oh review findings (A-H)
caf12680 fix(cmd): make `crush sessions fork` transactional
7a2ea514 fix(shell): resolve WSL launcher paths from %SystemRoot%, not hardcoded C:\Windows
ae1637d0 docs(config): restore doc comments, fix test goroutine UB, document lock cleanup
61c23314 fix(grep): don't match a partial line before returning its read error
```

Примечание: `docs/reviews/` (оба отчёта, 681+618 строк) — новые, нетронутые, не закоммичены. `web/dist/.gitkeep` — намеренное состояние (CLAUDE.md), не трогать без явного запроса. Три незакоммиченных `.md` — из более ранней части сессии, см. Open questions.
