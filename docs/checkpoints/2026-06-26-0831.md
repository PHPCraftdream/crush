# Checkpoint — 2026-06-26 08:31

## Session summary

Work on the `PHPCraftdream/crush` fork (Go backend + React/TS web UI,
`D:\dev\go\crush`). This session split into two arcs.

**Arc 1 — Phase 4 (event-loop autonomous idle-resume).** Designed + implemented
via `/babygoal`+`/crush` in 5 stages (#81–#85), all committed and pushed, CI
green: design doc (Shape A "wake-on-event supervisor"), config+guardrail
primitives (`Options.AutoResumeOnJobDone` default OFF, per-session
consecutive-resume bound = 5, `persistentMode`), the wake-supervisor branch in
`coordinator.notifyBackgroundJobDone` (eligible → `go c.Run` reusing
sessionAgent.Run's single-flight; else Phase-3 `InjectMessage`), a web
"↻ auto-resumed" badge (`message.AutoResumed`, threaded end-to-end like
`Hidden`), and a final pre-push gate. Also fixed a CI data race (`67cb105c`,
job_output tests under `-race`).

**Arc 2 — five bug/feature fixes (#86–#91), all committed + pushed.** All
delivered via `/crush` sub-agents with zero-trust verification by me:
- **#87** `sessions list` no longer mislabels cleanly-finished runs as
  "crashed" (a dead-PID lock + last assistant msg `end_turn` → "done").
- **#86** background-job-completion notices get a persisted `BackgroundJobNotice`
  flag (threaded like AutoResumed) + "⚙ background job" badge, set on BOTH the
  Phase-3 inject and Phase-4 auto-resume paths.
- **#88** "delete all sessions except this one" sidebar button + confirm dialog
  + `delete_other_sessions` WS command.
- **#90** those notices now render on the LEFT as a system notice
  (`BackgroundJobNotice` component mirroring `SummaryMessage`), not a right user
  bubble — Role=user kept in data so the model still reacts.
- **#89 then #91** "agent content disappears": #89 stopped `upsertMessage`
  wholesale-replace from erasing THINKING; #91 generalized it
  (`mergePreserveThinking` → `mergePreserveContent`) to also protect TEXT — a
  stale streaming snapshot with shorter text was erasing the answer when the
  thinking spoiler updated. Root cause confirmed store-side; `groupPartsIntoBlocks`
  render never drops parts.
- Doc: added a `/crush` rule forbidding git writes in parallel runs
  (`internal/cmd/claude_slash_command.md` + the live `~/.claude/commands/crush.md`).

Process notes: ran #86 & #87 in parallel (disjoint files) — surfaced the user's
valid concern about concurrent git; mitigated by disjoint file-sets + no-commit.
Recurring gotcha caught by hand twice: crush's `sqlc generate` re-introduces
unrelated drift in `internal/db/models.go` (QueueTask/Session budget fields) — I
trim it to only the new column each time. Local pre-push hook runs WITHOUT
`-race`; CI build job runs WITH `-race`, so I now run the race gate myself before
pushing.

In flight at checkpoint: CI for HEAD `7ce333fc` — lint ✅, security ✅, build
(`-race`) still in_progress (~4m, normal); every prior push this session went
fully green. No `/babysit` or `/goal` timers active (the Phase-4 babysit
self-deleted when its TaskList emptied).

## Active goal
none (no `/goal` Stop hook in force this session).

## TaskList
(empty — `TaskList` returns "No tasks found"; all session tasks #81–#91 were
completed and cleared.)
### recently completed (this session, from memory — not in live TaskList)
- #91 generalize merge so streamed TEXT never disappears (committed 7ce333fc)
- #90 render background-job notices on the left as system (committed ba67da8f)
- #89 stop upsert erasing shown THINKING (committed 9af1a2a0)
- #88 "delete all sessions except this one" button (committed b821a8e8)
- #86 BackgroundJobNotice flag + badge (committed 5a4b6a6c)
- #87 don't report cleanly-finished runs as "crashed" (committed 77b481bd)
- #81–#85 Phase 4 (committed 25b09990 / b1cff9fc / a0b5c920 / 8dc66b47 / ef52ce8e)

## Decisions
- **Phase 4 = Shape A (wake-on-event supervisor)** over Shape B (true mid-turn
  suspend) — reuses Run/messageQueue single-flight, no turn-loop surgery.
  Autonomy opt-in (default OFF), bounded, never for `crush run`.
- **Background-job notices keep Role=user** (so the model reacts) but render
  LEFT as system notices — fix the presentation, not the data model.
- **"Never disappear" is a store-level invariant**: `mergePreserveContent` keeps
  the longer side per accumulating kind (text, thinking); tool/finish always from
  the incoming update. Render side (`groupPartsIntoBlocks`) verified not to drop.
- **Parallel `/crush` only with disjoint file-sets + no commits**; orchestrator
  commits sequentially. Encoded as a `/crush` rule.
- **Always run `go test -race ./...` myself before pushing** — the local
  pre-push hook omits `-race`; CI caught a job_output race the hook couldn't.

## Open questions
- **Deploy gap (still open):** `fork/main` now carries Phase 4 + #86–#91, but the
  running crush binary doesn't have them until rebuilt. `deploy.go` exists
  (prod-build + kill running crush + replace npm binary) — it kills live
  sessions, so it needs an explicit "deploy" from the user. NOT done.
- CI build (`-race`) for `7ce333fc` was still in_progress when this checkpoint
  was written — confirm it went green.

## Repo state
```
 D web/dist/.gitkeep          (intentional — do NOT commit, per fork CLAUDE.md)
?? docs/bugs/                 (background-job-notice-as-user.md — untracked notes)
?? docs/checkpoints/2026-06-24-0100.md
?? docs/checkpoints/2026-06-25-2218.md
```
```
7ce333fc fix(web): never let a message update erase already-shown text either
ba67da8f feat(web): render background-job notices on the left as system notices
9af1a2a0 fix(web): never let a message update erase already-shown reasoning
9b742ca0 docs(crush): forbid git writes in parallel crush runs
b821a8e8 feat(web,server): "delete all sessions except this one" button in the sidebar
```
