# Checkpoint — 2026-07-30 14:00

## Session summary

Продолжение большой сессии (началась с чекпоинтов 2026-07-29-*, 2026-07-30-0730), выполняющей `/babygoal`-батч по устранению находок из ДВУХ независимых ревью-отчётов (`docs/reviews/2026-07-30-full-project-review.md` от @oxx — задачи #152-162, и `docs/reviews/2026-07-30-project-review.md` — задачи #163-171). Обе цепочки уже объединены в ОДИН последовательный `blockedBy`-чейн от #152 до #178. **Задачи #152-169 полностью завершены и закоммичены** (17 коммитов, от `262b1be7` до `5e36eb50`) по установленному в сессии протоколу: делегирование `Agent(subagent_type: "sx")` → личная проверка оркестратором (чтение диффа, `go build`, `gofmt -l`, scoped `-race` тесты) → коммит самим оркестратором (никогда не доверяя самоотчёту суб-агента).

**Пользователь явно попросил** (в текущем виде, сформулировано как `/babygoal` с теми же аргументами, что были даны в НАЧАЛЕ этого протокола): "реализуй задачи с помощью @sx, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу". Это переподтверждение уже действующего протокола — контракт `/babygoal` (CronList + TaskList decomposition) уже был выполнен ранее в сессии и остаётся в силе, что я перепроверил вызовами `CronList`/`TaskList` перед продолжением: babysit-cron `33ae5347` (7,22,37,52 * * * *) активен, TaskList корректно декомпозирован.

**Параллельно (побочный тред работы)**: пользователь попросил расследовать, почему сессия `crush run` дважды упала в другом репозитории (`D:\dev\rust\tor-socks5\.crush`, сессия `guard-desc-retry`). Расследование (через `crush sessions why/last/show` + код-ревью суб-агентом типа Explore) выявило: весь процесс `crush.exe` умирал БЕЗ единой строки в `crush.log` (паника Go идёт в os.Stderr, не через slog) дважды — оба раза сразу после вызова инструмента, управляющего асинхронной работой (`agent`-делегирование саб-агенту, и `job_output` чтение фонового job). Корень: горутина в `internal/app/app.go:1150` (`go func(){ app.AgentCoordinator.Run(...) }`) НЕ обёрнута в recover() — задача #154 (эта же сессия) добавила recover() ТОЛЬКО для WS-хендлеров веб-сервера, CLI-путь `crush run` остался незащищён. По итогам заведены задачи **#177** (добавить recover() в горутину агента + путь саб-агента + background.go callbacks) и **#178** (гарантировать что паника попадает в crush.log, а не только в stderr) — обе добавлены В КОНЕЦ существующей цепочки (`blockedBy: #176` и `#177` соответственно), чтобы не пересекаться файлами с уже запланированной работой.

**ТЕКУЩЕЕ СОСТОЯНИЕ НА МОМЕНТ /checkpoint — НЕЗАВЕРШЕНО И НЕ ПРОВЕРЕНО:** задача **#170** (пачка P3.1+P3.6+P3.4 — приватность HTTP body-логов, require() в горутинах session_test.go, SHA-256 для npm cache-ключа; P3.3 про ширину call tree намеренно пропущена — я решил, что существующий комментарий в querier.go:43-64 уже достаточно обосновывает design decision, не нужно ничего менять) была отправлена суб-агенту `Agent(subagent_type: "sx", isolation: not worktree)` МОМЕНТ НАЗАД. Мой foreground-ответ был прерван пользователем ПОЧТИ СРАЗУ после запуска (agentId `afe9d881b89f0c630`), но фоновый агент, похоже, УСПЕЛ начать работу — `git status --short` показывает уже изменённые `internal/log/http.go`, `internal/log/http_test.go`, `internal/session/session_test.go`, `npm/crush/bin/crush.js`. **Я НЕ ПРОВЕРИЛ этот дифф вообще** — не читал изменения, не гонял `go build`/`gofmt`/тесты, не коммитил. Это самое первое, что нужно сделать при возобновлении: либо дождаться завершения фонового агента `afe9d881b89f0c630` (через `SendMessage` или ожидание task-notification), либо, если он уже закончил, немедленно приступить к ревью его диффа по установленному протоколу (читать файлы, `go build ./...`, `gofmt -l`, `go test ./internal/log/... ./internal/session/... -race -count=1`, `node --test npm/crush/bin/*.test.js`) перед коммитом.

Также в рабочем дереве остаются НЕ относящиеся к текущей задаче, давно не тронутые изменения из более ранней части сессии: `internal/agent/templates/coder.md.tpl`, `internal/cmd/claude_crush_fallback_command.md`, `internal/cmd/claude_slash_command.md` (правки `/crush-fallback` и `coder.md.tpl` "under 4 lines" — задача #171 в очереди специально просит содержательно ПЕРЕСМОТРЕТЬ, а не просто закоммитить as-is), и намеренно удалённый `web/dist/.gitkeep` (никогда не коммитить без явного запроса, см. CLAUDE.md).

## Active goal

Нет `/goal` Stop-hook. Babysit-cron `33ae5347` (интервал `7,22,37,52 * * * *`, session-only) активен и мониторит TaskList — подтверждено через `CronList` непосредственно перед этим чекпоинтом.

## TaskList

### in_progress
- #170 P3.1+P3.6+P3.4: приватность body-логов (HTTP debug), require() в горутинах session_test.go, SHA-256 для npm cache-ключа — ОТПРАВЛЕНО суб-агенту (agentId `afe9d881b89f0c630`), диф уже частично материализовался в рабочем дереве, НЕ ПРОВЕРЕН, НЕ ЗАКОММИЧЕН

### pending
- #171 P3.9: пересмотреть незакоммиченные правки coder.md.tpl — «под 4 строки прозы» режет диагностику и review (blockedBy: #170)
- #172 Обновить CHANGELOG.md по итогам задач #152-171 (blockedBy: #171)
- #173 Закоммитить все обновлённые .md файлы (blockedBy: #172)
- #174 Выполнить /checkpoint после завершения всей работы (blockedBy: #173)
- #175 Запустить ревью агента @oh по всей проделанной работе (#152-174) (blockedBy: #174)
- #176 Применить находки ревью @oh (#175) (blockedBy: #175)
- #177 Добавить recover() в горутину агента crush run (app.go) и путь саб-агента (blockedBy: #176) — новая задача, из побочного расследования крашей
- #178 Гарантировать что паника в crush run попадает в crush.log, а не только в stderr (blockedBy: #177) — новая задача, из побочного расследования крашей
- #142 crush-fallback state (persistent sentinel — не завершать)

### recently completed (последние 10)
- #169 P2.6+P2.7: межпроцессный deploy lock + keyset-пагинация transcript (+ исправлена регрессия сборки от #168 в процессе)
- #168 P2.1: контракт immutable config snapshot нарушается
- #167 P2.3: Grant/Deny публикуют outcome ДО atomic Take
- #166 P1.7: cliprovider выбирает WSL bash.exe
- #165 P1.6: учёт токенов недетерминирован
- #164 P1.2+P1.3: config reload держит publishMu во время shell-resolve + PushPopCrushEnv
- #163 P1.1 [КРИТ]: sub-agent не берёт межпроцессный lock своей дочерней сессии
- #162 Ниты N-1..N-10 из ревью @oxx
- #161 M-5: разделить read/write пулы SQLite
- #160 Пачка Low: L-2,3,5,6,7,8

## Decisions

- P3.3 (ширина call tree, из задачи #170) сознательно ПРОПУЩЕНА без делегирования суб-агенту — существующий комментарий в internal/db/querier.go:43-64 уже полностью документирует и обосновывает, почему fan-out не ограничивается (SQLite не может бюджетировать строки внутри рекурсивного члена CTE), и что это осознанный выбор, а не недосмотр.
- Задача #168 (P2.1) при коммите вскрыла реальную регрессию сборки: удаление `ConfigStore.SetupAgents()` как "dead code" (по ошибочному выводу из недостаточно тщательного grep — не проверил, что `cfg` в трёх местах coordinator_test.go это `*ConfigStore`, а не локальный `*Config`) сломало `internal/agent` тесты. Исправлено В РАМКАХ задачи #169 (не отдельным коммитом): метод восстановлен, но теперь реализован через copy-on-write (`updateConfig`), а не мутацию published-снапшота на месте — то есть регрессия исправлена И одновременно исходная P2.1-находка для этого метода закрыта корректно.
- Побочные находки о двойном креше `crush run` в tor-socks5 оформлены как НОВЫЕ задачи (#177, #178), добавленные в КОНЕЦ существующей цепочки (после #176), а не встроены посередине — чтобы не конфликтовать файлами с уже запланированной работой над #170/#171 (app.go не тронут этими задачами).
- Один из фоновых верификационных прогонов (`go test ./internal/agent/... -race`, `./internal/cmd/...` и т.д.) несколько раз получал статус `killed` (не `failed`) от системы фонового исполнения без видимой причины — трактовано как проблему окружения/ресурсов, не связанную с диффом; в таких случаях полагались на уже пройденную verification сузкого/целевого скоупа (per-package) как достаточную, не переповторяя бесконечно.

## Open questions

- Диф задачи #170 (http.go/http_test.go/session_test.go/crush.js) — дождаться завершения фонового агента `afe9d881b89f0c630` и провести полную проверку (чтение диффа + build + gofmt + тесты) ПЕРЕД коммитом — это не решено, а прервано на середине.
- Судьба трёх незакоммиченных .md/.tpl файлов (`coder.md.tpl`, `claude_crush_fallback_command.md`, `claude_slash_command.md`) — решение отложено до задачи #171 (которая явно просит содержательно пересмотреть coder.md.tpl, а не механически закоммитить).
- Нужно ли после #178 (новые задачи про crash-safety) продолжать искать похожие непокрытые recover()-пробелы в других горутинах проекта, или ограничиться тем, что уже найдено расследованием — не решено, будет видно после #177/#178.

## Repo state
```
 M internal/agent/templates/coder.md.tpl
 M internal/cmd/claude_crush_fallback_command.md
 M internal/cmd/claude_slash_command.md
 M internal/log/http.go
 M internal/log/http_test.go
 M internal/session/session_test.go
 M npm/crush/bin/crush.js
 D web/dist/.gitkeep
?? docs/checkpoints/2026-07-29-2018.md
?? docs/checkpoints/2026-07-29-2124.md
?? docs/checkpoints/2026-07-30-0730.md
?? docs/reviews/
```
```
5e36eb50 fix: cross-process deploy lock, keyset transcript pagination, SetupAgents regression
a6871307 fix(config): stop mutating published config snapshots in place
417c6510 fix(permission): take before publish/persist so only the race winner acts
4ddfc635 fix(cliprovider): resolve bash via the WSL-aware PATH lookup, not bare exec.LookPath
07f0cd4b fix(agent): stop title generation from racing main-turn token counters
682cf21a fix(config): stop holding publishMu during shell-resolve, stop mutating process env
```

Примечание: `internal/log/http.go`, `internal/log/http_test.go`, `internal/session/session_test.go`, `npm/crush/bin/crush.js` — незакоммиченные изменения от фонового суб-агента задачи #170, ЕЩЁ НЕ ПРОВЕРЕНЫ оркестратором. `docs/reviews/` (оба отчёта) и уже существующие чекпоинты — не тронуты. `web/dist/.gitkeep` — намеренное состояние, не коммитить без явного запроса.
