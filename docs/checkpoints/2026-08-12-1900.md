# Checkpoint — 2026-08-12 19:00 [follow-up-review-round-in-flight]

## Session summary

This session closed the entire 2026-08-12 post-fix-release-readiness round (10 findings, tasks
#385-#403), merged 5 worktree branches into `main`, pushed to `fork/main`, and iteratively fixed
CI failures until the whole pipeline (build × 3 OS, lint, security) went green at commit `3a145e60`.
That push cycle surfaced and fixed several real defects beyond the original round: a genuine race
in `ConsumeInterruptInjectAndEnqueue` (matched "oldest row" instead of the specific peeked row), a
vacuous P0-3 test that closed the whole DB and couldn't distinguish old from new behavior (rewritten
against a real fake-SSE model), a real nil-pointer panic in `buildProvider`'s CLI-provider
auto-discovery branch (reachable on any dev machine with `claude`/`gemini` CLIs on PATH, silent on
clean CI), and two P1-2 timing tests that were flaky even in isolation under load (widened from
200-500ms TTL to 2s, confirmed 15-30 consecutive passes).

Immediately after, the user ran `/oh` against a NEW review —
`docs/reviews/2026-08-12-post-fix-release-readiness-follow-up.md` — which re-audited the just-pushed
`3a145e60` and returned **NO-GO** with three NEW release blockers on top of the previous round's
fixes. I personally verified 5 of the review's key claims against the actual code before trusting it
(all 5 confirmed, including a finding against a comment I wrote and reviewed myself last round) and
created 12 tasks (#404-#415) with dependency ordering matching the review's own recommended fix
order. Per explicit user answers to a clarifying question: P2-4 (the review's architectural
recommendation to unify the fragmented call-lifecycle state machine) was split into a read-only
`@ox` research task (#414, now complete) and an implementation task (#415), and the round is being
executed via parallel git worktrees (as in the prior round) rather than fully sequential.

`@ox`'s research (design doc: `docs/design/2026-08-12-state-machine-unification.md`) recommends
**against** gating the release on the full refactor — point fixes close the three blockers in days,
and the target state-machine model only structurally closes 2 of 3 P0 findings (P0-1, P0-3) and 1 of
4 P1 findings (P1-4), not P0-2/P1-2/P1-3/P2-1. It also found a genuinely new, previously-undocumented
bug: `agent.runTurn`'s `shouldSummarize` branch can silently lose a post-compaction continuation
because `mailbox.submit`'s `FromDurableQueue` guard drops it, and the pump then `Ack`s (deletes) the
durable row as if it succeeded. Per user instruction, I wrote a deferred plan doc
(`docs/plans/2026-08-12-state-machine-unification-plan.md`, NOT authorizing work to start), deleted
task #415 from the active list, and committed all three new docs (review + design + plan) as commit
`f2869afa`.

Three `/crush` background sessions are running in parallel worktrees for the three P0 blockers:
`fix/p0-1-interrupt-owner` (double mailbox/durable ownership), `fix/p0-2-checkpoint-publish` (stale
partial publish + generation data race), `fix/p0-3-orphan-durable` (orphan fallback durability +
SEC-1 prompt logging). **At the moment this checkpoint was written, the P0-3 session had just
completed** (task-notification arrived mid-checkpoint) — its worktree (`D:/dev/go/crush-wt-p03-durable`)
shows changes to `internal/agent/agent.go`, `internal/session/session.go`, `internal/db/*` (a new
`orphan_call_outbox` migration + generated sqlc code — consistent with the durable-outbox approach the
prompt suggested), **but ALSO modified `internal/agent/cliprovider/provider.go`, which was explicitly
out of scope for this task, and DELETED `internal/agent/p0_3_orphaned_recovery_test.go`** (the
carefully-rewritten, personally-verified non-vacuous test from the prior round) in favor of a new
`p0_3_sec1_orphan_outbox_test.go`. **This has NOT yet been reviewed — it is the very next thing to
do on resume.** The `cliprovider/provider.go` touch and the test deletion are both scope violations
or at minimum need a clear justification in the delegate's own report before acceptance; do not
assume they're fine without reading the diff and the delegate's stated reasoning first.

P0-1 (`crush-wt-p01-owner`) and P0-2 (`crush-wt-p02-publish`) were still running (empty `.out` files)
as of this checkpoint.

## Active goal

None (no `/goal` Stop hook armed). No `/babysit` cron currently active for this session — the prior
round's babysit self-deleted when its TaskList went empty; a fresh one has NOT been re-armed for this
new round's tasks (#404-#414). Consider `/babysit` if the user wants unattended progress monitoring
resumed.

## TaskList

### in_progress
- #404 P0-1: cross-process interrupt имеет двух владельцев — durable row И mailbox replacement
- #405 P0-2: late checkpoint публикует stale partial event (UI «зависает») + реальный data race в fencing
- #406 P0-3 + SEC-1: orphan fallback не даёт durability guarantee и логирует полный prompt в ERROR
  (underlying `/crush` session just completed — needs zero-trust review, see summary above)

### pending
- #407 P1-1: lease fail-closed срабатывает уже ПОСЛЕ возможного истечения lease
- #408 P1-3 + P1-2: неатомарный config snapshot и 401-retry со старым pinned client
- #409 P1-4: interrupt ticker не соответствует lifecycle turn, не join-ится и пропускает второй interrupt (blockedBy: #404)
- #410 P2-1: FIFO pending injects неоднозначен в пределах одной секунды
- #411 P2-2: удалить мёртвые interrupt recovery paths, живущие только ради тестов (blockedBy: #404, #409)
- #412 P2-3: привести doc-комментарии concurrency-кода в соответствие реальному контракту (blockedBy: #404, #405, #406, #409)
- #413 Release gate: составная проверка 9 свойств из ревью (blockedBy: #404, #405, #406, #407, #408, #409, #410, #411, #412)

### recently completed
- #414 P2-4 (research): спроектировать единую state machine — через @ox

### deleted
- #415 P2-4 (implementation) — deleted per explicit user instruction; captured instead in
  `docs/plans/2026-08-12-state-machine-unification-plan.md`

## Decisions

- Split P2-4 into research (#414, `@ox`, read-only) + implementation (#415), then per explicit user
  instruction converted #415 into a deferred plan document instead of a live task — implementation
  is not authorized to start without a fresh go-ahead.
- Chose parallel worktrees for the first wave (3 blockers: #404/#405/#406) rather than all 6
  independent tasks at once, specifically because every worker runs `go test -race` and this
  machine gives false timing failures under heavy concurrent `-race` load (established repeatedly
  in the prior round) — 3 concurrent `-race` runs is the chosen ceiling, not 6.
- Personally verified 5 of the follow-up review's key claims against actual code before trusting it
  and creating tasks from it (all 5 confirmed) — continuing this session's zero-trust discipline
  applied to the review itself, not just to delegated fixes.
- Wrote every delegation prompt with explicit scope boundaries between the 3 parallel P0 workers
  (who owns which file/region of `agent.go`), an explicit git-write ban (orchestrator commits after
  review), a mandatory revert-check requirement with a named predicted symptom, and an explicit
  warning about the global-config-leak/CI-panic hazard found during the previous push cycle — all
  lessons carried forward from concrete failures earlier this session.

## Open questions

- User was asked whether to verify the newly-found compaction-continuation-loss bug (§O-2 in the
  design doc) and file it as its own task now, or leave it captured in the deferred plan doc for a
  future round. **Not yet answered by the user as of this checkpoint** — the `/checkpoint` command
  arrived before a reply.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
```

```
f2869afa docs: follow-up release-readiness review, P2-4 state machine design + deferred plan
3a145e60 fix(agent,session): close test-only environment/timing hazards found while pushing
c5075988 fix(message): remove empty branch flagged by staticcheck (SA9003)
dc3b7fc5 chore(agent): closing @oh review cleanup — log wording, remove vacuous test
4c9e8b75 docs: changelog entry and checkpoint for the post-fix release-readiness round
```

Active worktrees (as of this checkpoint): `D:/dev/go/crush-wt-p01-owner` (branch
`fix/p0-1-interrupt-owner`, session `p0-1-interrupt-owner`, still running), `D:/dev/go/crush-wt-p02-publish`
(branch `fix/p0-2-checkpoint-publish`, session `p0-2-checkpoint-publish`, still running),
`D:/dev/go/crush-wt-p03-durable` (branch `fix/p0-3-orphan-durable`, session `p0-3-orphan-durable`,
**just completed, unreviewed** — see summary above for the scope-violation flags to check first).
