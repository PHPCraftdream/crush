# Checkpoint — 2026-08-05 17:04

## Session summary

Continuation of the release-stabilization round on the `crush` fork (`D:\dev\go\crush`). This checkpoint picks up right after the release gate (#309) was closed **GO**.

The remaining P0-4 residual (#312 — cross-process compaction not serialized) was closed this session in two rounds. The first delegated draft was rejected: it took the OS session lock via `defer`, which stayed held across the tail call to `a.Run()` for a drained queued message, so the process rejected itself with `SessionLockBusyError` naming its own PID — reproduced independently by forking an existing test with `DataDirectory` set (the original test left it empty and was structurally blind to the whole fix; the full package had passed green under `-race` for 171s with the defect in place). The second draft fixed the self-lock but shipped a vacuous inline-deadlock test (`ContextWindow: 100` never actually crossed the auto-summarize threshold) and skipped the required drain-path regression test entirely — both written and verified independently, landed in `d4f19852`.

The release gate itself (#309) then ran two audit rounds via `crush run`/`@sh`. Round 1 gave a **GO** verdict that its own table didn't support — 8 of 15 rows were unrevert-checked, including two entire scenarios (lock cleanup P0-5, and summarize mutual exclusion P268) dismissed with an unverified "revert impossible" excuse. That excuse was wrong: both had trivial one-line revert points, found in under a minute. Round 2 was launched to close those gaps but died mid-verification when `crush`'s weekly/monthly limit was exhausted, leaving a deliberately-broken `beginCompact` guard uncommented in the tree — restored by hand, tree confirmed clean.

`/crush-fallback` engaged automatically per the standing armed instruction (task #261): triggered by the hard-limit exhaustion (not peak-hours — no cron scheduled, resets 2026-08-06 17:01, ends only via explicit `/crush-fallback clear`). Remaining gate work routed through `@sh` sub-agents instead: scenario 6 (lock cleanup, 6 tests) and scenario 8a (P268, 5 tests) were both revert-checked in a single `@sh` run and independently verified (tree restored, both full suites green under `-race`). Two more gaps were closed via separate `@sh` delegations (#322, #323): scenario 3a's revert evidence was upgraded from a bare panic to a reasoned stack-trace explanation (the panic is a direct, in-branch consequence of removing exactly the guard under test — verified independently by re-reading the cited line); and scenario 5's real hole (#302 — an abandoned title-generation goroutine's deferred fallback clobbering a later turn's real title with `DefaultSessionName`) got an actual regression test, `TestGenerateTitle_AbandonedFallbackDoesNotClobberLaterTitle`, whose revert-failure I reproduced myself before committing (`f3489c4d`).

With all 8 gate scenarios now genuinely revert-checked (with two documented nuances: 3a's evidence is panic-based-but-explained, and P268 has 3-of-5 direct hits with the other 2 covering different invariants of the same function), #309 was marked **completed** and the wrap-up chain unblocked. #303 (this checkpoint) is now in progress; #304 (CHANGELOG), #305 (commit all .md), #306 (`@oh` review) remain queued behind it.

`/babysit` cron is armed (job `025b1422`, every 15 min, session-only — re-arm with `/babysit` if the session restarts and it's gone). The peak-hours-by-weekday feature (originally #314-318) was deliberately removed from the TaskList and now lives only as a standalone plan at `docs/plans/peak-hours-weekdays.md` — resuming it requires manually recreating the five tasks from that file.

One live anomaly worth carrying forward: `crush sessions locks` has shown `offline` for provably-alive processes (confirmed via `tasklist`) at least twice more this session, on top of the original #321 observation — the root cause is still unestablished.

## Active goal

Standing `/babygoal` Stop-hook condition (verbatim, still in force):

```
реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит
всю работу
```

## TaskList

### in_progress
- #303 [Wrap-up 1/4] /checkpoint после того как все блокеры закрыты

### pending
- #261 crush-fallback state (persistent — do not complete): sh — ACTIVE (hard-limit), switch back with /crush-fallback clear (sentinel, not real work)
- #262 [LOW] Round 8: attachmentsDataDir has zero test coverage, reverting #248 stays green
- #263 [LOW] sessions pick has the same --data-dir-forwarding bug #247 just fixed in queue run
- #264 [LOW] sessions_why's two "genuinely dead PID" branches print byte-identical text
- #274 [P2-1] Same-named attachments uploaded in the same second silently overwrite each other
- #275 [P2-2] Concurrent watchdog dumps from one process collide on the same filename
- #276 [P2-3] Hard-cap watchdog log reports idle duration instead of wall-clock elapsed
- #304 [Wrap-up 2/4] Обновить CHANGELOG за раунд (blockedBy: #303)
- #305 [Wrap-up 3/4] Закоммитить все .md (blockedBy: #304)
- #306 [Wrap-up 4/4] Запустить ревью @oh (blockedBy: #305)
- #311 [OPEN QUESTION] Убрать синхронную компакцию с пути ответа, не открывая заново гонку
- #313 [LOW] Проверить, защищён ли internal/shell от grandchild-wedge так же, как cliprovider
- #319 [LOW] message.CreatedAt имеет гранулярность в секунду, isAfter разрешает ничью по случайному UUID
- #320 [LOW] TestStreamWatchdog_* падают под нагрузкой
- #321 [MEDIUM] sessions locks показывает offline для живой сессии

### recently completed
- #309 Релизный гейт: 8 обязательных регрессионных сценариев — **GO**
- #322 Revert-проверка сценария 3a (CancelAll) без опоры на панику
- #323 Дефект #302 установлен и покрыт регрессионным тестом
- (P0-4 residual, #312) Кросс-процессная компакция сериализована — `d4f19852`

## Decisions

- **#312 второй черновик принят, первый отклонён.** Первый держал OS-лок через `defer` через хвостовой `a.Run()` — самоблокировка процесса. Второй освобождает лок явно до драйна очереди.
- **Вердикт GO раунда 1 гейта отменён.** Зелёная таблица без revert-проверки не считается доказательством; переоткрыт до фактической проверки всех 8 сценариев.
- **Отклонено обоснование «revert невозможен» для сценариев 6 и 8a.** Обе точки отката оказались тривиальными однострочными правками, найденными за минуту независимой проверки.
- **#314-318 (пиковые часы по дням недели) сняты с TaskList**, живут только в `docs/plans/peak-hours-weekdays.md` — решение не дать `/babysit` подхватить фичу автоматически после закрытия гейта.
- **Фоллбек на `@sh` — не по пиковым часам, а по жёсткому лимиту.** Cron на автовозврат не заводится (в отличие от peak-hours триггера); возврат к `/crush` только явной командой `/crush-fallback clear`, вероятно не раньше 2026-08-06 17:01.

## Open questions

- **#311** — убирать ли синхронную компакцию с пути ответа. Позиция оператора («кажется, синхронность нужно устранять») прозвучала до разбора @fm и не переподтверждена.
- **#321** — корень ложного `offline` в `sessions locks` для живых процессов всё ещё не установлен, несмотря на повторные наблюдения.
- **Стешей на момент этого чекпойнта не осталось** (все более ранние — из предыдущего чекпойнта — были либо применены, либо признаны устаревшими в прошлых раундах; не проверялось заново в этом чекпойнте — если `git stash list` покажет что-то, разобраться перед использованием).

## Repo state

```
 D web/dist/.gitkeep
?? dev/
?? docs/checkpoints/2026-08-04-2150.md
?? docs/checkpoints/2026-08-05-1446.md
?? docs/plans/peak-hours-weekdays.md
?? internal/agent/mailbox.go.tmp.2524.ec6bfba9866c
```

(`web/dist/.gitkeep` deletion is long-standing and must NOT be committed. `internal/agent/mailbox.go.tmp.2524.ec6bfba9866c` is untracked leftover cruft from an earlier agent session in this same conversation — not touched, ownership unclear, safe to ignore or delete at the operator's discretion.)

```
f3489c4d test(agent): close the release-gate hole on #302 — abandoned title fallback must not clobber a later title
d4f19852 fix(agent): serialize manual compaction across processes without locking the process out of its own drain (P0-4 residual)
5f707abb fix(agent): make compaction report its progress to both the heartbeat and the stream watchdog (P0-4 follow-up)
95a7322c refactor(agent): migrate QueueMessage off the legacy queue, completing the mailbox migration (stage 2.6)
5b2f3ff0 fix(agent): an interrupt between turns no longer kills the dispatcher, and no longer eats a queued message (P1-2 follow-up)
```
