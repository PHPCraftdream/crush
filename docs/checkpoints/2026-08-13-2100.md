# Checkpoint — 2026-08-13 21:00 [oh-review-round-closed-ci-flakes-followup-filed]

## Session summary

Continues directly from `2026-08-13-1615.md`. That checkpoint left the
H2/H3/H4/M6/M7 fixes committed (`b0c35f58`) and README (`caf2c7b1`)
committed, with the user's explicit answer "пуш, ci" — push and let real
CI settle the open B1 disagreement (whether `TestP1_4_BoundedWorkerPoolRespectsLimit`
genuinely flakes ~10% on real CI, per `@oh`'s claimed blocker).

**B1 is now resolved: not reproduced on real CI.** ubuntu-latest's
`internal/session` package passed clean on the very first CI run after
pushing (`31714546616`). Combined with this session's 85+ failed local
reproduction attempts, the working conclusion is that `@oh`'s B1 finding
was a misattribution, most likely of an already-known-flaky, unrelated
test's failure. This is documented, not silently dismissed — see the
CHANGELOG entry and this checkpoint's Decisions section.

**What happened after that, in order** (5 separate push+CI-watch rounds):

1. First push (`8f731e3c`, containing `b0c35f58` + `caf2c7b1`): CI RED, but
   NOT on B1 — two different, review-unrelated tests failed on ubuntu-latest
   (`TestP0_338_FinalizerReachableDespiteHungCleanup`, a 200ms margin that
   hit 371.9ms) and macos-latest (`TestAbandonOwnershipWithHandoff_ManualCompactionSuccess_UsesPlainAbandon`,
   "expected 3, actual 0"). User chose "investigate now" over accepting or
   deferring.
   - P0-338: root-caused as a binary property (fast path vs. a 2s injected
     hang) with an unnecessarily tight bound; widened 200ms→1s. Verified
     10/10 clean.
   - ownership-handoff: investigated deeper (traced to `agent.go`'s Run()
     defer always calling `abandonOwnershipWithHandoff`, which pops the
     mailbox queue if the epoch still matches). Formed a hypothesis
     (Cancel() landing before `beginGeneration` causes an early-bailout
     path that clears the queue) and TESTED it empirically rather than
     trusting the theory — the "fix" (wait for `mb.current.cancel != nil`
     before Cancel) made the test fail DETERMINISTICALLY (30/30) instead
     of rarely. Reverted with a comment documenting the finding; left as
     an open, unresolved CI flake (task #443, disposition: investigated,
     not fixed, real mechanism still unknown).
   - Committed as `dcb49d11`.

2. Second push (`dcb49d11`): ubuntu-latest and macos-latest BOTH green
   (confirms this round's actual H2-M7 changes and the P0-338 fix are
   solid). windows-latest failed on two DIFFERENT tests:
   `TestReleaseGate_P0_2_LockBusyNeverExhaustsRetries` and the
   already-once-verified-"fine"-in-isolation `TestReleaseGate_P350_QueuedNotExecutedBacksOffWithoutAttemptPenalty`.
   Both share an identical shape: `require.Eventually(..., 5*time.Second, ...)`
   waiting for 25 successful async pump-tick cycles. Since this is an
   async-COUNT wait (not a precise timing assertion), widening cannot mask
   the regression each test exists to catch — widened both 5s→20s.
   Verified 10/10 each locally, full package clean. Committed as `deb3047a`.

3. Third push (`deb3047a`): ubuntu-latest green. windows-latest STILL
   failed, now on `TestOrphanOutbox_ConcurrentExecution` — a test already
   confirmed 15/15 clean in local isolation earlier this session (before
   any of today's fixes), now failing on real CI with an entry stuck
   'pending' after being successfully drained.
   - First fix attempt: replaced a fixed `time.Sleep(500ms)` with
     `require.Eventually` polling both queues empty, then `Stop()`.
     Flaked 2/15 LOCALLY (worse than the original in isolation) — traced
     the real bug: a leased-but-not-yet-executed entry reads as "pending
     count == 0" too (status is 'leased', not 'pending'), so the
     Eventually could fire while the drained entry was merely leased, and
     `Stop()` raced that still-in-flight lease.
   - Second, correct fix: replaced the shared `mockCoordinator` with a
     local `countingCoordinator` tracking real `Run()` invocations; wait
     for `calls >= 2` (both entries GENUINELY executed) before `Stop()`.
     Verified 40/40 clean locally, full package clean. Committed as
     `a009159b`.

4. Fourth push (`a009159b`): ubuntu-latest and macos-latest green AGAIN.
   windows-latest failed YET AGAIN — this time
   `TestReleaseGate_P350_QueuedNotExecutedBacksOffWithoutAttemptPenalty`
   and `TestReleaseGate_P0_2_LockBusyNeverExhaustsRetries` both failed a
   SECOND time, now taking 24-25 seconds — EXCEEDING the just-widened 20s
   bound — plus a brand-new third test
   (`TestReleaseGate_P350_NoDuplicateDispatchForSameSession`) also failed.

At this point, presented the pattern to the user directly: 5 rounds of
push+fix, margin-widening no longer converging (widened bounds themselves
now being exceeded), new distinct tests surfacing each run. User chose
to **stop chasing individual fixes and close the round** rather than
keep going — this is NOT a silent abandonment: filed task #447 to
capture the open question (transient GH Actions contention vs. a deeper
systemic issue specific to windows-latest under `-race` + full-suite
load) for later, dedicated investigation.

**Net result of this round's CI-flake chase**: 3 real fixes shipped
(P0-338 margin, P0_2/P350 margin widening, orphan-outbox real
pending-vs-leased bug), 1 investigated-and-safely-reverted non-fix
(ownership-handoff), 1 open disagreement resolved in this round's favor
(B1, not reproduced), 1 new follow-up task filed for the broader pattern
(#447, windows-latest CI reliability).

## Active goal

None (no `/goal` Stop hook armed). This checkpoint marks the intended
close of task #435's `/babygoal`-established closing sequence
(checkpoint + changelog + commit md + `@oh` review). A third `@oh` review
was explicitly considered and NOT run this round — the user's explicit
choice, given B1 is now resolved via real CI and the round's remaining
open items (windows-latest flakiness) are captured as a separate,
non-blocking follow-up task rather than something a fourth review pass
would meaningfully add to.

## TaskList

### pending
- #440 P0-4 follow-up: orphan outbox attempts/exhaustion tracking
  (M5/M8) — non-blocking, unchanged
- #447 Investigate systemic windows-latest CI flakiness under -race —
  new this round, non-blocking, captures the open pattern from the 5
  push+CI rounds above
- **#421** [ВАЖНО, ОТЛОЖЕНО] P0-1 regression — unchanged, still awaiting
  a user architectural decision

### recently completed
- #435 `@oh` review of this round's work — CLOSED this checkpoint. All
  actionable findings (H2/H3/H4/M6/M7) fixed, verified, committed,
  pushed, and confirmed clean on ubuntu-latest + macos-latest CI. B1
  (the only declared blocker) resolved as not-reproduced on real CI.
- #442-#446 — the five CI-flake investigate/fix tasks from this
  checkpoint's session summary above
- #441 README.md rewrite (already committed prior checkpoint)

## Decisions

- B1 resolved as "not reproduced" based on converging evidence: 85+
  local isolated attempts (prior checkpoint) PLUS a clean real-CI
  ubuntu-latest `internal/session` run (this checkpoint) — not a single
  data point, a second independent confirmation. Documented in
  CHANGELOG as an open disagreement with `@oh`'s finding, not silently
  resolved as if `@oh` was simply wrong.
- For ownership-handoff (#443): chose to test the hypothesized fix
  empirically before trusting the theory, exactly as this session did
  for M7 earlier — found the fix made things deterministically worse,
  reverted rather than ship a confidently-wrong change. This is the
  SECOND time this exact discipline (test the fix, don't just reason
  about it) caught a would-be regression this session (M7 was the
  first).
- For the orphan-outbox test (#446): same discipline again — first fix
  attempt (poll pending-count) looked correct by reasoning alone but
  flaked 2/15 empirically; root-caused the REAL ambiguity (leased reads
  as pending-empty) and fixed it properly with a call-counter, verified
  40/40.
- Stopped the CI-flake chase after 5 rounds per explicit user direction,
  rather than continuing indefinitely — filed #447 as the honest
  acknowledgment that windows-latest's reliability under this test
  suite's `-race` load is an open, unresolved, and now clearly
  recurring pattern, not something resolved by this session's margin
  fixes alone.
- Did not run a third `@oh` review this round — B1 was resolved via
  direct CI evidence (a stronger signal than another local-only review
  pass would add), and the remaining open item (windows-latest CI
  flakiness) is explicitly filed as separate follow-up work rather than
  something in scope for the review to re-litigate.

## Open questions

- **#421** — unchanged, still open.
- **#440** (M5/M8 orphan-outbox recovery/exhaustion tracking) —
  unchanged, still open, non-blocking.
- **#447** (windows-latest CI reliability under -race) — genuinely open:
  is this a transient GitHub Actions infrastructure issue (busy shared
  runners on the day this session ran) or a real, repo-specific
  resource-contention pattern (DB connection pool exhaustion under
  sqlite+race, goroutine buildup, etc.)? Needs dedicated investigation,
  not resolved by this session's individual test fixes.

## Repo state

```
 D web/dist/.gitkeep
?? dev/
```
(CHANGELOG.md and this checkpoint file are about to be committed as the
final act of this round.)

```
a009159b fix(session): make TestOrphanOutbox_ConcurrentExecution wait on real completion (task #446)
deb3047a fix(session): widen 5s->20s async-wait margin in two CI-flaky tests (tasks #444/#445)
dcb49d11 fix(agent): widen CI-flaky margin, investigate+revert a worse "fix" (tasks #442/#443)
8f731e3c docs: checkpoint session state after committing @oh review-response fixes (task #435)
caf2c7b1 docs(readme): reposition as agent-driven CLI, point install/links at fork (task #441)
```

All five commits above are already pushed to `fork/main`
(`https://github.com/PHPCraftdream/crush`). ubuntu-latest and
macos-latest are confirmed green on the latest push (`31740244975`);
windows-latest remains red on that same push (tracked by #447, not
blocking this round's close).
