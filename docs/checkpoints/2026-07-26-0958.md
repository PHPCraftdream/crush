# Checkpoint — 2026-07-26 09:58

## Session summary

This session resumed from `docs/checkpoints/2026-07-19-0729.md` and covered
three major phases, all now landed as 13 local commits on `main` (none
pushed, no CI monitored — separate explicit step, not yet requested).

**Phase 1 — multi-CLI `/crush` installers** (already committed before this
checkpoint's active window began, but part of the same unbroken session):
`/crush-fallback <agent>` peak-hours failover command (mirrors the existing
`PeakHoursError`/`PeakHoursGuidance` pattern — force-finish the turn with a
ready-to-run resume command, since `crush run` can never block mid-turn for
input) plus `codex-init`/`grok-init`/`gemini-init`/`qwen-init` (+`-del`)
commands installing `/crush` + `/crush-fallback` into Codex CLI Skills,
Grok Build CLI Skills, Gemini CLI TOML commands, and Qwen Code CLI Markdown
commands respectively, all converting from the same canonical source via a
new shared `internal/cmd/multi_cli_convert.go`.

**Phase 2 — upstream triage (v0.84.0→v0.87.0, 176 commits)**: investigated
thoroughly by reading actual diffs, not commit titles. Found and corrected a
stale `CLAUDE.md` claim — `internal/lsp/` does not exist in the fork at all
(confirmed via grep, zero imports anywhere) — added a REMOVED-subsystem row
documenting this. Added a new "Multi-client / multi-session" caution: the
fork's lock-file+heartbeat session engine predates and is architecturally
distinct from upstream's multi-client model, so session/dispatch-race fixes
in shared code (`coordinator.go`, `agent.go`) are EVAL-not-PORT by default
now. Wrote `docs/plans/2026-07-19-upstream-triage-and-question-tool.md` as
the full triage plan, then executed it via `/babygoal` + parallel `sh`
agents: 5 low-risk ports (system-wide `/etc/crush/crush.json` config path,
`OAuthClient`/`TokenExchangeError` types, Go-native coreutils shell
fallback, MCP stdio process-group reaping — this last one's Windows
follow-up investigation found and fixed a *real*, previously-unknown
tree-kill gap in `internal/agent/tools/mcp/process_other.go`, same bug class
as `internal/shell/exec_windows.go`'s earlier fix) and 2 EVAL verdicts
(coordinator dispatch race: **not reproducible** in the fork's
`TryAcquireSessionLock` OS-mandatory-lock model, documented with full
evidence, no code changed; edit-tool refactor: upstream's raw patch
conflicted with fork-specific `AtomicWriteFile`/warn-on-mtime changes, hand-
merged the dedup pattern on top instead of forcing the raw diff).

**Phase 3 — question tool feature** (fully designed and implemented end to
end this session): `ask_question` agent tool force-finishes the turn
(mirrors peak-hours mechanism exactly) instead of blocking, since neither
headless `crush run` nor web sessions can pause mid-turn for synchronous
input (verified directly from `internal/server/handlers.go` — both
unconditionally auto-approve permissions, no blocking channel exists
anywhere in the codebase; an earlier design assumption to the contrary was
caught and corrected before implementation). `crush run --json` now reports
`exit_reason: "awaiting_answer"` (previously fell through to generic
`"error"`), carrying the question + a ready-to-run
`crush run --session <id> "<answer>"` resume command in `.error`. Web UI
gained `AskQuestionBlock` (clickable option chips + an always-visible inline
free-text field + dismiss) — detection is a documented, honestly-flagged
text-heuristic (no structural wire signal exists yet). `/crush`'s canonical
doc gained a new section on reacting to this exit_reason.

**Phase 4 — reviewer/worker model roles** (new feature, only 1 of 3 planned
pieces landed): user wants 4 model roles instead of 2 — `reviewer`
(strongest, invoked only explicitly, never auto-selected), `smart`
(=large, default, delegates hands-on work when worker is configured),
`worker` (cheap, new — sub-agent tool should prefer it over the parent's own
model when parent=smart and worker is configured), `fast` (=small). After
being asked to clarify scope, user chose the fuller option: extend
`config.SelectedModelType` properly (not just add CLI override flags).
Landed: `SelectedModelTypeWorker`/`SelectedModelTypeReviewer` constants,
`--role worker`/`--role reviewer` vocabulary in `resolveModelRole`,
`crush_info` tool's model display extended. **Explicitly flagged and left
undone**: `internal/cmd/run.go`'s `roleLarge` boolean still treats anything
non-large as "small" for actual resolution, so `--role worker`/`--role
reviewer` currently parse successfully but silently resolve to the small
model config — this is exactly what the next task must fix. User then
explicitly said: stop all further autonomous work once this task lands, and
delete all cron jobs — both done (babysit cron `0850077e` deleted, tasks
#59/#60 left `pending`, not started).

A pre-existing, non-regressed environmental issue was reconfirmed multiple
times this session: `internal/agent/cliprovider`'s
`TestStreamWaitBoundedOnGrandchildHoldsStderr`/
`TestStreamKillUsesTreeKillStillTerminatesChild` fail consistently — proven
via `git stash` to also fail on a clean pre-session tree, so not caused by
any of this session's work. Left untouched, not investigated further, per
the standing "don't touch machine-level state without being asked" rule
from earlier sessions.

## Active goal
None. A `/goal` was set for the `/babygoal` work ("реализуй таски с помощью
агентов @sh, всё покрывай тестами, между тасками делай коммиты") and was
explicitly cleared by the user (`/goal clear`) partway through, once they
decided to pause after task #58.

## TaskList
### pending
- #59 Reviewer/worker roles: dynamic sub-agent model resolution (prefer Worker when parent is Smart)  (blockedBy: none, #58 done)
- #60 Reviewer/worker roles: conditional "delegate to worker" prompt instruction  (blockedBy: #59 not formally, but logically depends on it landing first)

### recently completed
- #58 Reviewer/worker roles: extend SelectedModelType + --role vocabulary
- #57 Question tool: document exit_reason "awaiting_answer" in /crush slash-command
- #56 Question tool: web UI quick-reply chips
- #55 Question tool: new agent tool + exit_reason "awaiting_answer" in crush run --json
- #54 Question tool: AwaitingAnswerError + AwaitingAnswerGuidance (core, headless path)
- #53 EVAL: edit tool refactor (dedupe find/replace, Sourcegraph result limits)
- #52 EVAL: coordinator dispatch race (verdict: not reproducible, no fix needed)
- #51 Port + verify: MCP stdio process-group reaping (+ Windows tree-kill fix)
- #50 Port: internal/shell/coreutils_exec.go + coreutils_exec_stub.go
- #49 Port: internal/oauth/token.go (TokenExchangeError + persisted OAuthClient)

(#36-#47 — the earlier CLI-installer research/design/implementation batch —
also completed this session but roll off the "recently completed" top-10;
see git log for `4f068779` through `4544233b`.)

## Decisions
- LSP is fully and permanently removed from the fork — `CLAUDE.md` now
  documents this explicitly (REMOVED-subsystem table) instead of the stale
  "owned" reference that pointed at a nonexistent file.
- Session/dispatch races in shared code are EVAL-not-PORT by default now
  (new `CLAUDE.md` rule) — the fork's lock-based session model predates and
  differs architecturally from upstream's multi-client one; don't assume
  their fixes apply without checking.
- Question tool: one unified backend mechanism (force-finish + exit_reason)
  serves both headless and web consumption — rejected an earlier, more
  complex design (separate blocking channel + new WebSocket protocol for
  "interactive" mode) after verifying no such blocking mechanism exists
  anywhere in the codebase for anything, including permissions.
- Reviewer/worker roles: extend `config.SelectedModelType` properly (4 named
  slots) rather than bolt on CLI-only override flags — user's explicit
  choice when given the tradeoff.
- `--role` stays mandatory on `crush run` — user declined to make it
  default to `smart`, reaffirming the existing "burn tokens unintentionally"
  guard rather than relaxing it.
- Stop autonomous work and delete all crons once #58 landed — explicit user
  instruction, executed exactly (#59/#60 left untouched, not even queued for
  a future auto-resume).

## Open questions
- **#59/#60 (reviewer/worker resolution + prompt injection)** — fully
  designed and described in the TaskList, not started. Needs the user's
  explicit go-ahead to resume (per the "stop all work" instruction that
  ended this session's active work).
- **Push + CI for this session's 13 local commits** — not done. Standing
  rule: push is a separate explicit step, never implied by "commit between
  tasks."
- **MCP OAuth** (Phase 2 of the plan doc, "Фаза 2, пункт 4") — explicitly
  deferred pending a separate "do we even want this" decision from the
  user; not queued as a task.
- **npm `0.1.7` publish** and **73 orphaned processes on this machine** —
  both carried over unresolved from the prior (`2026-07-19-0729.md`)
  checkpoint; not revisited at all in this session's active window.
- **`internal/agent/cliprovider` test failures** — confirmed pre-existing/
  environmental (not a regression), not investigated further this session;
  still present as of this checkpoint.

## Repo state
```
 D web/dist/.gitkeep
?? docs/checkpoints/2026-07-13-1520.md
?? docs/checkpoints/2026-07-19-0729.md
?? docs/plans/2026-07-19-upstream-triage-and-question-tool.md
```
```
56a0e40d feat(config): add worker/reviewer SelectedModelType roles
12aa11a8 feat(web): render ask_question as clickable chips + inline answer box
02c27b0a docs(crush-cmd): document exit_reason \"awaiting_answer\" resume flow
77a0e540 feat(agent): ask_question tool + exit_reason "awaiting_answer"
e4ee2385 port(tools): dedupe edit/multiedit find-replace, Sourcegraph limits, grep lines [upstream 4f4b8469, 6b6fab5e, 18c27999]
```
