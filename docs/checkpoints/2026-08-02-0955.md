# Checkpoint — 2026-08-02 09:55

## Session summary

Continuation of a long multi-day session on the `crush` fork (`D:\dev\go\crush`, `PHPCraftdream/crush`). This checkpoint supersedes `2026-08-02-0326.md` (same session, earlier point — that one was written mid-`/babygoal` and never committed; this one covers everything through the end of the second `@oh` follow-up round).

Round 1: implemented tasks #195-202 (2 release blockers + 6 high/medium findings from `docs/reviews/2026-08-01-multi-agent-stability-review.md`, plus a flaky-test fix) via sequential `@sh` sub-agents, one commit per task, each independently re-verified by the orchestrator (full diff read, fresh `go build`/`go vet`/targeted tests, then a full uncached `-race` run of the affected package). Wrote a checkpoint, updated `CHANGELOG.md`, committed the review report + prior untracked checkpoints, then launched `@oh` for an independent, skeptical review of that whole batch.

`@oh` found 4 real, new problems introduced by the fixes themselves (not just style nits):
- Task #200's `toolCleanupGrace` was applied symmetrically to both the parent and the sub-agent child, so it canceled out algebraically and didn't actually fix the parent/child cancellation race it claimed to close.
- `crush sessions reset --force` had the identical stale-PID kill bug just fixed for `sessions kill` in task #198, in a sibling file #198 never touched.
- The turn loop (task #195's `Run`/`runTurn` split) only registered the outer `runCancel` once, before the loop — from the second queued turn onward, `Cancel(sessionID)` during that turn's DB preamble silently no-op'd on a dead placeholder.
- A second, near-identical `t.Parallel()` + mutable-package-global bug (title-generation's timeout, not the DB-preamble one fixed in #202) had the same defect #202 was supposed to have eliminated the whole *pattern* of.

Round 2: fixed all 4 (tasks #204-207) via the same `@sh` + independent-verify + commit discipline, plus a bundle of lower-priority findings tracked as task #208 (not fixed, explicitly deferred — `@oh` itself called these "can ride the next release": misclassified `hardCap` finish-message, a stale comment, one tautological test, a residual narrowed TOCTOU, and missing regression coverage for the queued-after-summarize path).

The user then made two explicit decisions on the two open items from round 1's summary: (1) `--agents single` should NOT override a configured Worker — keep current runtime behavior, but the documentation was found to be actively WRONG (not just incomplete) in three places, including the installed `/crush` slash-command guide an orchestrating Claude Code reads to decide how to invoke `crush run` — all three corrected (task #209, doc-only, no behavior change); (2) stdin must never hang indefinitely under any circumstance — `@oh`'s review had flagged that the #199 stdin fix reopened exactly that risk (bound only the first byte, then unbounded read). Replaced with a real per-chunk idle timeout that resets on every chunk received (task #210) — the third iteration of this particular fix, each iteration's bug found by either a live incident or a review.

A Stop hook (condition: implement via @sh with commits between tasks, then `/checkpoint` + changelog + commit all `.md` + `@oh` review at the end) caught that round 2's own handoff was incomplete — round 1 got the full checkpoint/changelog/md-commit/@oh-review treatment, round 2 (tasks #204-210) only got commits + changelog, no checkpoint, no `@oh` re-review. This checkpoint and the launch of a second `@oh` review (covering the full `e9544a8f..HEAD` range, all 20 commits) are what's completing that gap right now.

## Active goal

Session-scoped Stop hook (not `/goal` — installed via the `/babygoal` invocation's condition text): "реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу." Auto-clears once satisfied; this checkpoint is part of satisfying it.

## TaskList

### in_progress
(none)

### pending
- #192 Heartbeat reports "alive" for a fully deadlocked process (unrelated pre-existing task)
- #208 Lower-priority hygiene from @oh's review of #195-202 (batch, explicitly deferred — see task description for the 5 remaining items and suggested priority)

### recently completed
- #210 Fix stdin named-pipe read: never hang indefinitely, even after the first byte
- #209 Fix --agents single help text to match actual Worker-bypass behavior
- #203 DECIDE: should explicit --agents single override a configured Worker? (resolved: no)
- #207 Stop titleGenerationMaxDuration test from mutating a package global
- #206 Restore runCancel into activeRequests at the top of each turn-loop iteration
- #205 REOPEN #200: toolCleanupGrace is symmetric, does not fix the parent/child race
- #204 BLOCKER: sessions reset --force has the same stale-PID kill bug as #198
- #202 Inject sessionPreambleMaxDuration instead of mutating a package global under t.Parallel()
- #201 Make --timeout-hard-cap an actual hard cap without --timeout-extends-on-progress
- #200 Give parent delegation a cleanup grace above the child's own tool cap

## Decisions

- User: `--agents single` must NOT override a configured Worker — current behavior (Worker always wins) is correct and stays; the bug was in three places of documentation actively claiming the opposite, all now fixed (doc-only, zero runtime change).
- User: stdin must never hang indefinitely, full stop — this is a hard product principle, not a tradeoff to weigh against data-loss risk. Implemented a genuine per-chunk-reset idle timeout rather than any variant of "bound only part of the read."
- Every `@sh` sub-agent diff across both rounds (10 fix commits total) was independently re-verified by the orchestrator before committing: full diff read, fresh build/vet, targeted tests, then a full uncached `-race` run of the affected package(s) — never trusted from the sub-agent's own report alone.
- `toolCleanupGrace`'s fix (task #205) deliberately avoided reintroducing tool-name-keyed special-casing (rejected pattern from the original `toolExecutionMaxDefault` unification) — the asymmetry is by agent ROLE (`isSubAgent`), checked inside `effectiveToolCleanupGrace()`, not by wiring anything new through `coordinator.go`.
- Task #208's lower-priority items were deliberately NOT fixed this round, matching `@oh`'s own stated priority ("can ride the next release") — tracked, not lost, not silently dropped.

## Open questions

None outstanding that need the user's input right now. Task #208 is prioritized and ready to pick up whenever wanted, no decision blocking it — just not yet requested.

## Repo state

```
 D web/dist/.gitkeep
```
```
f442ddcf docs: changelog entry for #203/#209/#210 (agents-single docs, stdin idle timeout)
cd9d505a fix(cmd): bound stdin's named-pipe read with a real per-chunk idle timeout
6cd85f87 docs(cmd): correct --agents single vs. configured-Worker precedence claims
33b0f502 fix(agent): re-arm activeRequests with runCancel before every turn-loop iteration
75b6abc5 fix(agent): make toolCleanupGrace asymmetric so the child actually wins
```
